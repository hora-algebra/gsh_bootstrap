# Claims Ledger

Every nontrivial statement used by the project belongs here. The status labels are:

- `PROVED`: proved in this repository, with a file/theorem location.
- `CITED`: taken from a named source, with theorem/page information where available.
- `COMPUTED`: established by a deterministic program and preserved input/output manifest.
- `CONJECTURAL`: a precise mathematical conjecture.
- `SPECULATIVE`: a research direction not yet precise enough to be a conjecture.
- `REFUTED`: false; counterexample recorded.
- `UNREVIEWED`: imported or generated statement awaiting audit.

Do not upgrade a status by editing prose. Add the verification artifact.

| ID | Claim | Status | Evidence / location | Owner | Last review |
|---|---|---|---|---|---|
| GSH-STATUS-01 | No regular language of generalized star-height greater than one is currently known in the surveyed literature. | CITED | Place–Zeitoun 2017, introduction; Bourne 2017, introduction; `docs/SURVEY.md` §1 | survey lead | 2026-07-22 |
| GSH-BASE-01 | Generalized star-height zero equals the class of star-free languages. | CITED | definition plus Schützenberger 1965 interface; `docs/SURVEY.md` §2 | language lead | 2026-07-22 |
| GSH-BASE-02 | A regular language is star-free iff its syntactic monoid is aperiodic. | CITED | Schützenberger 1965 | language lead | 2026-07-22 |
| PST-CL-01 | Bounded generalized star-height is closed under left/right quotients, inverse alphabetic morphisms, and injective star-free substitutions under the hypotheses in Pin–Straubing–Thérien. | CITED | Pin–Straubing–Thérien 1992; exact hypotheses must be checked before formal use | source auditor | 2026-07-22 |
| PST-GRP-01 | Every language recognized by a finite commutative group has generalized star-height at most one. | CITED | Pin–Straubing–Thérien 1992 | group lead | 2026-07-22 |
| PST-GRP-02 | The height-one result extends to finite nilpotent groups of class at most two. | CITED | Pin–Straubing–Thérien 1992, Theorem 7.3 as indexed in the open version | group lead | 2026-07-22 |
| PST-GRP-03 | The published semidirect-product result covers divisors of a semidirect product of a commutative group by an elementary abelian 2-group. | CITED | Pin–Straubing–Thérien 1992 | group lead | 2026-07-22 |
| BR-REES-01 | Languages recognized by finite Rees (zero-)matrix semigroups over abelian groups have generalized star-height at most one. | CITED | Bourne–Ruškuc 2016 | language lead | 2026-07-22 |
| SMALL-12-01 | In Bourne's finite-group ladder, all groups of order below 12 are covered; `A_4` and `Dic_3` are the unresolved order-12 cases in that program. | CITED | Bourne 2017, group-order discussion | group lead | 2026-07-22 |
| SMALL-C3-FAIL | The direct extension of the cited `A ⋊ C_2` argument to `A ⋊ C_3` fails at a unique-factorization step. | CITED | Bourne 2017, failure analysis | source auditor | 2026-07-22 |
| A5-SCOPE-01 | A theorem for languages recognized by `A_5` would not by itself settle the global generalized star-height problem. | PROVED | immediate from scope of the quantified theorem; `README.md` | referee | 2026-07-22 |
| A5-MATHLIB-01 | Mathlib provides `alternatingGroup (Fin 5)` and a simplicity theorem for degree five. | CITED | mathlib module `Mathlib.GroupTheory.SpecificGroups.Alternating` | Lean lead | 2026-07-22 |
| COH-01 | Group or monoid cohomology supplies an established invariant of generalized star-height. | REFUTED | no such theorem found in surveyed sources; never assume this statement | referee | 2026-07-22 |
| COH-02 | A concrete word-to-cochain map may organize extension/gluing arguments. | SPECULATIVE | `docs/SURVEY.md` §7; requires coefficient object and closure theorem | cohomology lead | 2026-07-22 |
| CERT-01 | The Python checker accepts only when the supplied generalized expression has the claimed height bound and its compiled DFA is equivalent to the target DFA. | PROVED | `tools/regex_cert.py`, `tests/test_regex_cert.py`; proof is program audit, not Lean | computational lead | pending first CI |
| LEAN-LANG-01 | Language concatenation and star definitions match the mathematical definitions in the blueprint. | UNREVIEWED | Lean source (moved unchanged 2026-07-23 from `GSH/Language/Basic.lean` to `GSH/Challenges/GeneralizedStarHeight.lean` §1); compiled in first local build 2026-07-22 (`PROOF_OBLIGATIONS.md` repair log); semantic review pending | Lean lead | 2026-07-23 |
| LEAN-DFA-01 | `DFA.run_append` is formalized. | UNREVIEWED | `GSH/Recognition.lean` §1 (moved unchanged 2026-07-23 from `GSH/Automata/DFA.lean`); compiled in first local build 2026-07-22; semantic review pending | Lean lead | 2026-07-23 |
| LEAN-A5-01 | The repository's `A5` abbreviation reduces to `alternatingGroup (Fin 5)`. | UNREVIEWED | `GSH/Groups.lean` (moved unchanged 2026-07-23 from `GSH/Groups/A5.lean`); compiled in first local build 2026-07-22 (simplicity hypothesis discharged via `Nat.card_fin`); semantic review pending | Lean lead | 2026-07-23 |
| GLOBAL-ONE | Every regular language has generalized star-height at most one. | CONJECTURAL | workshop north-star target; Lean registration as explicitly open challenge: `GSH/Challenges/GeneralizedStarHeight.lean` (`generalized_star_height_conjecture : GeneralizedHeightOneConjecture`, obligation L-GSH-CHALLENGE-001); since 2026-07-23 that file contains the whole definition chain in one place (imports only mathlib) | all | 2026-07-23 |
| A4-STD-01 | With the morphism φ(a) = (123), φ(b) = (12)(34) generating `A_4`, the language { w ∈ {a,b}* : φ(w) = e } has generalized star-height at most one. | COMPUTED | `RESULTS.md` §5–§6; atoms proved by exhaustive product-automaton search (`scripts/a4_attempt.py`), end-to-end agreement on all words of length ≤ 16 plus 30k random words (`scripts/a4_final.py`); commit dd316b4; run manifest in `data/experiments/` still pending | Kazumi Kasaura | 2026-07-22 |
| A4-FULL-01 | For the alphabet consisting of all twelve elements of `A_4` under the evaluation morphism, the word-problem language { w : eval(w) = e } has generalized star-height at most one. | COMPUTED | `RESULTS.md` §5.5; 93 token languages certified aperiodic, reachability = f(P, N[g,p]) proved by exhaustive 384-state product-automaton search (`scripts/a4_full3.py`), reconstruction checks exhaustive to bounded length plus random sampling (`scripts/a4_full12.py`); commit f4258df; run manifest in `data/experiments/` still pending | Kazumi Kasaura | 2026-07-22 |
| A4-ANYGEN-01 | For every monoid morphism φ from a free monoid over a finite alphabet to `A_4`, the language φ⁻¹(e) has generalized star-height at most one. | UNREVIEWED | `RESULTS.md` §6 note; follows from A4-FULL-01 via inverse-alphabetic-morphism closure (PST-CL-01) once the exact hypotheses of that closure are checked | Kazumi Kasaura | 2026-07-22 |
| FULL-ALPH-RED-01 | For every finite group `G` and `n ≥ 0`, the identity fibre over the full-element alphabet has generalized star-height at most `n` iff every language recognized by `G` has generalized star-height at most `n`. | PROVED | `notes/conway_group_identities_and_full_alphabet.md` §3 with self-contained closure lemmas §3.5 (Brzozowski derivative for single-letter quotients; literal-inverse-morphism substitution) — no longer depends on PST-CL-01; one adversarial review pass 2026-07-23 (AI, no gaps found incl. edge cases); machine companion `scripts/closure_lemmas_check.py` (400 expressions, exact DFA equivalence, all PASS); independent human review still pending | source auditor | 2026-07-23 |
| A4-ALLLANG-01 | Every language recognized by `A_4` has generalized star-height at most one. | COMPUTED | A4-FULL-01 (`COMPUTED`) composed with FULL-ALPH-RED-01 (now `PROVED`, self-contained); the ceiling stays `COMPUTED` because the weak link is the bounded-exhaustive-plus-random reconstruction check in A4-FULL-01 — this row must never be upgraded to `PROVED` without upgrading A4-FULL-01 first | Kazumi Kasaura | 2026-07-23 |
| CONWAY-GRP-01 | Conway's finite-group identity is built from the full-element Cayley matrix `M_G = (x_{p⁻¹q})`; under the singleton-letter language valuation, the `(p,q)` entry of `M_G*` is the fibre `μ_G⁻¹(p⁻¹q)`. | CITED | Conway 1971; Krob 1991; Ésik 1999; derivation and comparison in `notes/conway_group_identities_and_full_alphabet.md` §§1–2 | source auditor | 2026-07-23 |
| GSH-INPUTEXT-01 | If a new input letter is added to a finite group automaton and acts as a transformation already induced by a word, generalized star-height at most one of the identity fibre is preserved. | CONJECTURAL | precise formulation in `notes/conway_group_identities_and_full_alphabet.md` §6; analogous unbounded equational input-extension theorem is due to Ésik, but no bounded-depth analogue was found | all | 2026-07-23 |
| LAAB-04-01 | The language L(aab,0,4) = { w ∈ {a,b}* : binom(w, aab) ≡ 0 mod 4 } has generalized star-height at most one. | COMPUTED | `RESULTS.md` §3, §5; proved W atoms plus arithmetic reduction, composite checked on all words of length ≤ 13 plus 150k random words; commit dd316b4; run manifest in `data/experiments/` still pending | Kazumi Kasaura | 2026-07-22 |
| A5-GEN145-01 | With the morphism φ(a) = (123), φ(b) = (145) generating `A_5`, the language φ⁻¹(e) ⊆ {a,b}* has generalized star-height exactly one. | COMPUTED | `RESULTS.md` §5.6, `notes/A5_generator_dependent_star_height_1.md`; point-stabilizer filtration K_1 ∩ K_2 ∩ K_3 with star-free first-return codes; `scripts/a5_check.py` (⟨(123),(145)⟩ = A_5, trivial 3-point stabilizer, agreement on all words of length ≤ 16 plus 200k random words); height ≥ 1 because the syntactic monoid is `A_5` (non-aperiodic); commit 4ee1018; run manifest in `data/experiments/` still pending | Kazumi Kasaura | 2026-07-22 |
| WEIS-L2-M2-01 | Every Boolean combination of stage-2 staged ba*b pair counts N_{p,q} mod 2 or mod 4 (phases = cumulative a-count mod 2, over {a,b}) and odd-run segment counts mod 4 has generalized star-height at most one. | COMPUTED | RESULTS.md §5.9; notes/weis_l2_stage2_height1.md; exact product-automaton proofs, cell-table random checks (30k words, len ≤ 300) and independent `re` evaluation of an explicit height-1 expression in `scripts/weis_l2_family.py`; run manifest `data/experiments/weis_l2_stage2.md` | hora-algebra | 2026-07-22 |
| THOMAS-D2-02 | The segment-counting languages { w : #(b-terminated a-runs of odd length) ≡ h mod 4 } have generalized star-height at most one, by flat token counting over the finite prefix code {b, aa, ab} with the exact identity #ab = \|w\|_a + 2\|w\|_b − 2·#tok − [tail=a]; this upgrades the d=2 Thomas 1981 citation to a self-contained machine proof. | COMPUTED | `scripts/weis_l2_family.py` steps [2][6] (identity exhaustive to length 16; explicit expression verified with the independent `re` engine); notes/weis_l2_stage2_height1.md §3 | hora-algebra | 2026-07-22 |
| WEIS-L2-M3-01 | For stage-3 (phases mod 3) staged ba*b pair counting, T1 mod 2 (runs ≡ 1 mod 3) is a proved function of certified height-1 features, and the remaining stage-3 atoms are exactly NOT functions of the current certified feature family (LC, flags, finite-code flat counts incl. cascade code Z3, W atoms mod 6). | COMPUTED | `python3 scripts/weis_l2_family.py --m3` (exact product-automaton verdicts); obstruction analysis in notes/weis_l2_stage2_height1.md §5; PROOF_OBLIGATIONS.md N-L2-M3-001 | hora-algebra | 2026-07-22 |
| WEIS-L2-AUDIT-01 | The language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) over {a,b} — proposed by Pin–Straubing–Thérien 1992 and studied in Weis 2011 §4.4 — has minimal DFA with 6 states and syntactic monoid a non-nilpotent group of order 48, isomorphic to C₂×S₄. | CITED | Weis 2011 (UMass thesis, DOI 10.7275/2177022) p.115, full PDF retrieved 2026-07-23 via Wayback Machine; "proposed in [30]" fixes the attribution to PST 1992; order 48 independently recomputed from the printed regex (`scripts/weis_l2_actual.py`); the earlier "48 not reproduced" note reflected a family reconstructed from secondary descriptions, not the printed L2 — see `notes/weis_2011_primary_audit.md`; thesis-internal inconsistency at p.118–119 recorded there and not relied upon | hora-algebra | 2026-07-23 |
| WEIS-L2-OPEN-01 | Weis 2011 leaves the generalized star-height of the full language L2 open ("we do not know whether it has generalized star-height 2", p.115); only the restricted subset with ≥ 4 a's between b's is shown to have height 1. Full L3 is likewise left open. | CITED | Weis 2011 §4.4 pp.114–118 and abstract pp.vi–vii, retrieved 2026-07-23 (Wayback URLs in `notes/weis_2011_primary_audit.md`); this makes full L2 a literature-backed candidate proposed by PST 1992 and left open by Weis. This row is a statement about the thesis and stays valid; the candidate itself was resolved in this repository on 2026-07-25 (WEIS-L2-GSH-01: gsh(L2) = 1). Full L3 remains open both in the thesis and here | hora-algebra | 2026-07-23 |
| WEIS-TALK-01 | The 2009-11-03 UMass CMPSCI Theory Seminar abstract for P. Weis's talk "The Power of One Star" states that the talk presents four candidate languages and shows all four have generalized star height one. | CITED | abstract retrieved verbatim 2026-07-23 (direct fetch of people.cs.umass.edu/~barrington/thysemS12/3nov09.html); CAUTION: inconsistent with the 2011 thesis, which leaves full L2 and L3 open (WEIS-L2-OPEN-01) — do not read this row as "Weis proved full L2 has height 1"; the talk's four languages were plausibly restricted variants (inference, slides not retrieved) — `notes/weis_2011_primary_audit.md` §2.4 | hora-algebra | 2026-07-23 |
| SEARCH-CAL-01 | The size-ordered synthesis search recovers a verified height-1 certificate for the language (aa)* over {a,b} at binary-AST size 4. | COMPUTED | `tools/height_search.py`; certificate `data/certificates/height1_aa_star_synth.json`; rerun `python3 -m tools.height_search --target aa_star --max-size 6` | computational lead | 2026-07-22 |
| SEARCH-CAL-02 | No generalized regular expression over {a,b} with syntactic star height ≤ 1 and at most 13 binary-AST nodes defines { w : number of a in w is even }; the minimal such size lies in [14, 15]. This is a size bound, not a star-height lower bound. | COMPUTED | complete enumeration (593,575 distinct languages, no pruning) plus a checked size-15 witness; exact command `python3 -m tools.height_search --target even_a --max-size 13 --max-states 0`; commit, code sha256, and normalized-output sha256 in the reproducibility manifest of `data/experiments/height_search_even_a.md` (independently rerun 2026-07-22) | computational lead | 2026-07-22 |

| WEIS-L2-NOTFN-01 | Membership in the full language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) is not a function of the certified stage-2 feature family {LC(8,4), Flags(2), CodeTok({b,aa,ab}) mod 4, W(2,0,4), W(2,1,4)}, nor of its tested extensions (certified atoms N_{p,q} mod 4 and T' mod 4 added as features; doubled moduli; the --m3 constructors X3, Z3, W(3,r,6), LC(72,8)). Hence WEIS-L2-M2-01 does not decide the generalized star-height of full L2. (Status update 2026-07-25: full L2 is no longer open — WEIS-L2-GSH-01 gives gsh(L2) = 1 by changing the group action instead of extending the feature family. The negative result in this row stands as stated: it is about the certified feature family, and it is what showed that a new mechanism was needed.) | COMPUTED | exact product-automaton sweep (114 reachable product states, no sampling), minimal witnesses `bababbb` ∉ L2 vs `bbababb` ∈ L2 (identical feature vectors and certified atom values), extension witnesses `baaababbb`/`bbabaaabb`; triple-checked (product automaton, feature-object rerun, independent `re` evaluation); DFA sanity: 6 states, syntactic monoid order 48 ≅ C₂×S₄, non-nilpotent, solvable; `scripts/weis_l2_actual.py` | hora-algebra | 2026-07-23 |
| WEIS-L2-GSH-01 | The full language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) over {a,b} — proposed by Pin–Straubing–Thérien 1992 and left open in Weis 2011 (WEIS-L2-OPEN-01) — has generalized star-height exactly one. | COMPUTED | `RESULTS.md` §5.10; derivation in `notes/weis_l2_full_height_one.md`; complete finite proof in `scripts/weis_l2_full_gsh1.py`: the ground-truth 6-state DFA is derived from the PRINTED regex by exact compilation (no reliance on hand-entered automata), the syntactic group (order 48 ≅ C₂×S₄), its central antipodal involution, the induced action ψ on the four cube diagonals (ψ(a) a transposition, ψ(b) a 4-cycle, doubly-active diagonals = the anchor pair), star-freeness of both first-return languages, well-definedness of the letter parities and injectivity of (parities, ψ) on all 48 elements are each asserted; the assembled expression is checked to have star-height 1 syntactically, compiled exactly to a 6-state DFA, cross-validated against an independent recursive matcher (all words of length ≤ 7) and Python's `re` engine on the printed regex (all 8191 words of length ≤ 12), and proved language-equal to the ground truth by product reachability (no sampling). Lower bound: syntactic monoid is a nontrivial group, so L2 is not star-free (Schützenberger 1965). Independently re-verified by `tools/regex_cert.py` (CERT-01, a separate implementation) on the emitted certificate `data/certificates/height1_weis_l2_full.json`, which `scripts/check.sh` checks on every run (`PASS: equivalent; height=1 <= 1; minimal states expression=6, target=6`); run manifest `data/experiments/weis_l2_full_gsh1.md`. Remaining risk is program audit / independent human re-derivation, and a targeted L2-specific prior-art search before any external write-up | hora-algebra | 2026-07-25 |
| WEIS-L2-RSH-01 | The same L2 has restricted star-height exactly two; in particular gsh(L2) = 1 < rsh(L2) = 2, and the printed expression (restricted star-height 3) is not optimal for either measure. | COMPUTED | `RESULTS.md` §5.10 and `notes/weis_l2_full_height_one.md` §6; `scripts/weis_l2_restricted_height.py` exhausts all 2¹² labelled-edge subsets of the 6-state minimal DFA and finds exactly one accepting L2 (the full automaton) with loop complexity 2; depends on UNIV-SUBGRP-01 (`PROVED`) plus two CITED theorems verified verbatim in Lombardy–Sakarovitch, "The universal automaton" (2008): Thm. 7.5 (Eggan 1963, star height = minimal loop complexity) and Thm. 7.10 (Lombardy–Sakarovitch 2003, the universal automaton of a group language contains a subautomaton of minimal loop complexity); loop complexity computed exactly per Def. 7.4 | hora-algebra | 2026-07-25 |
| UNIV-SUBGRP-01 | Let φ : Σ\* → G be onto with G finite, H ≤ G a subgroup, L = φ⁻¹(H). Then the maximal factorizations of L with both components nonempty are exactly the pairs (Hg⁻¹, gH), they correspond bijectively to the left cosets gH, and the universal automaton restricted to them is deterministic and isomorphic to the coset automaton, i.e. to the minimal DFA of L. | PROVED | one-paragraph proof in `notes/weis_l2_full_height_one.md` §6 (factorizations are saturated by the syntactic congruence, so the condition AB ⊆ H over subsets of G forces A = Hb₀⁻¹ and B = b₀H; transitions computed from Lombardy–Sakarovitch Def. 2.4 give a unique target coset); consistency check with the literature: TUA's remark after Cor. 7.12 states exactly this for W_q = {w : \|w\|_a ≡ \|w\|_b mod 2^q}, the H = {1} case; used by WEIS-L2-RSH-01 | hora-algebra | 2026-07-25 |
| A4-STD-02 | The A4-STD-01 height-one representation (φ(a) = (123), φ(b) = (12)(34)) is confirmed by an independent complete finite proof, not only by bounded-length and random agreement. | COMPUTED | `scripts/a4_std_dfa_equivalence.py`, written from the `RESULTS.md` §5 prose alone: the arithmetic characterization is checked against the group on all 32767 words of length ≤ 14, the explicit height-1 expression is compiled exactly to a 12-state DFA, the compiler is cross-validated against an independent recursive matcher (length ≤ 9), and language equality with the 12-state A4 word-problem DFA is proved by product reachability (complete, no sampling). This strengthens the evidence of A4-STD-01 for the two-generator case; A4-FULL-01 (all twelve letters) is untouched and remains bounded-exhaustive-plus-random | hora-algebra | 2026-07-25 |
| WATOM-45-01 | The staged-count atoms `{ w ∈ {a,b}* : N_r(w) ≡ h mod 5 }`, where `N_r(w)` counts the occurrences of `a` whose number of preceding `b`'s is `≡ r mod 4`, have generalized star-height at most one for every `r ∈ Z/4` and `h ∈ Z/5`. This is the W-atom scheme of `RESULTS.md` §5 at the new parameters (phases mod 4, counts mod 5). | COMPUTED | `scripts/f20_word_problem.py` check [3b]: with opener `(a*b)^r`, token set `X = a ∪ b a* b a* b a* b`, tails `(b a*)^s` and the identities `B = r + 4f + s`, `t = N_r + f`, each of the 20 atoms is compiled exactly and compared with the direct staged count on all 8191 words of length ≤ 12, and cross-checked with an independent recursive matcher on all 511 words of length ≤ 8; each atom is verified to have syntactic height 1; run manifest `data/experiments/f20_word_problem.md` | hora-algebra | 2026-07-25 |
| F20-STD-01 | With the morphism φ(a) = (x ↦ x+1), φ(b) = (x ↦ 2x) onto `F_20 = AGL(1,5) = C_5⋊C_4` (faithful action), the word problem φ⁻¹(e) ⊆ {a,b}* has generalized star-height exactly one. `F_20` is the smallest non-abelian group outside the PST class (`FRONTIER-ORD20-01`), so this is the first height-one result inside the new frontier. | COMPUTED | `RESULTS.md` §5.11; `scripts/f20_word_problem.py`. Upper bound: the arithmetic characterization "the number of `b`'s is `≡ 0 mod 4` and `N_0 + 3N_1 + 4N_2 + 2N_3 ≡ 0 mod 5`" (coefficients `2^{-p} mod 5`) is verified against the group on all 32767 words of length ≤ 14; the height-1 expression built from `WATOM-45-01` compiles exactly to a 20-state DFA and is proved language-equal to the 20-state Cayley automaton by complete product reachability (no sampling); a second, transparent W-atom assembly independently compiles to the same language (check [5b]); independently re-verified by `tools/regex_cert.py` (CERT-01, separate implementation) on `data/certificates/height1_f20_word_problem.json`, which `scripts/check.sh` checks on every run. Lower bound: the minimal DFA is the Cayley automaton of `F_20`, so the syntactic monoid is a nontrivial group, not aperiodic, hence not star-free (Schützenberger 1965). CAUTION: this is one morphism and one accepting set; it does NOT prove `HeightOneForGroup F_20`, which by `FULL-ALPH-RED-01` needs the full 20-letter alphabet — `F_20` stays open in `FRONTIER-ORD20-01`. Run manifest `data/experiments/f20_word_problem.md` | hora-algebra | 2026-07-25 |
| F20-FULL-OBS-01 | For the full 20-element alphabet of `F_20`, the multi-mover mechanism of `A4-FULL-01` (`RESULTS.md` §5.5, pattern-conditioned cuts) fails on every one of its 291 candidate cut patterns, and the failure is caused exactly by the five letters with `ε = 2` (`α = 4`, the involution of `C_4`): the phase group `Z/4` is composite, so such a letter has phase orbit `{0,2}` and bounces between the odd phases `1 ↔ 3` without ever meeting the cut phase, giving a period-2 element of the transition monoid. The `A_4` mechanism silently required the phase group to have prime order. Separately, the GF(5) linear algebra is *not* the bottleneck: formally assuming the uncertified staged features, `β` would be determined. | COMPUTED | `scripts/f20_full_alphabet.py` (exact, full transition-monoid enumeration, 0.1 s): the 291-pattern table is 0 certified; the localization experiment certifies the sub-alphabets `ε ∈ {0,1}`, `{0,2}`, `{0,3}` and the 15-letter `ε ∈ {0,1,3}`, and fails on `{0,1,2}`, `{0,2,3}`, `{0,1,2,3}` — which also serves as a positive control proving the judge is not trivially answering "not aperiodic"; GF(5) ranks 8/8, 7/8 (β-neutral null direction), 8/8 formally versus 20/80 soundly. Derivation `notes/f20_full_alphabet_obstruction.md`; run manifest `data/experiments/f20_full_alphabet.md`. CAUTION: this is NOT a lower bound (research rule 1) — one known mechanism does not apply, nothing more; `F20-STD-01` is unaffected; the script's length-4 "minimal witness" pair has the same letter multiset and therefore only shows that count-level features are insufficient, which was expected and must not be over-reported | hora-algebra | 2026-07-25 |
| F20-SUB10-OBS-01 | Let `Σ₁₀` be the 10-letter sub-alphabet `{ g ∈ F_20 : ε_g ∈ {0,1} }`, which generates `F_20`. The certified feature family of the `A4-FULL-01` mechanism (`RESULTS.md` §5.5) over `Σ₁₀` — total phase, arrivals per phase, per-nonmover arrivals per phase, certified pattern-conditioned cuts, letter counts mod 5, first and last letter, all closed under reversal — does **not** separate the group element. On `Σ₁₀` no pair pattern is certifiable at all (every one has a period-2 element, period 4 for same-letter pairs), so the mechanism has no way to order two distinct letters carrying the same `ε`, and their `β` contributions differ. Minimal witness at length 4: with `k = g(ε=0,β=0)`, `u₀ = g(ε=1,β=0)`, `u₁ = g(ε=1,β=1)`, the words `k u₀ u₁ k` and `k u₁ u₀ k` agree on every certified feature in both directions but have images `(4,1)` and `(4,2)`. This is a distinct obstruction from `F20-FULL-OBS-01`: that one is about the base cut failing aperiodicity because of the `ε = 2` letters, this one is about expressive power and appears with no `ε = 2` letter present. It refutes route (ii) of `N-F20-001`. | PROVED | Human proof in `notes/f20_subalphabet_obstruction.md` §2 (seven clauses: identical `ε` sequence hence identical phase trajectory, hence identical arrivals; identical nonmover arrivals; no certified pair pattern exists; identical letter multiset and endpoints; reversal symmetry; and `β = 2β_{u₀} + β_{u₁}` versus `2β_{u₁} + β_{u₀}`, distinct mod 5 because `β_{u₀} ≠ β_{u₁}`). Its one computed input, the complete certified pattern table, is exact finite enumeration of the transition monoid including same-letter pairs. `scripts/f20_subalphabet_obstruction.py` checks every clause mechanically, verifies minimality by exhaustive search per length (1,765 words for 10 letters, 5,045 for 15), shows the witness survives on the 15-letter sub-alphabet, shows it survives when 555 uncertified `Σε = 0` patterns are granted for free, and runs a positive control on the 2-generator alphabet where the same family does separate the image on all 8,191 words of length at most 12. Run manifest `data/experiments/f20_subalphabet_obstruction.md`. CAUTION: this is NOT a lower bound (research rule 1) — it bounds one mechanism, and says nothing about the actual height of any language; `F20-STD-01` is unaffected | hora-algebra | 2026-07-25 |
| F20-BLOCK-OBS-01 | **Delay theorem.** Let `G` be a finite group and `Σ = G` the full-element alphabet, so the identity element is a letter. Then every *finite* code `X ⊆ Σ⁺` with bounded delay (i.e. `Σ* = X* F` for some finite `F`) satisfies `mu(X) = G`; and if it only has to parse the identity fibre `T`, then `mu(X) ⊇ G ∖ {e}`. Consequence for route (iii) of `N-F20-001`: the token word of a block decomposition is again a word over an alphabet of group elements obeying the same coordinate formula, so the decomposition is a **self-reduction with no gain** — for `F_20` the token alphabet again carries `ε = 2` elements, the base cut again has a period-2 element and none of the 291 candidate patterns certify. Giving up finiteness does not help: the one canonical phase-neutral code, the first-return code `R` with `T = R*`, is infinite and also not star-free, because its DFA **is** the §5.5 base cut of `F20-FULL-OBS-01`. | PROVED | Human proof in `notes/f20_block_decomposition.md` §2 (three lines: `z e^D` has a prefix in `X`, and every nonempty prefix of it has image `z`) and §5. `scripts/f20_block_decomposition.py` checks every step exactly and without sampling: the token-level coordinate formula agrees with the letter level on all 336,842 words of length ≤ 4 for the codes `Σ²` and `Σ³`; the witness families `z e^n` and `z e^n z^{-1} ∈ T` for all 20 resp. 19 elements; all 20 maximal proper token images unrealizable on the full alphabet, reduced from all proper images by a verified monotonicity check; a realizability criterion on the image walk cross-checked against direct enumeration of the code-free set on 63 (alphabet, image) pairs with 0 disagreements, every realizable case witnessed by an explicitly constructed frontier code; 0 of 291 token-level patterns certify; a single `ε = 2` token still has period 2 on the genuine 400-token alphabet `Σ²` (1,606 DFA states), so token multiplicity does not repair aperiodicity. Positive control: on the 2-generator alphabet of `F20-STD-01` finite bounded-delay codes with a **proper** token image do exist (minimum size 2, and 4 when length-1 tokens are forbidden, realized by `X = Σ²`), so the theorem is specific to the alphabet containing the identity element and the judge is not answering "unrealizable" by default. Run manifest `data/experiments/f20_block_decomposition.md`. CAUTION: this is NOT a lower bound (research rule 1) — it closes one mechanism. The residual route named when this row was first written — star-free **infinite** codes — was **closed the same day by `F20-BASECODE-01`**, which also shows it was never viable (it fails on the 2-generator instance where height one is proved). Still not excluded: non-greedy or context-dependent factorizations, and finite unions using a different code per case; neither is yet in the shape of a mechanism | hora-algebra | 2026-07-25 |
| FULL-ALPH-RED-02 | The full-alphabet reduction of `FULL-ALPH-RED-01` needs only `\|G\| - 1` letters: the identity element may be deleted from the alphabet. Precisely, let `pi : Σ* → (Σ ∖ {e})*` erase the identity letter; then `mu = mu ∘ pi`, so `T_Σ = pi^{-1}(T_{Σ∖{e}})`, and `pi^{-1}` preserves generalized star height ≤ 1. Hence `gsh(T_{Σ∖{e}}) ≤ 1` implies `gsh(T_Σ) ≤ 1`. | PROVED | Human proof in `notes/f20_block_decomposition.md` §6, four steps: `pi^{-1}` commutes with Boolean operations; `pi^{-1}(L₁L₂) = pi^{-1}(L₁) pi^{-1}(L₂)` because erasure neither merges nor splits letters; `pi^{-1}` of a star-free language is star-free, since it is recognized by the same monoid and syntactic monoids divide; and the star step `pi^{-1}(A*) = (B ∪ {e})*` where `B = pi^{-1}(A)` minus the empty word and minus every word ending in `e`, with `B ∪ {e}` star-free whenever `A` is. Steps 2 and 4 are machine-checked in `scripts/f20_block_decomposition.py` §6 (9,837 concatenation instances; the star identity on all 3,280 words of length ≤ 7 for four test codes), together with `mu(w) = mu(pi(w))` on all 8,421 words of length ≤ 3. The naive guess `pi^{-1}(A*) = ({e} ∪ A)*` is **false** and the script exhibits its failures (counterexample `A = {a, ka}`, `w = k e a`), which is also the control proving the checker is not vacuous. CAUTION: `pi` is a non-alphabetic morphism, and PST 1992 item 7 records that non-alphabetic inverse morphisms do NOT preserve height in general; the claim here is the special case of erasure, with step 2 identified as what fails for length-increasing morphisms. Registered for independent audit as `N-ERASE-AUDIT-001` and NOT to be used as settled folklore until that audit closes. This reduction is generic: it applies to `A_5`, `C_7⋊C_3` and `S_4` as well. It does not by itself help route (iii) — see `F20-BLOCK-OBS-01`, where 19 letters still force `\|mu(X)\| ≥ 19` | hora-algebra | 2026-07-25 |
| F20-BASECODE-01 | The identity fibre `T = mu^{-1}(e)` is a **biunitary** submonoid of the free monoid (`uv ∈ T` and `u ∈ T` imply `v ∈ T`, and symmetrically), hence a **free** submonoid, so its base is unique: the first-return code `R_T = { w ∈ T ∖ {ε} : no proper nonempty prefix of w lies in T }`. Therefore the question "is there a star-free **code** `C` with `C* = T`?" is a single decision problem, and the answer is **no**: the minimal DFA of `R_T` has 22 states and the single letter `a = (x ↦ x+1)` has period 5 in its syntactic monoid, so `R_T` is not aperiodic and by Schützenberger 1965 not star-free. **Calibration, and the point of the row**: the same computation on the 2-generator alphabet — where `gsh(T) = 1` is already established by `F20-STD-01` — also returns "not star-free" (22 states, period 5). A star-free `C` with `C* = T` is only a *sufficient* condition for height one, and it fails on an instance where height one holds; so this route could never have been the mechanism behind a height-one representation. | COMPUTED | `scripts/f20_block_decomposition.py` §8, exact and without sampling: biunitarity on all words of length ≤ 4 (8,422 fibre words); the base equals the first-return code, checked as `T` minus the products of two nonempty fibre words (7,240 words agree exactly); non-aperiodicity by an explicit period-5 element of the syntactic monoid (the transition monoid of the *minimized* DFA — minimizing first is required, since a period in a non-minimal automaton can die in the quotient). First established by full transition-monoid enumeration (20,971,502 elements on 20 letters, 294,881 on 2 letters, max period 5 in both); the script carries the `O(states)` witness version of the same verdict. Run manifest `data/experiments/f20_block_decomposition.md`. CAUTION: this is NOT a lower bound (research rule 1). Scope: decided for `C` a **code**. For an arbitrary generating set `R_T ⊆ C ⊆ T` not required to be a code it is undecided here — two data points only (`C = R_T` above, and `C = T`, whose syntactic monoid is the group `F_20`) — but the calibration removes the motive for pursuing it | hora-algebra | 2026-07-25 |
| DIC3-RED-01 | `Dic_3` embeds into `H = (C_3×C_4) ⋊ C_2` (τ acting by inversion on `C_3`), and `H` lies in the Pin–Straubing–Thérien class "abelian by elementary abelian 2-group"; via the injection, every `Dic_3`-recognized language is `H`-recognized. | PROVED | `RESULTS.md` §3 (order-12 audit): explicit embedding a ↦ ((1,0),0), b ↦ ((0,1),1), relations, injectivity, accepting-set transfer via injectivity; exhaustively machine-verified in `scripts/dic3_embedding.py` (relations, 12 distinct images, Dic3 order profile, abelian normal `C_3×C_4` with index 2, no A4-profile subgroup of order 12 in `H`); adversarial review pass 2026-07-23 | hora-algebra | 2026-07-23 |
| DIC3-ALL-01 | Every language recognized by `Dic_3` has generalized star-height at most one. | CITED | PST-GRP-03 (Pin–Straubing–Thérien 1992 semidirect-product theorem) applied through DIC3-RED-01; closes the `Dic_3` half of Bourne's order-12 barrier (SMALL-12-01, SMALL-C3-FAIL) by routing around the `C_3` unique-factorization obstruction instead of repairing it | hora-algebra | 2026-07-23 |
| PST-DIV-CRIT-01 | If a finite group `G` divides `A ⋊ E` with `A` commutative and `E` elementary abelian 2, then `G` has a commutative normal subgroup `B` with `G/B` elementary abelian 2. Equivalently (and decidably): some commutative normal `B ◁ G` satisfies `x² ∈ B` for all `x` and `[x,y] ∈ B` for all `x,y`. Failure of this condition *proves* that `G` divides no such semidirect product, for any size of `A` and `E`. | PROVED | one-paragraph proof in `notes/small_group_pst_frontier.md` §2; the same exact sequence already appears in `RESULTS.md` §3 for the special case `G = A_4`, so the contribution here is the extraction of a decidable criterion, not the argument; implemented and exercised in `scripts/small_group_pst_coverage.py` (`pst_necessary_criterion`) | hora-algebra | 2026-07-25 |
| DICM-EMB-01 | For every `n ≥ 2` the dicyclic group `Dic_n`, generated by `x, y` with defining relations `x^{2n} = 1`, `y² = xⁿ`, `y⁻¹xy = x⁻¹`, embeds into `H_n = (C₂ × C_{2n}) ⋊ C₂`, where the `C₂` acts by `u ↦ u vⁿ`, `v ↦ v⁻¹`, via `x ↦ v`, `y ↦ u t`. Hence every dicyclic group — in particular every generalized quaternion group `Q_{2^k}` — lies in the PST-GRP-03 class and has generalized star-height at most one for all recognized languages. | PROVED | three-line proof in `notes/small_group_pst_frontier.md` §3 (`(ut)² = u·φ(u) = vⁿ`, `(ut)⁻¹v(ut) = v⁻¹`, and the `4n` images are distinct); the explicit map is machine-verified to be an injective homomorphism for `n = 2..12` in `scripts/small_group_pst_coverage.py` step [5]; generalizes `DIC3-RED-01` (which used the different host `(C_3×C_4)⋊C_2`) from `n = 3` to all `n`; run manifest `data/experiments/small_group_pst_coverage.md` | hora-algebra | 2026-07-25 |
| PST-WREATH-06-01 | Pin–Straubing–Thérien 1992 also prove: if a rational language is recognized by a monoid in the variety generated by wreath products `M ∘ (G ∘ N)` with `M`, `N` aperiodic monoids and `G` a commutative group, then it has generalized star-height at most one (the result indexed as item 6 in `docs/SURVEY.md` §3). | CITED | statement of the theorem's shape taken from the paper's abstract, retrieved 2026-07-25 via web search of the HAL/ScienceDirect records; CAUTION: the full-text PDF (`hal.science/hal-00019978v1/file/StarHeight.pdf`) is behind anti-bot protection and ScienceDirect returns 403, so the wording has NOT been verified verbatim against the primary source and the exact wreath-product convention is unknown; `PST-WREATH-COMM-01` is deliberately proved so that it is insensitive to that convention. **Superseded 2026-07-25 by `PST-WREATH-78-01`**, which has the theorem number (7.8) and the sharp form from a quotable secondary source: the base factor must be **aperiodic**. That also explains `PST-WREATH-COMM-01` structurally rather than by computation | hora-algebra | 2026-07-25 |
| PST-WREATH-COMM-01 | Let `V` be the monoid pseudovariety generated by wreath products `M ∘ (G ∘ N)` with `M`, `N` finite aperiodic monoids and `G` a finite commutative group. Then every group in `V` is commutative. Consequently the Pin–Straubing–Thérien wreath-product result (`PST-WREATH-06-01`) settles the full solution for no non-abelian group whatsoever. | PROVED | proof in `notes/small_group_pst_frontier.md` §3.5: the projection of a wreath product onto its non-aperiodic-free factor is a monoid morphism whose restriction to a subgroup has kernel inside a direct power of an aperiodic monoid, hence trivial, so subgroups embed downward twice and land in `G^N`; combined with the standard fact that a group dividing a finite monoid divides one of its subgroups (`CITED`, standard semigroup theory). The argument is symmetric in the wreath-product convention, so it does not depend on the unverified wording of `PST-WREATH-06-01`. This closes, inside PST 1992, the coverage caveat attached to `FRONTIER-ORD20-01` | hora-algebra | 2026-07-25 |
| SMALL-NONAB-31-01 | Among the 45 non-abelian groups of order at most 31, exactly six lie outside the PST-GRP-03 class: `A_4` (12), `F_20 = C_5⋊C_4` faithful (20), `C_7⋊C_3` (21), `SL(2,3)` (24), `S_4` (24), `C_2×A_4` (24). All other 39 are covered by `PST-GRP-02` (nilpotent of class 2) or `PST-GRP-03` (split decomposition, or an explicit embedding). All six exceptions are solvable. | COMPUTED | `scripts/small_group_pst_coverage.py` (exact, no sampling, ~3 s): constructions validated exhaustively (closure/associativity/identity/inverses), the catalogue verified pairwise non-isomorphic with per-order counts matching the standard classification (`CITED`), nilpotency class from the upper central series, split decompositions by exhaustive subgroup search, non-membership by `PST-DIV-CRIT-01` (exact, not a search), positive divisor cases by explicit embeddings; the run reproduces `SMALL-12-01` as an assertion; run manifest `data/experiments/small_group_pst_coverage.md`; derivation `notes/small_group_pst_frontier.md` §4 | hora-algebra | 2026-07-25 |
| FRONTIER-ORD20-01 | Combining `SMALL-NONAB-31-01` with `A4-ALLLANG-01`, the smallest non-abelian group whose full solution is not settled by the theorems audited in this ledger is `F_20 = C_5⋊C_4` (order 20), followed by `C_7⋊C_3` (21) and by `SL(2,3)`, `S_4`, `C_2×A_4` (24). This replaces "the barrier is at order 12" (`SMALL-12-01`) by "the barrier is at order 20". | COMPUTED | group-theoretic content is `SMALL-NONAB-31-01`; CAUTION on scope: the literature side is `UNREVIEWED` — the audit covers `PST-GRP-01/02/03`, plus (added 2026-07-25) `PST-WREATH-COMM-01`, which proves that PST 1992's wreath-product result covers no non-abelian group at all and so cannot settle any of the six; what remains unchecked is other 1992–2026 literature, for which no search specific to these six groups was made; "outside the PST class" is never a star-height lower bound (research rule 1); `notes/small_group_pst_frontier.md` §§5–6 | hora-algebra | 2026-07-25 |
| ORD12-ALL-01 | Every language recognized by a group of order at most 12 has generalized star-height at most one. | COMPUTED | order < 12: PST 1992 Corollary 7.7 (`CITED`); `C_12`, `C_6×C_2`: PST-GRP-01; `Dih_6 = C_6⋊C_2`: PST-GRP-03; `Dic_3`: DIC3-ALL-01; `A_4`: A4-ALLLANG-01 — the label is the weakest link (`A_4` base case is `COMPUTED`); order-12 classification into 5 groups is standard (Sylow); independent human review pending for the `A_4` chain | hora-algebra | 2026-07-23 |

| CORE2-EQV-EXT-01 | For every finite alphabet, the global height-one collapse conjecture is equivalent to: for all star-free languages A,B,C,D, the language (A ∪ B D* C)* has generalized star-height at most one. | UNREVIEWED | External merged-main proof note `hora-algebra/exploring-math/ELEMENTARY_EXPANSION_TWO_STATE_EQUIVALENCE.md`; cross-repository audit in `RESULTS.md` §6.1; not yet independently reconstructed or formalized here | cross-repo audit | 2026-07-24 |
| CORE2-4096-EXT-01 | Every tuple in the designated 4096 singleton-word CORE2 family has generalized star-height at most one. | UNREVIEWED | External exact audit merged in `exploring-math` PR #2, especially `generalized-star-height/CORE2_FINAL_PAIR_EXPLICIT_GH1.md` and `check_core2_final_pair_explicit_gh1.py`; merge commit 8ca43507f6ab2681535606abd3ac4215ffddab61; not replayed here | cross-repo audit | 2026-07-24 |
| GH1-KR-EXT-01 | For every n ≥ 3 there is a finite binary prefix code C_n with T_n dividing the syntactic monoid of C_n*, while C_n* has generalized star-height at most one; binary finite-code star syntactic monoids are division-universal. | UNREVIEWED | External proof/audit `exploring-math/generalized-star-height/FINITE_CODE_TN_LIFT_PROBE.md`, merged in PR #2; exact n=4 replay and primary references recorded there; not independently audited here | cross-repo audit | 2026-07-24 |
| CORE2-A5-RAW-EXT-01 | For the fixed order-5/order-5 CORE2 tuple A=S, B=C=D=∅ in exploring-math, the complete raw relation image has 2723 elements and all raw-stamp-recognized languages have an external fixed-stamp GH1 certificate; this does not settle the a5_235 word problem. | UNREVIEWED | External exact audit `exploring-math/generalized-star-height/CORE2_CONCRETE_A5_RAW_NUQ.md`, merged in PR #2; no local replay | cross-repo audit | 2026-07-24 |
| CORE2-SCH1-EXT-01 | For the fixed C4 benchmark, the full finite pool of 2178 one-marker languages leaves a period-4 relative-orbit witness, while the canonical full Sch1 profile search is inconclusive after 1,000,000 joint states without closure. | UNREVIEWED | External exact/cutoff records in `exploring-math` PR #2: `CORE2_C4_FULL_MARKED_POOL_CEGAR_20260722.md` and `CORE2_SCHUETZENBERGER_PROFILE_COMPLETENESS_20260722.md`; not replayed here | cross-repo audit | 2026-07-24 |
| F20-FIB-01 | The `F_20` word problem is exactly the trivial-holonomy condition of a flat affine `Z/5`-bundle. Writing elements as `(α, β)` with `α = 2^ε`, the map `ε : Σ* → Z/4` is a monoid morphism (the base projection) and `β : Σ* → Z/5` satisfies `β(uv) = ε(v)·β(u) + β(v)`, i.e. `β` is a **1-cocycle (crossed homomorphism)** for the `Z/4`-action pulled back along `ε`. The §5.12 coordinate formula `β(w) = Σ_i β_i 2^{E_i}` with `E_i` the suffix phase is not a derived gadget but the unique solution of that condition, and `E_i` is the base holonomy of the suffix. Over a free monoid there is no cocycle obstruction: `Z¹(Σ*, Z/5) = (Z/5)^Σ`, so the difficulty is the *definability* of `β`, never its existence. | PROVED | Induction on word length from the product law, in `notes/f20_fibration_geometry.md` §1. `scripts/f20_fibration_geometry.py` §1 checks the cocycle identity on all 177,241 pairs of words of length ≤ 2 over the full 20-letter alphabet, on 4,000 random pairs of length ≤ 11 each, and checks freeness by extending 200 random assignments of fibre values to the letters. Ground truth is the direct group evaluator, never the formula. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| F20-COH-SEP-01 | The cohomology of the extension **cannot** separate the settled cases from the open one. In the family `C_5 ⋊_r C_4`, `r ∈ {1, 4, 2}` — namely `C_20`, `Dic_5` and `F_20` — one has `H^n(C_4, Z/5) = 0` for every `n ≥ 1` and every one of the three module structures (orders 4 and 5 are coprime). So `H² = 0` (all three split) and `H¹ = 0` (splitting unique up to conjugacy): the classifying data of the extension is identical, and zero, for all three. Yet `C_20` is settled by `PST-GRP-01`, `Dic_5` by `PST-GRP-03` + `DICM-EMB-01`, and `F_20` is the open frontier `FRONTIER-ORD20-01`. Hence no functor of the extension's classifying data is a generalized-star-height invariant. | COMPUTED | `scripts/f20_fibration_geometry.py` §2–§3, exact: `H^n` for `n = 0..4` from the **bar resolution** by F_5 linear algebra (the periodic-resolution formula for cyclic groups is deliberately not used), plus the family table — pairwise non-isomorphic, `r=1 ≅ C_20` and `r=4 ≅ Dic_5` machine-verified, and `pst_necessary_criterion` (`PST-DIV-CRIT-01`) returning a witness for `r = 1, 4` and `None` for `r = 2`. This strengthens `COH-01` from "no such theorem was found" to "extension cohomology provably cannot decide it"; it does **not** rule out some other cohomological construction. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| F20-MONO-FRONT-01 | Inside the order-20 family `C_5 ⋊_r C_4` the settled/open boundary is a function of **one** parameter, the order of the monodromy `r` in `(Z/5)^×`: orders 1, 2, 4 give `C_20` (settled), `Dic_5` (settled), `F_20` (OPEN). The same integer is (a) the dimension of the hard irreducible representation — `Ind_{C_5}^{G}(χ)` is a sum of `4/ord(r)` distinct irreps of dimension `ord(r)`, and for `F_20` orthogonality gives `5·1_{g=e} = 1_{ε(g)=0} + χ_ρ(g)`, so the four linear characters supply only the base counting language and a single 4-dimensional induced character carries the whole difficulty; and (b) half the 5-primary cohomological period of `G`, which is `2·ord(r)` = 2, 4, 8. The PST condition "abelian by elementary abelian 2" reads, in this language, as "the monodromy is an involution". | COMPUTED | `scripts/f20_fibration_geometry.py` §3, §4, §7: `⟨Ind χ, Ind χ⟩` computed exactly in `Z[ζ_5]` from the coset formula (4, 2, 1), dimensions read off by Clifford theory; an explicit 4×4 monomial model of `ρ` verified a homomorphism on all 400 products, with trace equal to the coset character, exactly one nonzero entry per row and column, and permutation part a function of `ε(g)` alone; the character identity checked on all 20 elements; the period via the LHS collapse, whose short proof is in `notes/f20_fibration_geometry.md` §5 and whose input (`H^{2k}(C_5, Z) = Z/5` with `C_4` acting by `r^k`) is CITED standard. CAUTION: "settled" for `Dic_5` rests on `DICM-EMB-01` (PROVED) plus `PST-GRP-03` (CITED); the correspondence with PST's actual proof method is `UNREVIEWED` | hora-algebra | 2026-07-25 |
| F20-TRANSD-RED-01 | Let `Γ = Z/4 × Σ`, let `σ : Σ* → Γ*` be the length-preserving right-sequential transducer tagging each letter with its suffix phase (state set `Z/4`, the monodromy group itself), and let `K = { v ∈ Γ* : Σ_{(p,g)∈v} β_g 2^p ≡ 0 mod 5 }`, which is recognized by the commutative group `C_5`. Then `mu⁻¹(e) = {w : ε(w) ≡ 0 mod 4} ∩ σ⁻¹(K)`. Both factors have gsh ≤ 1 by `PST-GRP-01`, and Boolean operations do not raise generalized star height, so **`HeightOneForGroup F_20` follows if `σ⁻¹` preserves gsh ≤ 1** (via `FULL-ALPH-RED-01`). The step is not formal: `σ⁻¹` does **not** preserve star-freeness — `A·Γ*` uses no star yet its preimage has a 5-state minimal DFA with a period-4 element — so the needed closure cannot come from any star-free closure theorem, and the gap to `PST-CL-01` (inverse *alphabetic* morphisms) is exactly "one state" versus "monodromy many states". | PROVED | Immediate from `F20-FIB-01`; `notes/f20_fibration_geometry.md` §6, §8. `scripts/f20_fibration_geometry.py` §5, §8: the identity on all 168,421 words of length ≤ 4 over the full alphabet and all 8,191 of length ≤ 12 over two generators, against the direct group evaluator; three negative controls (constant weights, two transposed, one zeroed) wrong on 173/160/109 words, with the note that a cyclic weight shift is *not* a control since it scales by a unit; the wreath embedding `F_20 ↪ C_5 ≀ C_4` verified injective and multiplicative on all 400 products; non-preservation of star-freeness by transition-monoid enumeration of the minimized DFA. **CALIBRATION PASSED** (`F20-BASECODE-01` rule): specialised to `{a, b}` this is verbatim the published characterization of `F20-STD-01`, agreeing with the group on all 32,767 words of length ≤ 14 — the first `N-F20-001` mechanism to survive the 2-generator test. CAUTION: for this **single** `K` the reduction is close to a restatement and falls under the AGENTS.md stop condition, so it must not be attacked in that form; the usable content is `TRANSD-ABEL-01`. Prior art unsearched (`N-FIB-PRIOR-001`): this looks like a specialisation of Straubing's wreath product principle, and novelty must not be claimed — the derivation here is elementary and depends on no citation beyond `PST-GRP-01`. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| TRANSD-ABEL-01 | Let `σ` be a length-preserving sequential function whose transducer has a finite **abelian group** as its state monoid. Then `σ⁻¹` preserves generalized star height ≤ 1. | CONJECTURAL | Motivated by `F20-TRANSD-RED-01`, of which the case (state group `C_4`, input a `C_5`-counting language) is `HeightOneForGroup F_20`. Strictly stronger than that target, hence not blocked by the stop condition that applies to the single-language form. Reading of the known results, `UNREVIEWED`: applying the same construction to `A ⋊ E` with `A` commutative and `E` elementary abelian 2 gives a transducer with state group `E`, so `PST-GRP-03` would be exactly the elementary-abelian-2 case of this conjecture — whether PST 1992's proof actually proceeds this way is unverified for the same reason as `PST-WREATH-06-01` (primary source full text unobtained). Independent of that reading, the *reach* of the two statements coincides on the order-20 family (`F20-MONO-FRONT-01`). Expected to be **false** for non-abelian state groups; a counterexample there would be cheap and would correctly delimit the scope, since otherwise `A_5` would fall and contradict `GSH-STATUS-01`. **Prior-art search run 2026-07-25 (`N-FIB-PRIOR-001`), outcome: not found stated, and correctly placed.** The construction is standard (`EIL-WPP-01`), so no novelty is claimed for it; the aperiodic-state case is a theorem (`PST-WREATH-78-01`) and the abelian-state case was not located in the surveyed sources, neither proved nor refuted. The scope is now pinned by `TRANSD-LADDER-01`: unrestricted state monoid is equivalent to the entire generalized star-height conjecture, and this abelian form would settle every finite solvable group. Search coverage was limited — PST 1992's full text remains unobtained — so "not found" is weaker than "not known". Derivation `notes/f20_fibration_geometry.md` §8, §10 | hora-algebra | 2026-07-25 |
| PST-WREATH-78-01 | Pin–Straubing–Thérien 1992, Theorem 7.8: every language belonging to the pseudovariety generated by **wreath products of abelian groups by aperiodic monoids** has generalized star height at most one. Read through the transducer picture of `F20-TRANSD-RED-01`, this is the statement "`σ⁻¹` preserves gsh ≤ 1 when the transducer's state monoid is **aperiodic**", i.e. the aperiodic rung of `TRANSD-LADDER-01`. | CITED | Quoted verbatim from Bourne–Ruškuc, *On the star-height of subword counting languages and their relationship to Rees zero-matrix semigroups*, arXiv:1603.06236, which writes: "Since all languages that belong to the pseudovariety generated by wreath products of abelian groups by aperiodic monoids have star-height at most one [9, Theorem 7.8]", with [9] = Pin–Straubing–Thérien, *Inform. and Comput.* 101(2):219–250, 1992. Retrieved 2026-07-25 from the ar5iv HTML rendering. **This supersedes the wording of `PST-WREATH-06-01`**, which came from the paper's abstract and got the shape "`M ∘ (G ∘ N)`" without identifying which factor must be aperiodic; the two are consistent, and `PST-WREATH-COMM-01` (every group in that variety is commutative) is explained by exactly this: the base must be aperiodic, so no non-aperiodic monodromy is covered. CAUTION: the PST 1992 **primary full text is still unobtained** — HAL returns an Anubis anti-bot page, ScienceDirect 403, `irif.fr` 403, and `web.archive.org` is not reachable from this environment; the theorem number and wording rest on the secondary source | hora-algebra | 2026-07-25 |
| EIL-WPP-01 | Eilenberg, Proposition IX.1.1 (with the modification for **generalized** sequential functions): if `K` is obtained as the preimage of a language under a generalized sequential function, the transition monoid of `K` divides a wreath product of the recognizing monoid by the transducer's state monoid. This is the standard machinery that the construction of `F20-TRANSD-RED-01` instantiates. | CITED | Quoted as used in Bourne–Ruškuc arXiv:1603.06236: "by Eilenberg [3, Proposition IX.1.1] (suitably modified to deal with generalised sequential functions), the transition monoid of K divides a wreath product of an abelian group by an aperiodic monoid"; [3] = S. Eilenberg, *Automata, Languages and Machines*, vol. B. Retrieved 2026-07-25. Recorded so that **no novelty is claimed** for the transducer decomposition in `F20-TRANSD-RED-01`: that construction is the wreath-product principle applied to a specific `σ`, and the repo's derivation is elementary only so that nothing depends on an unobtained citation. CAUTION: Eilenberg vol. B not consulted directly | hora-algebra | 2026-07-25 |
| TRANSD-LADDER-01 | The hypothesis on the transducer's state monoid carries the entire content of `TRANSD-ABEL-01`, and the rungs are these. (a) **No hypothesis**: for every regular `L` over `Σ`, marking each letter with the DFA state in front of it makes `L = σ⁻¹(K)` with `K = Γ*·S` **star-free**; so "`σ⁻¹` preserves gsh ≤ 1 for every finite state monoid" is **equivalent to the whole generalized star-height conjecture**, and "`σ⁻¹` preserves gsh 0" is outright **false**. (b) **Aperiodic** state monoid: PROVED, `PST-WREATH-78-01`. (c) **Elementary abelian 2**: PROVED, `PST-GRP-03` (reading `UNREVIEWED`). (d) **Arbitrary abelian** (= `TRANSD-ABEL-01`): would settle gsh ≤ 1 for **every finite solvable group**, since iterating the Krasner–Kaloujnine embedding `G ↪ G' ≀ (G/G')` along the derived series presents `G` by transducers whose state monoids are the abelian quotients `G^(i)/G^(i+1)`. (e) The **first open rung** is a cyclic state monoid of order 4, which is exactly `N-F20-001`. | COMPUTED | `scripts/f20_fibration_geometry.py` §9–§10, exact. (a) checked on all 8,191 words of length ≤ 12 over two generators and all 168,421 of length ≤ 4 over the full alphabet, against the direct group evaluator; the refutation of gsh-0 preservation uses that `mu⁻¹(e)` has the group `F_20` as syntactic monoid, hence is not star-free (Schützenberger 1965). (d) the Krasner–Kaloujnine step `g ↦ (q ↦ t_q g t_{q·π(g)}⁻¹, π(g))` is verified an injective homomorphism at **every** stage of the derived series for all six groups outside the PST class (`SMALL-NONAB-31-01`): `A_4` 12→4→1, `F_20` 20→5→1, `C_7⋊C_3` 21→7→1, `SL(2,3)` 24→8→2→1, `S_4` 24→12→4→1, `C_2×A_4` 24→4→1, with every quotient checked abelian. CAUTION: quotient orders are reported as orders, not names — an abelian group of order 4 may be `C_4` or `C_2×C_2` and the computation does not decide which. The consequence in (d) is a **warning as much as a motivation**: a conjecture that settles all solvable groups is not a small step, which is the argument for attacking rung (e) rather than (d) | hora-algebra | 2026-07-25 |

| SFA-EGGAN-01 | Let an SF-automaton be a finite automaton whose transitions are labelled by star-free languages, and let its loop complexity be the cycle rank of the underlying digraph (Lombardy–Sakarovitch Def. 7.4). Then for all vertices p,q the path language from p to q is denoted by a generalized regular expression of syntactic star height at most the loop complexity; hence gsh(L) ≤ r_SF(L) := min over SF-automata accepting L of the loop complexity. In particular loop complexity ≤ 1 implies gsh ≤ 1, and r_SF(L) = 0 iff L is star-free. | PROVED | full induction in `notes/sf_labeled_automata.md` §2 (three cases: no edges; several strongly connected components, where the acyclic condensation adds no star; strongly connected, where the rank-witness vertex contributes the single new star); implemented in `tools/sf_automaton.py` (`all_pairs`, `SFAutomaton.to_expression`), which re-checks the height-vs-rank inequality on every call and raises otherwise, so the theorem is a runtime-enforced invariant; exercised by `tests/test_sf_automaton.py` and by every step of `scripts/sf_automaton_calibration.py`; NOT a lower bound — the converse half of Eggan's theorem does not relativize because complementation has no loop-complexity-controlled automaton construction (`notes/sf_labeled_automata.md` §2.4); PRIORITY (prior-art search done 2026-07-25, `M-SFA-PRIOR-001`): the statement is NOT new. The label-agnostic index bookkeeping is Sakarovitch, "Automata and rational expressions" (arXiv:1502.03573, long version of AutoMathA Handbook Ch. 2) §3.6, which defines the index of a *generalised automaton* — transitions "labelled not by letters only but by rational expressions in general" — as eqs (3.24)–(3.26) from `i(e) = h[label]`, with Property 3.12 (`lc = min over orders of I_ω`) and Proposition 3.13 (`I_ω(A) = h[B_ω(A)]`, credited to Lombardy–Sakarovitch, "On the star height of rational languages", 2003), yielding Theorem 3.11 (Eggan 1963). That source's `h` is the RESTRICTED star height and complement is outside its framework, and it does give an EQUALITY over elimination orders (`min_ω h[B_ω(𝒜)] = lc(𝒜)`). The generalized reading is published too: Pin–Straubing–Thérien, "Some results on the generalized star-height problem", Inf. Comput. 101(2):219–250, 1992, Theorem 4.6 (p.229f) — for a star-free INJECTIVE substitution σ, `h(Lσ) ≤ h(L)` with `h` = generalized star height, by the same induction extending σ to expressions; their injectivity and `(Aσ)*` star-free hypotheses (their Prop. 4.4) are needed only because their expressions may contain complement and intersection, and drop out here where only `∪,·,*` are substituted into. Moreover `r_SF` is not a new quantity: it is Hashiguchi's RELATIVE star height (Inf. Comput. 78(2):124–169, 1988, Def. 2.6, pp.127–128) with the base ranging over star-free languages — `r_SF(L) = min over finite 𝒞 ⊆ SF of h_r(L,𝒞)`, since an SF-automaton has finitely many labels. NET: this row has NO new mathematical content; the repository claims the packaging (rank as a finite certificate object), the implementation, and the measurements only, and the row stays PROVED because the proof is checked here, not as a priority claim | hora-algebra | 2026-07-25 |
| SFA-STAR-ONLY-01 | GH₁, the class of languages of generalized star height at most one, is the smallest class containing the star-free languages and the stars of star-free languages that is closed under union, intersection, complement and concatenation. Consequently GLOBAL-ONE holds if and only if GH₁ is closed under Kleene star. | PROVED | `notes/sf_labeled_automata.md` §3 (Propositions 3.1 and 3.2); both directions elementary — star height propagates as a maximum through the Boolean operations and concatenation and increases only at star, and the converse is Kleene's theorem; closure under quotients is `FULL-ALPH-RED-01` §3.5, not reproved here. The content is scope isolation, not difficulty: it shows the star is the unique missing closure property | hora-algebra | 2026-07-25 |
| SFA-CORE2-RANK-01 | Modulo CORE2-EQV-EXT-01, GLOBAL-ONE is equivalent to the statement that every SF-automaton of loop complexity 2 has generalized star height at most one — indeed to the single two-state shape with self-loops A and D and crossing edges B and C, whose path language from the initial vertex to itself is (A ∪ B D\* C)\* and whose cycle rank is exactly 2. Hence for every r ≥ 2 the statement "every rank-r SF-automaton has gsh ≤ 1" is equivalent to GLOBAL-ONE, and the rank hierarchy collapses to a single rank-2-to-rank-1 reduction question. | UNREVIEWED | `notes/sf_labeled_automata.md` §4; the automaton reading of the right-hand side and the cycle-rank computation are immediate and machine-checkable, but the equivalence itself is inherited from the external `CORE2-EQV-EXT-01`, which has not been independently reconstructed in this repository (`M-EXP-PR2-001`). Do not treat as a settled reformulation until that row is upgraded | hora-algebra | 2026-07-25 |
| SFA-L2-MEASURE-01 | The minimal DFA of the full Weis L2, read as a letter-labelled SF-automaton, has loop complexity exactly 2, so r_SF(L2) ≤ 2. After absorbing its two a-self-loops into the incoming edges via the star-free a\*, the four-diagonal walk graph of WEIS-L2-GSH-01 has loop complexity exactly 1, and all four atom families that that proof intersects — the eight anchor-walk languages W_d(x) for d in {D₂, D₃} and both letter parities — are languages of rank-1 SF-automata. Hence L2 lies in the Boolean closure of rank-1 SF-automaton languages. | COMPUTED | `scripts/sf_automaton_calibration.py` (exact, no sampling, no length cutoffs, ~0.04 s): ground truth recompiled from the PRINTED regex and asserted equal to the six-state walk automaton; cycle ranks by exhaustive vertex-deletion recursion; the eliminated first-return and escape languages asserted star-free and proved language-equal by DFA equivalence to the expressions printed in `notes/weis_l2_full_height_one.md` §3 for both anchors, by an independently written eliminator; each atom proved language-equal to its exact walk DFA; two certificates emitted and re-verified by the independent `tools/regex_cert.py` (`CERT-01`) on every `./scripts/check.sh`; run manifest `data/experiments/sf_automaton_calibration.md`. CAUTION: no rank-1 SF-automaton for L2 itself is known, but labels range over all star-free languages, so that is a search result and NEVER a lower bound; r_SF(L2) = 2 is not claimed | hora-algebra | 2026-07-25 |

## Update template

```text
| ID | Fully quantified claim. | STATUS | exact source, theorem, code, or counterexample | owner | YYYY-MM-DD |
```

For `COMPUTED` claims also record: commit hash, command, input hash, output hash, and resource bound in `data/experiments/`.
