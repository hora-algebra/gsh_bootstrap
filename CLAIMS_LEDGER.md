# Claims Ledger

Every nontrivial statement used by the project belongs here. The status labels are:

- `PROVED`: proved in this repository, with a file/theorem location.
- `CITED`: taken from a named source, with theorem/page information where available.
- `COMPUTED`: established by a deterministic program that **decides the whole claim**: the
  statement is reduced to a finite object and that object is traversed exhaustively (product-
  automaton reachability, complete enumeration, full transition-monoid search). Logically
  complete; the only residual risk is a bug in the program.
- `EMPIRICAL`: checked by a deterministic program on a **finite sample** of the claim —
  bounded word length, random words, spot checks. **This is not evidence for the claim.**
  It can refute (a mismatch is a counterexample) but it can never establish, because the
  untested part of the domain is infinite. An `EMPIRICAL` row is a lead, not a result.
- `CONJECTURAL`: a precise mathematical conjecture.
- `SPECULATIVE`: a research direction not yet precise enough to be a conjecture.
- `REFUTED`: false; counterexample recorded.
- `UNREVIEWED`: imported or generated statement awaiting audit.

Do not upgrade a status by editing prose. Add the verification artifact.

**Status is the ceiling, and it propagates.** A row is at most as strong as its weakest
input. If any row it depends on is `EMPIRICAL`, this row is `EMPIRICAL` too, and its
evidence field must name the specific incomplete step — not merely cite the weaker row.
`scripts/ci/lint_claims.py` enforces the `COMPUTED`/`EMPIRICAL` split mechanically.

**Vocabulary is bound to status.** Never write "settled", "resolved", "closed", "決着",
"解決" — in this file, in `RESULTS.md`, in `README.md`, or in a talk — for a claim whose
status is `EMPIRICAL`. The verb must not outrun the label. (This is the failure mode that
`A4-FULL-01` produced between 2026-07-22 and 2026-07-25: the label was honestly recorded as
`COMPUTED` under the old definition, which did not distinguish sampling from exhaustion, and
the prose in `RESULTS.md` §5.5 then called it "解決した" with no qualifier.)

| ID | Claim | Status | Evidence / location | Owner | Last review |
|---|---|---|---|---|---|
| GSH-STATUS-01 | No regular language of generalized star-height greater than one is currently known in the surveyed literature. | CITED | Place–Zeitoun 2017, introduction; Bourne 2017, introduction; `docs/SURVEY.md` §1 | survey lead | 2026-07-22 |
| GSH-BASE-01 | Generalized star-height zero equals the class of star-free languages. | CITED | definition plus Schützenberger 1965 interface; `docs/SURVEY.md` §2 | language lead | 2026-07-22 |
| GSH-BASE-02 | A regular language is star-free iff its syntactic monoid is aperiodic. | CITED | Schützenberger 1965 | language lead | 2026-07-22 |
| PST-CL-01 | Bounded generalized star-height is closed under left/right quotients, inverse alphabetic morphisms, and injective star-free substitutions under the hypotheses in Pin–Straubing–Thérien. | CITED | Pin–Straubing–Thérien 1992; exact hypotheses must be checked before formal use | source auditor | 2026-07-22 |
| PST-GRP-01 | Every language recognized by a finite commutative group has generalized star-height at most one. | CITED | Pin–Straubing–Thérien 1992 | group lead | 2026-07-22 |
| PST-GRP-02 | The height-one result extends to finite nilpotent groups of class at most two. | CITED | Pin–Straubing–Thérien 1992, Theorem 7.3 as indexed in the open version | group lead | 2026-07-22 |
| PST-GRP-03 | The published semidirect-product result covers divisors of a semidirect product of a commutative group by an elementary abelian 2-group. | CITED | Pin–Straubing–Thérien 1992 | group lead | 2026-07-22 |
| BR-REES-01 | Languages recognized by finite Rees (zero-)matrix semigroups over abelian groups have generalized star-height at most one. | CITED | Bourne–Ruškuc 2016 | language lead | 2026-07-22 |
| SMALL-12-01 | In Bourne's finite-group ladder, all groups of order below 12 are covered; `A_4` and `Dic_3` are the unresolved order-12 cases in that program. | CITED | Bourne 2017, group-order discussion | group lead | 2026-07-22 |
| SMALL-C3-FAIL | The direct extension of the cited `A ⋊ C_2` argument to `A ⋊ C_3` fails at a unique-factorization step. | CITED | Bourne 2017, failure analysis | source auditor | 2026-07-22 |
| A5-SCOPE-01 | A theorem for languages recognized by `A_5` would not by itself settle the global generalized star-height problem. | PROVED | immediate from scope of the quantified theorem; `README.md` | referee | 2026-07-22 |
| A5-MATHLIB-01 | Mathlib provides `alternatingGroup (Fin 5)` and a simplicity theorem for degree five. | CITED | mathlib module `Mathlib.GroupTheory.SpecificGroups.Alternating` | Lean lead | 2026-07-22 |
| COH-01 | Group or monoid cohomology supplies an established invariant of generalized star-height. | REFUTED | no such theorem found in surveyed sources; never assume this statement | referee | 2026-07-22 |
| COH-02 | A concrete word-to-cochain map may organize extension/gluing arguments. | SPECULATIVE | `docs/SURVEY.md` §7; requires coefficient object and closure theorem | cohomology lead | 2026-07-22 |
| CERT-01 | The Python checker accepts only when the supplied generalized expression has the claimed height bound and its compiled DFA is equivalent to the target DFA. | PROVED | `tools/regex_cert.py`, `tests/test_regex_cert.py`; proof is program audit, not Lean | computational lead | pending first CI |
| LEAN-LANG-01 | Language concatenation and star definitions match the mathematical definitions in the blueprint. | UNREVIEWED | Lean source (moved unchanged 2026-07-23 from `GSH/Language/Basic.lean` to `GSH/Challenges/GeneralizedStarHeight.lean` §1); compiled in first local build 2026-07-22 (`PROOF_OBLIGATIONS.md` repair log); semantic review pending | Lean lead | 2026-07-23 |
| LEAN-DFA-01 | `DFA.run_append` is formalized. | UNREVIEWED | `GSH/Recognition.lean` §1 (moved unchanged 2026-07-23 from `GSH/Automata/DFA.lean`); compiled in first local build 2026-07-22; semantic review pending | Lean lead | 2026-07-23 |
| LEAN-A5-01 | The repository's `A5` abbreviation reduces to `alternatingGroup (Fin 5)`. | UNREVIEWED | `GSH/Groups.lean` (moved unchanged 2026-07-23 from `GSH/Groups/A5.lean`); compiled in first local build 2026-07-22 (simplicity hypothesis discharged via `Nat.card_fin`); semantic review pending | Lean lead | 2026-07-23 |
| LEAN-CNT-01 | For every letter `a`, every modulus `m > 0` and every residue `r < m`, the language `{w \| count a w % m = r}` has generalized star height at most one. | PROVED | `GSH/Height/Counting.lean`, `GSH.Counting.hasHeightAtMost_count`; `sorry`-free; the height-zero letter-avoiding factor is built by complement, not by a star; axiom audit `GSHTest/Axioms.lean`. Independently replayed outside Lean: `data/certificates/height1_count_mod3_res1.json` is a literal transcription of the `cnt a 3 1` expression, and `tools/regex_cert.py` confirms exact DFA equivalence (minimal states 3 = 3) at syntactic height 1; negative controls recorded 2026-07-25 — a wrong residue gives `FAIL … witness=['a']` and a height-0 claim gives `ERROR` | Lean lead | 2026-07-25 |
| LEAN-TRANSFER-01 | `HeightOneForGroup` descends along injective and along surjective group morphisms, hence to divisors. | PROVED | `GSH/Transfer.lean`, `GSH.HeightOneForGroup.of_injective` / `.of_surjective` / `.of_mulEquiv` / `.subgroup`; `sorry`-free; axiom audit `GSHTest/Axioms.lean` | Lean lead | 2026-07-25 |
| LEAN-ABELIAN-01 | Every language recognized by a finite commutative group has generalized star height at most one, over every finite alphabet, for every morphism and every accepting set. | PROVED | `GSH/Groups/Abelian.lean`, `GSH.heightOne_of_commGroup` (and `GSH.heightOne_of_mul_comm`); `sorry`-free; uses no structure theorem and no choice of generators; axiom audit `GSHTest/Axioms.lean` | Lean lead | 2026-07-25 |
| LEAN-ORD5-01 | Every language recognized by a group of order at most five has generalized star height at most one. | PROVED | `GSH/Groups/SmallOrder.lean`, `GSH.heightOneUpTo_five : HeightOneUpTo 5`; `sorry`-free; via `GSH.mul_comm_of_card_le_five` (orders 1,2,3,5 prime/trivial, order 4 by the `p²` theorem) and LEAN-ABELIAN-01. CAUTION: the bound is exactly five — `S_3` at order six is not commutative, and the non-commutative machinery does not exist yet | Lean lead | 2026-07-25 |
| LEAN-AXIOM-01 | The four rows above depend on no axioms beyond `propext`, `Classical.choice`, `Quot.sound`; in particular none of them depends on `sorryAx` or on `Lean.ofReduceBool`. | PROVED | `GSHTest/Axioms.lean`, `#guard_msgs in #print axioms …`, run by `scripts/check.sh`; includes a negative control asserting that `generalized_star_height_conjecture` *does* depend on `sorryAx` | Lean lead | 2026-07-25 |
| GLOBAL-ONE | Every regular language has generalized star-height at most one. | CONJECTURAL | workshop north-star target; Lean registration as explicitly open challenge: `GSH/Challenges/GeneralizedStarHeight.lean` (`generalized_star_height_conjecture : GeneralizedHeightOneConjecture`, obligation L-GSH-CHALLENGE-001); since 2026-07-23 that file contains the whole definition chain in one place (imports only mathlib) | all | 2026-07-23 |
| A4-STD-01 | With the morphism φ(a) = (123), φ(b) = (12)(34) generating `A_4`, the language { w ∈ {a,b}* : φ(w) = e } has generalized star-height at most one. | COMPUTED | `RESULTS.md` §5–§6; atoms proved by exhaustive product-automaton search (`scripts/research/a4_attempt.py`), end-to-end agreement on all words of length ≤ 16 plus 30k random words (`scripts/research/a4_final.py`); commit dd316b4; run manifest in `data/experiments/` still pending **2026-07-25 completeness audit:** the evidence above is a sample and would not support this row on its own. The row survives at `COMPUTED` because `A4-STD-02` independently supplies a **complete finite proof** of exactly this claim — the height-1 expression is compiled to a DFA and proved language-equal to the 12-state `A_4` word-problem automaton by product reachability, with no sampling. Read `A4-STD-02` as the operative evidence for this row.  This row is `COMPUTED`. | Kazumi Kasaura | 2026-07-25 |
| A4-FULL-01 | For the alphabet consisting of all twelve elements of `A_4` under the evaluation morphism, the word-problem language { w : eval(w) = e } has generalized star-height at most one. | EMPIRICAL | **Partially repaired 2026-07-25.** Step [4] — "the group element is `(Σ_{(g,p)} τ^p(V(g))) · t^P`" — is now **decided**: the right-hand side is computed by a 12-state automaton on `(phase, running V4 sum)` because `V4` is elementary abelian, so the BFS over its product with the Cayley automaton settles the identity at all 12 reachable states, for every word (`scripts/ci/completeness_upgrade.py`; negative control: shifting the `τ` exponent by 1 is rejected). **Step [3] is still a sample and this row therefore stays `EMPIRICAL`**: the reconstruction of `N[g,p]` from the certified features is exhaustive only to length 4 plus 4,000 random words. Closing it needs the six per-mover GF(2) identities to be decided (each is a bounded-bit product BFS, ~40k states) together with the reversal bookkeeping, for which the backward features must be carried as transition profiles rather than states. That is the remaining work; everything else in `A4-FULL-01` is now complete. Prior record: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** The incomplete steps are named exactly: the reconstruction of all `N[g,p]` from the certified features is exhaustive only to **length 4** (22,621 words) plus 4,000 random words, and the composition `(P, N[g,p] mod 2) -> group element` is **20,000 random words only**. Neither step is reduced to a finite object and traversed. Step [1] (93 token languages certified aperiodic) IS complete and survives. Note the 3-letter `{u,d,k}` version *does* have the exhaustive 384-state product-automaton proof (`scripts/research/a4_full3.py`); the 12-letter version simply never received one. Upgrade path: a product BFS proving the target is constant on each certified-feature cell, in the style of `prove_function` in `scripts/research/weis_l2_family.py`. Prior evidence: `RESULTS.md` §5.5; 93 token languages certified aperiodic, reachability = f(P, N[g,p]) proved by exhaustive 384-state product-automaton search (`scripts/research/a4_full3.py`), reconstruction checks exhaustive to bounded length plus random sampling (`scripts/research/a4_full12.py`); commit f4258df; run manifest in `data/experiments/` still pending  This row is `EMPIRICAL`. | Kazumi Kasaura | 2026-07-25 |
| A4-ANYGEN-01 | For every monoid morphism φ from a free monoid over a finite alphabet to `A_4`, the language φ⁻¹(e) has generalized star-height at most one. | UNREVIEWED | `RESULTS.md` §6 note; follows from A4-FULL-01 via inverse-alphabetic-morphism closure (PST-CL-01) once the exact hypotheses of that closure are checked **2026-07-25 audit:** the input `A4-FULL-01` is `EMPIRICAL`, so this row is at most that strong regardless of the closure question; it stays `UNREVIEWED` for the separate reason that the closure hypotheses are unchecked.  This row is `UNREVIEWED`. | Kazumi Kasaura | 2026-07-25 |
| FULL-ALPH-RED-01 | For every finite group `G` and `n ≥ 0`, the identity fibre over the full-element alphabet has generalized star-height at most `n` iff every language recognized by `G` has generalized star-height at most `n`. | PROVED | `notes/conway_group_identities_and_full_alphabet.md` §3 with self-contained closure lemmas §3.5 (Brzozowski derivative for single-letter quotients; literal-inverse-morphism substitution) — no longer depends on PST-CL-01; one adversarial review pass 2026-07-23 (AI, no gaps found incl. edge cases); machine companion `scripts/research/closure_lemmas_check.py` (400 expressions, exact DFA equivalence, all PASS); independent human review still pending | source auditor | 2026-07-23 |
| A4-ALLLANG-01 | Every language recognized by `A_4` has generalized star-height at most one. | EMPIRICAL | **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** **Inherited**: the base case `A4-FULL-01` is `EMPIRICAL`, so this row cannot be stronger. The composition with `FULL-ALPH-RED-01` (`PROVED`) is sound; the input is not. Prior evidence: A4-FULL-01 (`EMPIRICAL` since the 2026-07-25 audit) composed with FULL-ALPH-RED-01 (now `PROVED`, self-contained); the ceiling was written as `COMPUTED` at the time and is `EMPIRICAL` now, because the weak link is the bounded-exhaustive-plus-random reconstruction check in A4-FULL-01 — this row must never be upgraded to `PROVED` without upgrading A4-FULL-01 first  This row is `EMPIRICAL`. | Kazumi Kasaura | 2026-07-25 |
| CONWAY-GRP-01 | Conway's finite-group identity is built from the full-element Cayley matrix `M_G = (x_{p⁻¹q})`; under the singleton-letter language valuation, the `(p,q)` entry of `M_G*` is the fibre `μ_G⁻¹(p⁻¹q)`. | CITED | Conway 1971; Krob 1991; Ésik 1999; derivation and comparison in `notes/conway_group_identities_and_full_alphabet.md` §§1–2 | source auditor | 2026-07-23 |
| GSH-INPUTEXT-01 | If a new input letter is added to a finite group automaton and acts as a transformation already induced by a word, generalized star-height at most one of the identity fibre is preserved. | CONJECTURAL | precise formulation in `notes/conway_group_identities_and_full_alphabet.md` §6; analogous unbounded equational input-extension theorem is due to Ésik, but no bounded-depth analogue was found | all | 2026-07-23 |
| LAAB-04-01 | The language L(aab,0,4) = { w ∈ {a,b}* : binom(w, aab) ≡ 0 mod 4 } has generalized star-height at most one. | COMPUTED | **Upgraded back to `COMPUTED` 2026-07-25** after the gap named below was closed by `scripts/ci/completeness_upgrade.py` (exact, with a negative control that must fire). The composite is now decided, not sampled: the identity `binom(w,aab) ≡ M1 + 2·M2 (mod 4)` — with `M1 = N_2^(4)+N_3^(4) mod 4` and `M2 = N_3^(8)+N_4^(8)+N_5^(8)+N_6^(8) mod 2` — **holds at all 64 reachable states, no sampling,** of the automaton on `(#a mod 8, binom(#a,2) mod 4, binom(w,aab) mod 4, M1, M2)`, which decides it for every word. Negative control: giving `M2` weight 1 is rejected at state `(3,3,3,1,1)`. The atoms were already exact: `scripts/research/a4_attempt.py` proves each `W(h,k,r,m)` a function of the height-1 features by product BFS (1,944–1,952 product states for stage 4, 896–920 for stage 8) and carries its own negative control (wrong opener `r=3` FAILS as expected). The former sampled evidence, superseded: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** The atoms are proved, but the **composite** is checked only on all words of length <= 16 plus 30,000 random words (`scripts/research/a4_final.py`), never by DFA equivalence against the target. Prior evidence: `RESULTS.md` §3, §5; proved W atoms plus arithmetic reduction, composite checked on all words of length ≤ 13 plus 150k random words; commit dd316b4; run manifest in `data/experiments/` still pending  This row is `COMPUTED`. | Kazumi Kasaura | 2026-07-25 |
| A5-GEN145-01 | With the morphism φ(a) = (123), φ(b) = (145) generating `A_5`, the language φ⁻¹(e) ⊆ {a,b}* has generalized star-height exactly one. | COMPUTED | **Upgraded back to `COMPUTED` 2026-07-25** after the gap named below was closed by `scripts/ci/completeness_upgrade.py` (exact, with a negative control that must fire). The height-1 expression `K1 ∩ K2 ∩ K3` is compiled and proved **language-equal to the 60-state Cayley automaton of `A_5` by complete product reachability, no sampling**, at syntactic height 1; `a*`/`b*` are written as complements so they cost height zero. Negative control: deleting the `b a* b a* b` half of the first-return code `C` is rejected with witness `bbb`. The former sampled evidence, superseded: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** The expression-vs-group agreement is checked only on all words of length <= 16 plus 200,000 random words (`scripts/research/a5_check.py`), never against the 60-state Cayley automaton by product reachability. The group-theoretic facts (order 60, trivial 3-point stabilizer) are exact and survive. Prior evidence: `RESULTS.md` §5.6, `notes/A5_generator_dependent_star_height_1.md`; point-stabilizer filtration K_1 ∩ K_2 ∩ K_3 with star-free first-return codes; `scripts/research/a5_check.py` (⟨(123),(145)⟩ = A_5, trivial 3-point stabilizer, agreement on all words of length ≤ 16 plus 200k random words); height ≥ 1 because the syntactic monoid is `A_5` (non-aperiodic); commit 4ee1018; run manifest in `data/experiments/` still pending  This row is `COMPUTED`. | Kazumi Kasaura | 2026-07-25 |
| WEIS-L2-M2-01 | Every Boolean combination of stage-2 staged ba*b pair counts N_{p,q} mod 2 or mod 4 (phases = cumulative a-count mod 2, over {a,b}) and odd-run segment counts mod 4 has generalized star-height at most one. | COMPUTED | **Upgraded back to `COMPUTED` 2026-07-25** after the gap named below was closed by `scripts/ci/completeness_upgrade.py` (exact, with a negative control that must fire). The inherited gap is closed: `THOMAS-D2-02`, whose integer identity this row consumes, is now `COMPUTED` by complete product reachability with no sampling. This row's own cell-constancy results were already exact product BFS. The former note, superseded: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** **Inherited**: the cell-constancy proofs in this row are exact product BFS and are complete, but they consume the integer identity of `THOMAS-D2-02`, which is only checked to length 16. Upgrading `THOMAS-D2-02` restores this row. Prior evidence: RESULTS.md §5.9; notes/weis_l2_stage2_height1.md; exact product-automaton proofs, cell-table random checks (30k words, len ≤ 300) and independent `re` evaluation of an explicit height-1 expression in `scripts/research/weis_l2_family.py`; run manifest `data/experiments/weis_l2_stage2.md`  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| THOMAS-D2-02 | The segment-counting languages { w : #(b-terminated a-runs of odd length) ≡ h mod 4 } have generalized star-height at most one, by flat token counting over the finite prefix code {b, aa, ab} with the exact identity #ab = \|w\|_a + 2\|w\|_b − 2·#tok − [tail=a]; this upgrades the d=2 Thomas 1981 citation to a self-contained machine proof. | COMPUTED | **Upgraded back to `COMPUTED` 2026-07-25** after the gap named below was closed by `scripts/ci/completeness_upgrade.py` (exact, with a negative control that must fire). The identity is now decided rather than checked to length 16: the automaton on `(parse state of the prefix code {b, aa, ab}, signed defect D)` reaches exactly 2 states and the identity holds at both — an exhaustive traversal, no sampling — which covers every word. Negative control: dropping the `[tail=a]` term is rejected at the reachable state `(1, -1)`. The former sampled evidence, superseded: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** The integer identity is checked **exhaustively only to length 16** (`scripts/research/weis_l2_family.py` step [2]); it is an induction on words that was never carried out, nor replaced by an automaton argument. Prior evidence: `scripts/research/weis_l2_family.py` steps [2][6] (identity exhaustive to length 16; explicit expression verified with the independent `re` engine); notes/weis_l2_stage2_height1.md §3  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| WEIS-L2-M3-01 | For stage-3 (phases mod 3) staged ba*b pair counting, T1 mod 2 (runs ≡ 1 mod 3) is a proved function of certified height-1 features, and the remaining stage-3 atoms are exactly NOT functions of the current certified feature family (LC, flags, finite-code flat counts incl. cascade code Z3, W atoms mod 6). | COMPUTED | `python3 scripts/research/weis_l2_family.py --m3` (exact product-automaton verdicts); obstruction analysis in notes/weis_l2_stage2_height1.md §5; PROOF_OBLIGATIONS.md N-L2-M3-001 | hora-algebra | 2026-07-22 |
| WEIS-L2-AUDIT-01 | The language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) over {a,b} — proposed by Pin–Straubing–Thérien 1992 and studied in Weis 2011 §4.4 — has minimal DFA with 6 states and syntactic monoid a non-nilpotent group of order 48, isomorphic to C₂×S₄. | CITED | Weis 2011 (UMass thesis, DOI 10.7275/2177022) p.115, full PDF retrieved 2026-07-23 via Wayback Machine; "proposed in [30]" fixes the attribution to PST 1992; order 48 independently recomputed from the printed regex (`scripts/research/weis_l2_actual.py`); the earlier "48 not reproduced" note reflected a family reconstructed from secondary descriptions, not the printed L2 — see `notes/weis_2011_primary_audit.md`; thesis-internal inconsistency at p.118–119 recorded there and not relied upon | hora-algebra | 2026-07-23 |
| WEIS-L2-OPEN-01 | Weis 2011 leaves the generalized star-height of the full language L2 open ("we do not know whether it has generalized star-height 2", p.115); only the restricted subset with ≥ 4 a's between b's is shown to have height 1. Full L3 is likewise left open. | CITED | Weis 2011 §4.4 pp.114–118 and abstract pp.vi–vii, retrieved 2026-07-23 (Wayback URLs in `notes/weis_2011_primary_audit.md`); this makes full L2 a literature-backed candidate proposed by PST 1992 and left open by Weis. This row is a statement about the thesis and stays valid; the candidate itself was resolved in this repository on 2026-07-25 (WEIS-L2-GSH-01: gsh(L2) = 1). Full L3 remains open both in the thesis and here | hora-algebra | 2026-07-23 |
| WEIS-TALK-01 | The 2009-11-03 UMass CMPSCI Theory Seminar abstract for P. Weis's talk "The Power of One Star" states that the talk presents four candidate languages and shows all four have generalized star height one. | CITED | abstract retrieved verbatim 2026-07-23 (direct fetch of people.cs.umass.edu/~barrington/thysemS12/3nov09.html); CAUTION: inconsistent with the 2011 thesis, which leaves full L2 and L3 open (WEIS-L2-OPEN-01) — do not read this row as "Weis proved full L2 has height 1"; the talk's four languages were plausibly restricted variants (inference, slides not retrieved) — `notes/weis_2011_primary_audit.md` §2.4 | hora-algebra | 2026-07-23 |
| SEARCH-CAL-01 | The size-ordered synthesis search recovers a verified height-1 certificate for the language (aa)* over {a,b} at binary-AST size 4. | COMPUTED | `tools/height_search.py`; certificate `data/certificates/height1_aa_star_synth.json`; rerun `python3 -m tools.height_search --target aa_star --max-size 6` | computational lead | 2026-07-22 |
| SEARCH-CAL-02 | No generalized regular expression over {a,b} with syntactic star height ≤ 1 and at most 13 binary-AST nodes defines { w : number of a in w is even }; the minimal such size lies in [14, 15]. This is a size bound, not a star-height lower bound. | COMPUTED | complete enumeration (593,575 distinct languages, no pruning) plus a checked size-15 witness; exact command `python3 -m tools.height_search --target even_a --max-size 13 --max-states 0`; commit, code sha256, and normalized-output sha256 in the reproducibility manifest of `data/experiments/height_search_even_a.md` (independently rerun 2026-07-22) | computational lead | 2026-07-22 |

| WEIS-L2-NOTFN-01 | Membership in the full language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) is not a function of the certified stage-2 feature family {LC(8,4), Flags(2), CodeTok({b,aa,ab}) mod 4, W(2,0,4), W(2,1,4)}, nor of its tested extensions (certified atoms N_{p,q} mod 4 and T' mod 4 added as features; doubled moduli; the --m3 constructors X3, Z3, W(3,r,6), LC(72,8)). Hence WEIS-L2-M2-01 does not decide the generalized star-height of full L2. (Status update 2026-07-25: full L2 is no longer open — WEIS-L2-GSH-01 gives gsh(L2) = 1 by changing the group action instead of extending the feature family. The negative result in this row stands as stated: it is about the certified feature family, and it is what showed that a new mechanism was needed.) | COMPUTED | exact product-automaton sweep (114 reachable product states, no sampling), minimal witnesses `bababbb` ∉ L2 vs `bbababb` ∈ L2 (identical feature vectors and certified atom values), extension witnesses `baaababbb`/`bbabaaabb`; triple-checked (product automaton, feature-object rerun, independent `re` evaluation); DFA sanity: 6 states, syntactic monoid order 48 ≅ C₂×S₄, non-nilpotent, solvable; `scripts/research/weis_l2_actual.py` **2026-07-25 audit:** `WEIS-L2-M2-01` was briefly `EMPIRICAL` (via `THOMAS-D2-02`) and is now `COMPUTED` again; independently of that, this row never consumed it — the statement proved here is that L2-membership is not a function of the stage-2 feature family, which does not depend on whether those features have height one. The product sweep here is exact.  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| WEIS-L2-GSH-01 | The full language L2 = L((ab\*a ∪ ba\*b(ab\*a)\*ba\*b)\*) over {a,b} — proposed by Pin–Straubing–Thérien 1992 and left open in Weis 2011 (WEIS-L2-OPEN-01) — has generalized star-height exactly one. | COMPUTED | `RESULTS.md` §5.10; derivation in `notes/weis_l2_full_height_one.md`; complete finite proof in `scripts/research/weis_l2_full_gsh1.py`: the ground-truth 6-state DFA is derived from the PRINTED regex by exact compilation (no reliance on hand-entered automata), the syntactic group (order 48 ≅ C₂×S₄), its central antipodal involution, the induced action ψ on the four cube diagonals (ψ(a) a transposition, ψ(b) a 4-cycle, doubly-active diagonals = the anchor pair), star-freeness of both first-return languages, well-definedness of the letter parities and injectivity of (parities, ψ) on all 48 elements are each asserted; the assembled expression is checked to have star-height 1 syntactically, compiled exactly to a 6-state DFA, cross-validated against an independent recursive matcher (all words of length ≤ 7) and Python's `re` engine on the printed regex (all 8191 words of length ≤ 12), and proved language-equal to the ground truth by product reachability (no sampling). Lower bound: syntactic monoid is a nontrivial group, so L2 is not star-free (Schützenberger 1965). Independently re-verified by `tools/regex_cert.py` (CERT-01, a separate implementation) on the emitted certificate `data/certificates/height1_weis_l2_full.json`, which `scripts/check.sh` checks on every run (`PASS: equivalent; height=1 <= 1; minimal states expression=6, target=6`); run manifest `data/experiments/weis_l2_full_gsh1.md`. Remaining risk is program audit / independent human re-derivation, and a targeted L2-specific prior-art search before any external write-up | hora-algebra | 2026-07-25 |
| WEIS-L2-RSH-01 | The same L2 has restricted star-height exactly two; in particular gsh(L2) = 1 < rsh(L2) = 2, and the printed expression (restricted star-height 3) is not optimal for either measure. | COMPUTED | `RESULTS.md` §5.10 and `notes/weis_l2_full_height_one.md` §6; `scripts/research/weis_l2_restricted_height.py` exhausts all 2¹² labelled-edge subsets of the 6-state minimal DFA and finds exactly one accepting L2 (the full automaton) with loop complexity 2; depends on UNIV-SUBGRP-01 (`PROVED`) plus two CITED theorems verified verbatim in Lombardy–Sakarovitch, "The universal automaton" (2008): Thm. 7.5 (Eggan 1963, star height = minimal loop complexity) and Thm. 7.10 (Lombardy–Sakarovitch 2003, the universal automaton of a group language contains a subautomaton of minimal loop complexity); loop complexity computed exactly per Def. 7.4 This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| UNIV-SUBGRP-01 | Let φ : Σ\* → G be onto with G finite, H ≤ G a subgroup, L = φ⁻¹(H). Then the maximal factorizations of L with both components nonempty are exactly the pairs (Hg⁻¹, gH), they correspond bijectively to the left cosets gH, and the universal automaton restricted to them is deterministic and isomorphic to the coset automaton, i.e. to the minimal DFA of L. | PROVED | one-paragraph proof in `notes/weis_l2_full_height_one.md` §6 (factorizations are saturated by the syntactic congruence, so the condition AB ⊆ H over subsets of G forces A = Hb₀⁻¹ and B = b₀H; transitions computed from Lombardy–Sakarovitch Def. 2.4 give a unique target coset); consistency check with the literature: TUA's remark after Cor. 7.12 states exactly this for W_q = {w : \|w\|_a ≡ \|w\|_b mod 2^q}, the H = {1} case; used by WEIS-L2-RSH-01 | hora-algebra | 2026-07-25 |
| A4-STD-02 | The A4-STD-01 height-one representation (φ(a) = (123), φ(b) = (12)(34)) is confirmed by an independent complete finite proof, not only by bounded-length and random agreement. | COMPUTED | `scripts/research/a4_std_dfa_equivalence.py`, written from the `RESULTS.md` §5 prose alone: the arithmetic characterization is checked against the group on all 32767 words of length ≤ 14, the explicit height-1 expression is compiled exactly to a 12-state DFA, the compiler is cross-validated against an independent recursive matcher (length ≤ 9), and language equality with the 12-state A4 word-problem DFA is proved by product reachability (complete, no sampling). This strengthens the evidence of A4-STD-01 for the two-generator case; A4-FULL-01 (all twelve letters) is untouched and remains bounded-exhaustive-plus-random **2026-07-25 audit:** the `A4-FULL-01` mentioned above is `EMPIRICAL`; this row does not consume it (it is named only to record that the 12-letter case was left untouched), and the proof here is a complete product-reachability check, so this row stays `COMPUTED`.  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| WATOM-45-01 | The staged-count atoms `{ w ∈ {a,b}* : N_r(w) ≡ h mod 5 }`, where `N_r(w)` counts the occurrences of `a` whose number of preceding `b`'s is `≡ r mod 4`, have generalized star-height at most one for every `r ∈ Z/4` and `h ∈ Z/5`. This is the W-atom scheme of `RESULTS.md` §5 at the new parameters (phases mod 4, counts mod 5). | COMPUTED | **Upgraded back to `COMPUTED` 2026-07-25** after the gap named below was closed by `scripts/ci/completeness_upgrade.py` (exact, with a negative control that must fire). Each of the 20 atoms is proved equal to its 20-state staged-count DFA — state `(b-count mod 4, N_r mod 5)` — by **complete product reachability, no sampling**. Negative control: every atom is rejected when compared against the shifted residue `h+1`, so the checker is not vacuously accepting. The former sampled evidence, superseded: **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** Each of the 20 atoms is compared with the direct staged count only on all 8,191 words of **length <= 12** (`scripts/research/f20_word_problem.py` check [3b]); the atoms are never proved equal to their 20-state target DFAs by product reachability. Note `F20-STD-01`, which consumes these atoms, is unaffected: its assembled expression is checked directly against the Cayley automaton. Prior evidence: `scripts/research/f20_word_problem.py` check [3b]: with opener `(a*b)^r`, token set `X = a ∪ b a* b a* b a* b`, tails `(b a*)^s` and the identities `B = r + 4f + s`, `t = N_r + f`, each of the 20 atoms is compiled exactly and compared with the direct staged count on all 8191 words of length ≤ 12, and cross-checked with an independent recursive matcher on all 511 words of length ≤ 8; each atom is verified to have syntactic height 1; run manifest `data/experiments/f20_word_problem.md`  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| F20-STD-01 | With the morphism φ(a) = (x ↦ x+1), φ(b) = (x ↦ 2x) onto `F_20 = AGL(1,5) = C_5⋊C_4` (faithful action), the word problem φ⁻¹(e) ⊆ {a,b}* has generalized star-height exactly one. `F_20` is the smallest non-abelian group outside the PST class **after `A_4`** (`FRONTIER-ORD20-01`; `A_4` at order 12 is smaller and its evidence is `EMPIRICAL`), so this is a height-one result inside the frontier rather than the first one. | COMPUTED | `RESULTS.md` §5.11; `scripts/research/f20_word_problem.py`. Upper bound: the arithmetic characterization "the number of `b`'s is `≡ 0 mod 4` and `N_0 + 3N_1 + 4N_2 + 2N_3 ≡ 0 mod 5`" (coefficients `2^{-p} mod 5`) is verified against the group on all 32767 words of length ≤ 14; the height-1 expression built from `WATOM-45-01` compiles exactly to a 20-state DFA and is proved language-equal to the 20-state Cayley automaton by complete product reachability (no sampling); a second, transparent W-atom assembly independently compiles to the same language (check [5b]); independently re-verified by `tools/regex_cert.py` (CERT-01, separate implementation) on `data/certificates/height1_f20_word_problem.json`, which `scripts/check.sh` checks on every run. Lower bound: the minimal DFA is the Cayley automaton of `F_20`, so the syntactic monoid is a nontrivial group, not aperiodic, hence not star-free (Schützenberger 1965). CAUTION: this is one morphism and one accepting set; it does NOT prove `HeightOneForGroup F_20`, which by `FULL-ALPH-RED-01` needs the full 20-letter alphabet — `F_20` stays open in `FRONTIER-ORD20-01`. Run manifest `data/experiments/f20_word_problem.md` **2026-07-25 audit:** `WATOM-45-01` was briefly `EMPIRICAL` and is now `COMPUTED` again; either way this row never consumed it — the atoms are only the *source* of the assembled expression, and that expression is checked directly against the 20-state Cayley automaton by complete product reachability, so a wrong atom would be caught there.  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| F20-FULL-OBS-01 | For the full 20-element alphabet of `F_20`, the multi-mover mechanism of `A4-FULL-01` (`RESULTS.md` §5.5, pattern-conditioned cuts) fails on every one of its 291 candidate cut patterns, and the failure is caused exactly by the five letters with `ε = 2` (`α = 4`, the involution of `C_4`): the phase group `Z/4` is composite, so such a letter has phase orbit `{0,2}` and bounces between the odd phases `1 ↔ 3` without ever meeting the cut phase, giving a period-2 element of the transition monoid. The `A_4` mechanism silently required the phase group to have prime order. Separately, the GF(5) linear algebra is *not* the bottleneck: formally assuming the uncertified staged features, `β` would be determined. | COMPUTED | `scripts/research/f20_full_alphabet.py` (exact, full transition-monoid enumeration, 0.1 s): the 291-pattern table is 0 certified; the localization experiment certifies the sub-alphabets `ε ∈ {0,1}`, `{0,2}`, `{0,3}` and the 15-letter `ε ∈ {0,1,3}`, and fails on `{0,1,2}`, `{0,2,3}`, `{0,1,2,3}` — which also serves as a positive control proving the judge is not trivially answering "not aperiodic"; GF(5) ranks 8/8, 7/8 (β-neutral null direction), 8/8 formally versus 20/80 soundly. Derivation `notes/f20_full_alphabet_obstruction.md`; run manifest `data/experiments/f20_full_alphabet.md`. CAUTION: this is NOT a lower bound (research rule 1) — one known mechanism does not apply, nothing more; `F20-STD-01` is unaffected; the script's length-4 "minimal witness" pair has the same letter multiset and therefore only shows that count-level features are insufficient, which was expected and must not be over-reported **2026-07-25 audit:** the `A4-FULL-01` referenced here is `EMPIRICAL`, but only its *mechanism* (the pattern-conditioned cut construction) is used, not its claim; the failure of that mechanism on `F_20` is established by exact transition-monoid enumeration here. This row stays `COMPUTED`.  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| F20-SUB10-OBS-01 | Let `Σ₁₀` be the 10-letter sub-alphabet `{ g ∈ F_20 : ε_g ∈ {0,1} }`, which generates `F_20`. The certified feature family of the `A4-FULL-01` mechanism (`RESULTS.md` §5.5) over `Σ₁₀` — total phase, arrivals per phase, per-nonmover arrivals per phase, certified pattern-conditioned cuts, letter counts mod 5, first and last letter, all closed under reversal — does **not** separate the group element. On `Σ₁₀` no pair pattern is certifiable at all (every one has a period-2 element, period 4 for same-letter pairs), so the mechanism has no way to order two distinct letters carrying the same `ε`, and their `β` contributions differ. Minimal witness at length 4: with `k = g(ε=0,β=0)`, `u₀ = g(ε=1,β=0)`, `u₁ = g(ε=1,β=1)`, the words `k u₀ u₁ k` and `k u₁ u₀ k` agree on every certified feature in both directions but have images `(4,1)` and `(4,2)`. This is a distinct obstruction from `F20-FULL-OBS-01`: that one is about the base cut failing aperiodicity because of the `ε = 2` letters, this one is about expressive power and appears with no `ε = 2` letter present. It refutes route (ii) of `N-F20-001`. | PROVED | Human proof in `notes/f20_subalphabet_obstruction.md` §2 (seven clauses: identical `ε` sequence hence identical phase trajectory, hence identical arrivals; identical nonmover arrivals; no certified pair pattern exists; identical letter multiset and endpoints; reversal symmetry; and `β = 2β_{u₀} + β_{u₁}` versus `2β_{u₁} + β_{u₀}`, distinct mod 5 because `β_{u₀} ≠ β_{u₁}`). Its one computed input, the complete certified pattern table, is exact finite enumeration of the transition monoid including same-letter pairs. `scripts/research/f20_subalphabet_obstruction.py` checks every clause mechanically, verifies minimality by exhaustive search per length (1,765 words for 10 letters, 5,045 for 15), shows the witness survives on the 15-letter sub-alphabet, shows it survives when 555 uncertified `Σε = 0` patterns are granted for free, and runs a positive control on the 2-generator alphabet where the same family does separate the image on all 8,191 words of length at most 12. Run manifest `data/experiments/f20_subalphabet_obstruction.md`. CAUTION: this is NOT a lower bound (research rule 1) — it bounds one mechanism, and says nothing about the actual height of any language; `F20-STD-01` is unaffected **2026-07-25 audit:** as with `F20-FULL-OBS-01`, the `A4-FULL-01` reference is to the *mechanism*, which is `EMPIRICAL` as a claim but perfectly well defined as a construction; the obstruction proved here does not depend on that claim being true. This row stays `PROVED`.  This row is `PROVED`. | hora-algebra | 2026-07-25 |
| F20-BLOCK-OBS-01 | **Delay theorem.** Let `G` be a finite group and `Σ = G` the full-element alphabet, so the identity element is a letter. Then every *finite* code `X ⊆ Σ⁺` with bounded delay (i.e. `Σ* = X* F` for some finite `F`) satisfies `mu(X) = G`; and if it only has to parse the identity fibre `T`, then `mu(X) ⊇ G ∖ {e}`. Consequence for route (iii) of `N-F20-001`: the token word of a block decomposition is again a word over an alphabet of group elements obeying the same coordinate formula, so the decomposition is a **self-reduction with no gain** — for `F_20` the token alphabet again carries `ε = 2` elements, the base cut again has a period-2 element and none of the 291 candidate patterns certify. Giving up finiteness does not help: the one canonical phase-neutral code, the first-return code `R` with `T = R*`, is infinite and also not star-free, because its DFA **is** the §5.5 base cut of `F20-FULL-OBS-01`. | PROVED | Human proof in `notes/f20_block_decomposition.md` §2 (three lines: `z e^D` has a prefix in `X`, and every nonempty prefix of it has image `z`) and §5. `scripts/research/f20_block_decomposition.py` checks every step exactly and without sampling: the token-level coordinate formula agrees with the letter level on all 336,842 words of length ≤ 4 for the codes `Σ²` and `Σ³`; the witness families `z e^n` and `z e^n z^{-1} ∈ T` for all 20 resp. 19 elements; all 20 maximal proper token images unrealizable on the full alphabet, reduced from all proper images by a verified monotonicity check; a realizability criterion on the image walk cross-checked against direct enumeration of the code-free set on 63 (alphabet, image) pairs with 0 disagreements, every realizable case witnessed by an explicitly constructed frontier code; 0 of 291 token-level patterns certify; a single `ε = 2` token still has period 2 on the genuine 400-token alphabet `Σ²` (1,606 DFA states), so token multiplicity does not repair aperiodicity. Positive control: on the 2-generator alphabet of `F20-STD-01` finite bounded-delay codes with a **proper** token image do exist (minimum size 2, and 4 when length-1 tokens are forbidden, realized by `X = Σ²`), so the theorem is specific to the alphabet containing the identity element and the judge is not answering "unrealizable" by default. Run manifest `data/experiments/f20_block_decomposition.md`. CAUTION: this is NOT a lower bound (research rule 1) — it closes one mechanism. The residual route named when this row was first written — star-free **infinite** codes — was **closed the same day by `F20-BASECODE-01`**, which also shows it was never viable (it fails on the 2-generator instance where height one is proved). Still not excluded: non-greedy or context-dependent factorizations, and finite unions using a different code per case; neither is yet in the shape of a mechanism | hora-algebra | 2026-07-25 |
| FULL-ALPH-RED-02 | The full-alphabet reduction of `FULL-ALPH-RED-01` needs only `\|G\| - 1` letters: the identity element may be deleted from the alphabet. Precisely, let `pi : Σ* → (Σ ∖ {e})*` erase the identity letter; then `mu = mu ∘ pi`, so `T_Σ = pi^{-1}(T_{Σ∖{e}})`, and `pi^{-1}` preserves generalized star height ≤ 1. Hence `gsh(T_{Σ∖{e}}) ≤ 1` implies `gsh(T_Σ) ≤ 1`. | PROVED | Human proof in `notes/f20_block_decomposition.md` §6, four steps: `pi^{-1}` commutes with Boolean operations; `pi^{-1}(L₁L₂) = pi^{-1}(L₁) pi^{-1}(L₂)` because erasure neither merges nor splits letters; `pi^{-1}` of a star-free language is star-free, since it is recognized by the same monoid and syntactic monoids divide; and the star step `pi^{-1}(A*) = (B ∪ {e})*` where `B = pi^{-1}(A)` minus the empty word and minus every word ending in `e`, with `B ∪ {e}` star-free whenever `A` is. Steps 2 and 4 are machine-checked in `scripts/research/f20_block_decomposition.py` §6 (9,837 concatenation instances; the star identity on all 3,280 words of length ≤ 7 for four test codes), together with `mu(w) = mu(pi(w))` on all 8,421 words of length ≤ 3. The naive guess `pi^{-1}(A*) = ({e} ∪ A)*` is **false** and the script exhibits its failures (counterexample `A = {a, ka}`, `w = k e a`), which is also the control proving the checker is not vacuous. CAUTION: `pi` is a non-alphabetic morphism, and PST 1992 item 7 records that non-alphabetic inverse morphisms do NOT preserve height in general; the claim here is the special case of erasure, with step 2 identified as what fails for length-increasing morphisms. Registered for independent audit as `N-ERASE-AUDIT-001` and NOT to be used as settled folklore until that audit closes. This reduction is generic: it applies to `A_5`, `C_7⋊C_3` and `S_4` as well. It does not by itself help route (iii) — see `F20-BLOCK-OBS-01`, where 19 letters still force `\|mu(X)\| ≥ 19` | hora-algebra | 2026-07-25 |
| F20-BASECODE-01 | The identity fibre `T = mu^{-1}(e)` is a **biunitary** submonoid of the free monoid (`uv ∈ T` and `u ∈ T` imply `v ∈ T`, and symmetrically), hence a **free** submonoid, so its base is unique: the first-return code `R_T = { w ∈ T ∖ {ε} : no proper nonempty prefix of w lies in T }`. Therefore the question "is there a star-free **code** `C` with `C* = T`?" is a single decision problem, and the answer is **no**: the minimal DFA of `R_T` has 22 states and the single letter `a = (x ↦ x+1)` has period 5 in its syntactic monoid, so `R_T` is not aperiodic and by Schützenberger 1965 not star-free. **Calibration, and the point of the row**: the same computation on the 2-generator alphabet — where `gsh(T) = 1` is already established by `F20-STD-01` — also returns "not star-free" (22 states, period 5). A star-free `C` with `C* = T` is only a *sufficient* condition for height one, and it fails on an instance where height one holds; so this route could never have been the mechanism behind a height-one representation. | COMPUTED | `scripts/research/f20_block_decomposition.py` §8, exact and without sampling: biunitarity on all words of length ≤ 4 (8,422 fibre words); the base equals the first-return code, checked as `T` minus the products of two nonempty fibre words (7,240 words agree exactly); non-aperiodicity by an explicit period-5 element of the syntactic monoid (the transition monoid of the *minimized* DFA — minimizing first is required, since a period in a non-minimal automaton can die in the quotient). First established by full transition-monoid enumeration (20,971,502 elements on 20 letters, 294,881 on 2 letters, max period 5 in both); the script carries the `O(states)` witness version of the same verdict. Run manifest `data/experiments/f20_block_decomposition.md`. CAUTION: this is NOT a lower bound (research rule 1). Scope: decided for `C` a **code**. For an arbitrary generating set `R_T ⊆ C ⊆ T` not required to be a code it is undecided here — two data points only (`C = R_T` above, and `C = T`, whose syntactic monoid is the group `F_20`) — but the calibration removes the motive for pursuing it | hora-algebra | 2026-07-25 |
| C7C3-FULL-01 | For the full 21-element alphabet of `C_7⋊C_3` (order 21, the smallest odd-order group in the `FRONTIER-ORD20-01` exception list), the multi-mover mechanism of `A4-FULL-01` (`RESULTS.md` §5.5) transposes without modification: all 288 candidate cut patterns are aperiodic, both mover classes give a GF(7) system of full rank 6/6 so that `β` is determined, and the certified features reconstruct membership in the identity fibre correctly on every word tested. This is the first full-alphabet positive result for any group in that exception list, and it confirms the diagnosis of `F20-FULL-OBS-01` on an independent group: the phase group here is `Z/3`, of prime order, so the composite-order failure mode of `Z/4` cannot arise. | EMPIRICAL | **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit, on merging this branch. Updated 2026-07-28: the sample this row was downgraded for is now DECIDED, and the row is still `EMPIRICAL` for a different and much narrower reason.** Section 7 of `scripts/research/metacyclic_full_alphabet.py` — the parameterized form of this row's own script — BFSes the product of the certified-feature machine with the group element and finds agreement at all 442 reachable product states, hence on every word with no length bound, with all 84 single-coefficient mutations rejected, each re-decided by its own BFS rather than replayed. What is still sampled is section 7(c) alone: the comparison of that machine with the section 5 function this row's evidence describes, on bounded and sampled words. So the ceiling rule caps the row there, not at the reconstruction (`N-METACYC-TRANS-001`). Worth recording and NOT evidence: those 442 states are also the reachable count of `scripts/research/c7c3_identity_proof.py` behind `C7C3-IDENT-01`, reached by an independent route over a different certified family; agreement of two counts is a good sign and not a proof. The original 2026-07-25 text follows unchanged. The sample, stated up front: the reconstruction of the identity fibre is exact only on all 204,205 words of **length <= 4** plus 40,000 fixed-seed words of length 5..400 — the row's own CAUTION already said "exhaustive coverage stops at length 4". This is the identical defect to `A4-FULL-01`, which is unsurprising since it is the identical mechanism. It previously read `COMPUTED`, and passed the gate because its evidence said "no sampling in the exhaustive parts" — a tautology, now rejected by `SCOPED_AWAY`. **What is unaffected**: the sub-steps that really are exhaustive (288/288 patterns by complete transition-monoid enumeration; the group identification; the three negative controls) — those stand, they simply do not decide this row's claim. **What supersedes it**: `C7C3-IDENT-01` (`PROVED`) proves the reconstruction identity for **every** word with no length bound, but over a smaller certified family (57 atoms, not this row's 741 conditioned cuts), so it does not retroactively certify this row as stated. Prior evidence: `scripts/research/c7c3_full_alphabet.py` (exact where finite, 53 s, no sampling in the exhaustive parts): group verified as the unique non-abelian order-21 group by exhaustive closure/associativity/identity/inverse checks plus the presentation `⟨x,y : x⁷ = y³ = 1, yxy⁻¹ = x²⟩` with `⟨x,y⟩` the whole group; 288/288 patterns certified by complete transition-monoid enumeration; solved `β` functionals `6·F1+4·F2+1·B0+3·B1` (`ε=1`) and `5·F0+4·F2+3·B0+2·B1` (`ε=2`) re-checked independently on 5000 random `(x,n)` vectors; reconstruction exact on all 204205 words of length ≤ 4 and 40000 fixed-seed words of length 5..400. Three controls, because the positive branch of this computation had never been executed for any group: the 14 excluded same-letter pure-power patterns all FAIL aperiodicity (period 3), so the judge can answer no; the membership base rate is 4.76 % (theoretical 1/21), so a constant predictor would not pass; and all 84 single-coefficient perturbations of the solved combinations break the reconstruction. Derivation `notes/c7c3_full_alphabet.md`; run manifest `data/experiments/c7c3_full_alphabet.md`. CAUTION: this is NOT a proof of `HeightOneForGroup (C_7⋊C_3)` — no height-one regular expression was constructed or compiled and no language equivalence was proved, unlike `F20-STD-01` and `WEIS-L2-GSH-01`; agreement on finitely many words is not language equality, exhaustive coverage stops at length 4, and `N-C7C3-001` stays OPEN  This row is `EMPIRICAL`. | hora-algebra | 2026-07-25 |
| C7C3-IDENT-01 | For the full 21-element alphabet of `C_7⋊C_3`, the reconstruction of the identity fibre from certified cut counts holds for **every** word, not merely up to length 4: writing `ν(u)` for the right-to-left product of the letters, `ν(u) = e` iff the total phase is 0 and a fixed GF(7) combination of 57 cut counts vanishes. **2026-07-25 audit:** `C7C3-FULL-01` is `EMPIRICAL`; this row does not consume it — it *replaces* its sampled reconstruction with a proof over a different, smaller family, and is verified independently by complete state-space BFS. Hence this row stays `PROVED`. The certified family is much smaller than the one of `C7C3-FULL-01`: one `("anti", g)` pattern (skip the cut iff the letter is `g` and the previous letter is not `g`) replaces the 20 pair patterns that scheme summed per mover, and three nonmover bit-set patterns replace six single-letter ones, cutting 741 conditioned cuts to 57 atoms. | PROVED | The key relations `cF_p + cB_p = 2^p` and `cF_{p+ε} = cF_p − 2^p` are forced (a one-parameter family containing the particular solutions `6F1+4F2+B0+3B1` and `5F0+4F2+3B0+2B1` that `scripts/research/c7c3_full_alphabet.py` reached by elimination); the second telescopes, giving `cF_p + cB_{p+(L−1)ε} = Σ_{j<L} 2^{p+jε}` for a maximal run of length L starting at prefix phase p, which is that run's true contribution to `β`. Summing over maximal runs proves the identity with no length bound. Machine-verified by complete state-space BFS in `scripts/research/c7c3_identity_proof.py` (2.5 s, no sampling in the proof parts): `β_prefix = δ_total` over 64 reachable states; 45 statements `base_cut − conditioned_cut = intended count` each by BFS carrying both cut machines' private previous-letter state; the fibre characterization over 442 reachable states. Aperiodicity of all 14 `anti` and 3 `set` patterns by complete transition-monoid enumeration, with both control families failing (period 3). Cross-checked end to end against actual cut counts on all 204205 words of length ≤ 4 and 4000 sweep words. Four negative controls: 72 coefficient perturbations, the dropped repeat correction, 57 term perturbations, and the 4.76 % base rate. Derivation `RESULTS.md` §5.16.1; run manifest `data/experiments/c7c3_identity_proof.md`. CAUTION: this is the arithmetic half only. It is NOT a proof of `HeightOneForGroup (C_7⋊C_3)` — no height-one regular expression has been built or compiled and no language equivalence has been proved. `N-C7C3-001` stays OPEN. | hora-algebra | 2026-07-25 |
| ALPH-RED-01 | Let `h : Σ* → Δ*` be **alphabetic** (letter-to-letter and non-erasing: `h(a) ∈ Δ` for every letter, so `h` preserves length). Then `gsh(L) ≤ 1` implies `gsh(h^{-1}(L)) ≤ 1`. | PROVED | Human proof in `notes/f20_alphabetic_reduction.md` §2, four steps: `h^{-1}` commutes with Boolean operations; `h^{-1}(L₁L₂) = h^{-1}(L₁)h^{-1}(L₂)` and `h^{-1}(L*) = (h^{-1}(L))*` because a length-preserving letterwise morphism transports the factorization boundaries verbatim; and `h^{-1}` of a star-free language is star-free by induction on the expression, the only nontrivial base case being `h^{-1}({d}) = {a ∈ Σ : h(a) = d}`, a finite set of letters. This makes the inverse-alphabetic-morphism half of `PST-CL-01` (`CITED`, "exact hypotheses must be checked") self-contained inside the repository for the non-erasing case; the erasing case is `FULL-ALPH-RED-02` and is genuinely harder. Consistent with PST 1992 item 7, which denies the statement for **non-**alphabetic inverse morphisms This row is `PROVED`. | hora-algebra | 2026-07-25 |
| SUBDIRECT-RED-01 | Let `G` be finite and `N_1, …, N_k ⊴ G` nontrivial with `⋂_j N_j = 1`. If every quotient `G/N_j` is a height-one group then so is `G`. Equivalently: the class of height-one groups is closed under finite **direct products** (take the two kernels `1×Q_2`, `Q_1×1`), hence under subdirect products. Since "some family of nontrivial normal subgroups has trivial intersection" is exactly "`G` is **not monolithic**", the consequence is that **only monolithic groups need a direct attack**: a group with two distinct minimal normal subgroups reduces to proper quotients. | PROVED | Human proof in `notes/f20_alphabetic_reduction.md` §4: the quotient map `q_j : G → G/N_j` induces an *alphabetic* morphism `Σ = G → Σ_{G/N_j} = G/N_j` whose preimage of the smaller identity fibre is `mu^{-1}(N_j)`; apply `ALPH-RED-01`, intersect, and use `FULL-ALPH-RED-01` at both ends. Machine support `scripts/research/f20_alphabetic_reduction.py` §2 (normal subgroups enumerated completely as the subgroups generated by unions of conjugacy classes) and §4 (positive control on `C_6`: `T = mu_1^{-1}(e) ∩ mu_2^{-1}(e)` over 2- and 3-letter alphabets on all 55,987 words of length ≤ 6). **Independent check against already-settled data**: `Dic_3` is not monolithic (normal subgroups of order 2 and 3 meeting trivially, quotients `≅ S_3` and `C_4`, both of order < 12), so this theorem re-derives `DIC3-ALL-01` without the `DIC3-RED-01` embedding. CAUTION: the direct-product closure may be folklore; the literature check is registered as `N-ALPH-CITE-001` and is NOT done, so do not cite this as new | hora-algebra | 2026-07-25 |
| F20-QUOT-OBS-01 | **Rigidity.** Over the full alphabet `Σ = G`, if relabelings `f_j : G → G'_j` have `⋂_j mu_j^{-1}(e) = T`, then every `f_j` is a group homomorphism and the intersection is `mu^{-1}(⋂_j ker f_j)`. Consequence for route (iv) of `N-F20-001`: since `F_20` is **monolithic** (normal subgroups exactly `1`, `C_5`, `D_5`, `F_20`, and every nontrivial one contains `C_5`), the identity-fibre form of route (iv) can only ever reach `mu^{-1}(N)` with `N ⊇ C_5`, never `T`. What it does give is the partial positive `gsh(mu^{-1}(C_5)) ≤ 1`, so the residual difficulty of `HeightOneForGroup F_20` is localized entirely in the `C_5` coordinate at phase 0. | PROVED | Human proof in `notes/f20_alphabetic_reduction.md` §3 and §5: the hypothesis applied to the three words `(e)`, `(g)(g^{-1})`, `(g)(h)((gh)^{-1})` — all of which exist in `Σ*` precisely because the alphabet is the whole group — forces `f(e) = e`, `f(g^{-1}) = f(g)^{-1}` and `f(g)f(h) = f(gh)`. Machine-verified in `scripts/research/f20_alphabetic_reduction.py` §3 by exhausting **all** maps `G → G` for `C_4`, `C_6`, `S_3` (256, 46,656, 46,656): the maps preserving every fibre word of length ≤ 3 are exactly the 4, 6 and 10 endomorphisms, so the lemma is sharp at length 3. CAUTION: this bounds only the identity-fibre form of route (iv). With general accepting sets the route is strictly stronger and NOT blocked — see `F20-ALPH8-01`. This is NOT a lower bound (research rule 1) | hora-algebra | 2026-07-25 |
| F20-ALPH8-01 | Route (iv) of `N-F20-001` is **not** blocked, and it reduces the alphabet. Let `Δ = Z/4 × {0,1} ⊆ F_20` (8 letters, generating `F_20`). Then `gsh(T_Δ) ≤ 1` implies `HeightOneForGroup F_20`; and since `Δ` contains the identity, `FULL-ALPH-RED-02`-style erasure drops this to a **7-letter** obligation. Construction: with `H = {(x₁,…,x₄) ∈ F_20⁴ : the four phases agree} ≅ (C_5⁴)⋊C_4`, the map `rho(ε, u₁,…,u₄) = (ε, Σ u_i)` is a homomorphism `H → F_20`, and `λ(ε,β) = ((ε,a₁),…,(ε,a₄))` with `(a_i) ∈ {0,1}⁴`, `Σ a_i = β`, is a section of it; so `mu = rho ∘ Φ` and `T_Σ = Φ^{-1}(ker rho)` is a union of 125 intersections of `Δ`-instances pulled back by alphabetic morphisms (`ALPH-RED-01`). **Lemma G**: for any class of groups closed under subgroups, quotients and direct products and not containing `F_20`, no such reduction can have all its coordinate groups in that class; instantiated with "abelian by elementary abelian 2" this forces at least one coordinate to be an `F_20` instance over a proper sub-alphabet. So route (iv) can never cross the frontier using known height-one *groups* — only alphabet-restricted results such as `F20-STD-01`. | PROVED | Human derivation in `notes/f20_alphabetic_reduction.md` §§6–8. `scripts/research/f20_alphabetic_reduction.py` §6–§7, exact where it matters: `rho` verified to be a homomorphism on **all 6,250,000 pairs** of `H`; `lambda` a section landing in `H`; `mu(w) = rho(Φ(w))` on all 168,421 words of length ≤ 4 plus 60,000 random words of length 5..40; `T = Φ^{-1}(ker rho)` on all 168,421 words of length ≤ 4; the erasure identity on all 37,449 `Δ`-words of length ≤ 5; `F_20` verified not (abelian)-by-(elementary abelian 2) while `C_4`, `C_5`, `C_20`, `C_2×C_2`, `D_5`, `S_3` are, and among the 14 subgroups of `F_20` only `F_20` escapes. `F_20` itself is cross-checked against `scripts/research/f20_full_alphabet.py` on the element set, identity and all 400 products. Negative controls: an all-constant family does not factor, 12 of 12 random phase-preserving splittings do not factor, and a family keeping one coordinate injective factors but yields no reduction. Run manifest `data/experiments/f20_alphabetic_reduction.md`. **CAUTION, and it is the point**: this is a reduction of the obligation, NOT a mechanism. Both known obstructions transfer to `Δ`, verified rather than assumed — `Δ` still carries the `ε = 2` letters `(2,0)`, `(2,1)` of `F20-FULL-OBS-01`, and the minimal witness of `F20-SUB10-OBS-01` (`k = (0,0)`, `u₀ = (1,0)`, `u₁ = (1,1)`) consists entirely of `Δ` letters with the two words still separated (`(2,1)` vs `(2,2)`). The witness does collapse under the erasure to 7 letters, since `k` is the identity letter, so the 7-letter instance needs its own test. Not a lower bound (research rule 1) | hora-algebra | 2026-07-25 |
| DIC3-RED-01 | `Dic_3` embeds into `H = (C_3×C_4) ⋊ C_2` (τ acting by inversion on `C_3`), and `H` lies in the Pin–Straubing–Thérien class "abelian by elementary abelian 2-group"; via the injection, every `Dic_3`-recognized language is `H`-recognized. | PROVED | `RESULTS.md` §3 (order-12 audit): explicit embedding a ↦ ((1,0),0), b ↦ ((0,1),1), relations, injectivity, accepting-set transfer via injectivity; exhaustively machine-verified in `scripts/research/dic3_embedding.py` (relations, 12 distinct images, Dic3 order profile, abelian normal `C_3×C_4` with index 2, no A4-profile subgroup of order 12 in `H`); adversarial review pass 2026-07-23 | hora-algebra | 2026-07-23 |
| DIC3-ALL-01 | Every language recognized by `Dic_3` has generalized star-height at most one. | CITED | PST-GRP-03 (Pin–Straubing–Thérien 1992 semidirect-product theorem) applied through DIC3-RED-01; closes the `Dic_3` half of Bourne's order-12 barrier (SMALL-12-01, SMALL-C3-FAIL) by routing around the `C_3` unique-factorization obstruction instead of repairing it. **Second, independent derivation added 2026-07-25**: `Dic_3` is not monolithic (normal subgroups of order 2 and 3 meeting trivially), and its quotients `Dic_3/C_2 ≅ S_3` and `Dic_3/C_3 ≅ C_4` both have order < 12, so `SUBDIRECT-RED-01` gives the same conclusion without the `DIC3-RED-01` embedding; machine-checked in `scripts/research/f20_alphabetic_reduction.py` §2. The two routes share only PST 1992 Cor. 7.7 | hora-algebra | 2026-07-23 |
| PST-DIV-CRIT-01 | If a finite group `G` divides `A ⋊ E` with `A` commutative and `E` elementary abelian 2, then `G` has a commutative normal subgroup `B` with `G/B` elementary abelian 2. Equivalently (and decidably): some commutative normal `B ◁ G` satisfies `x² ∈ B` for all `x` and `[x,y] ∈ B` for all `x,y`. Failure of this condition *proves* that `G` divides no such semidirect product, for any size of `A` and `E`. | PROVED | one-paragraph proof in `notes/small_group_pst_frontier.md` §2; the same exact sequence already appears in `RESULTS.md` §3 for the special case `G = A_4`, so the contribution here is the extraction of a decidable criterion, not the argument; implemented and exercised in `scripts/research/small_group_pst_coverage.py` (`pst_necessary_criterion`) | hora-algebra | 2026-07-25 |
| DICM-EMB-01 | For every `n ≥ 2` the dicyclic group `Dic_n`, generated by `x, y` with defining relations `x^{2n} = 1`, `y² = xⁿ`, `y⁻¹xy = x⁻¹`, embeds into `H_n = (C₂ × C_{2n}) ⋊ C₂`, where the `C₂` acts by `u ↦ u vⁿ`, `v ↦ v⁻¹`, via `x ↦ v`, `y ↦ u t`. Hence every dicyclic group — in particular every generalized quaternion group `Q_{2^k}` — lies in the PST-GRP-03 class and has generalized star-height at most one for all recognized languages. | PROVED | three-line proof in `notes/small_group_pst_frontier.md` §3 (`(ut)² = u·φ(u) = vⁿ`, `(ut)⁻¹v(ut) = v⁻¹`, and the `4n` images are distinct); the explicit map is machine-verified to be an injective homomorphism for `n = 2..12` in `scripts/research/small_group_pst_coverage.py` step [5]; generalizes `DIC3-RED-01` (which used the different host `(C_3×C_4)⋊C_2`) from `n = 3` to all `n`; run manifest `data/experiments/small_group_pst_coverage.md` | hora-algebra | 2026-07-25 |
| PST-WREATH-06-01 | Pin–Straubing–Thérien 1992 also prove: if a rational language is recognized by a monoid in the variety generated by wreath products `M ∘ (G ∘ N)` with `M`, `N` aperiodic monoids and `G` a commutative group, then it has generalized star-height at most one (the result indexed as item 6 in `docs/SURVEY.md` §3). | CITED | statement of the theorem's shape taken from the paper's abstract, retrieved 2026-07-25 via web search of the HAL/ScienceDirect records; CAUTION: the full-text PDF (`hal.science/hal-00019978v1/file/StarHeight.pdf`) is behind anti-bot protection and ScienceDirect returns 403, so the wording has NOT been verified verbatim against the primary source and the exact wreath-product convention is unknown; `PST-WREATH-COMM-01` is deliberately proved so that it is insensitive to that convention. **Superseded 2026-07-25 by `PST-WREATH-78-01`**, which has the theorem number (7.8) and the sharp form from a quotable secondary source: the base factor must be **aperiodic**. That also explains `PST-WREATH-COMM-01` structurally rather than by computation | hora-algebra | 2026-07-25 |
| PST-WREATH-COMM-01 | Let `V` be the monoid pseudovariety generated by wreath products `M ∘ (G ∘ N)` with `M`, `N` finite aperiodic monoids and `G` a finite commutative group. Then every group in `V` is commutative. Consequently the Pin–Straubing–Thérien wreath-product result (`PST-WREATH-06-01`) settles the full solution for no non-abelian group whatsoever. | PROVED | proof in `notes/small_group_pst_frontier.md` §3.5: the projection of a wreath product onto its non-aperiodic-free factor is a monoid morphism whose restriction to a subgroup has kernel inside a direct power of an aperiodic monoid, hence trivial, so subgroups embed downward twice and land in `G^N`; combined with the standard fact that a group dividing a finite monoid divides one of its subgroups (`CITED`, standard semigroup theory). The argument is symmetric in the wreath-product convention, so it does not depend on the unverified wording of `PST-WREATH-06-01`. This closes, inside PST 1992, the coverage caveat attached to `FRONTIER-ORD20-01` This row is `PROVED`. | hora-algebra | 2026-07-25 |
| SMALL-NONAB-31-01 | Among the 45 non-abelian groups of order at most 31, exactly six lie outside the PST-GRP-03 class: `A_4` (12), `F_20 = C_5⋊C_4` faithful (20), `C_7⋊C_3` (21), `SL(2,3)` (24), `S_4` (24), `C_2×A_4` (24). All other 39 are covered by `PST-GRP-02` (nilpotent of class 2) or `PST-GRP-03` (split decomposition, or an explicit embedding). All six exceptions are solvable. | COMPUTED | `scripts/research/small_group_pst_coverage.py` (exact, no sampling, ~3 s): constructions validated exhaustively (closure/associativity/identity/inverses), the catalogue verified pairwise non-isomorphic with per-order counts matching the standard classification (`CITED`), nilpotency class from the upper central series, split decompositions by exhaustive subgroup search, non-membership by `PST-DIV-CRIT-01` (exact, not a search), positive divisor cases by explicit embeddings; the run reproduces `SMALL-12-01` as an assertion; run manifest `data/experiments/small_group_pst_coverage.md`; derivation `notes/small_group_pst_frontier.md` §4 This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| FRONTIER-ORD20-01 | **Corrected 2026-07-25 by the completeness audit; the previous wording is withdrawn.** Six non-abelian groups of order at most 31 lie outside the PST-GRP-03 class (`SMALL-NONAB-31-01`, `COMPUTED`): `A_4` (12), `F_20` (20), `C_7⋊C_3` (21), `SL(2,3)`, `S_4`, `C_2×A_4` (24). **None of the six currently has a complete verification of its full solution in this ledger**, because `A_4`'s only evidence, `A4-ALLLANG-01`, is `EMPIRICAL`. Hence the smallest non-abelian group whose full solution is unsettled is **`A_4` (order 12)**, and the barrier stands where `SMALL-12-01` (Bourne 2017) put it. The earlier reading — "the barrier moved from order 12 to order 20" — is WITHDRAWN; it may be reinstated only when `A4-FULL-01` is upgraded to `COMPUTED`. **Amended 2026-07-25 by `SUBDIRECT-RED-01`** (`PROVED`), merged from `origin/main`: `C_2×A_4` is not monolithic, so it reduces to its quotients `C_2` and `A_4`. On `origin/main` this was read as `C_2×A_4` leaving the list; that reading assumed `A_4` is height-one, which is `EMPIRICAL`. Corrected: `C_2×A_4` does not leave the list, it **collapses onto `A_4`** — it is height-one exactly when `A_4` is. So the six exceptions are five distinct problems, `A_4` / `F_20` / `C_7⋊C_3` / `SL(2,3)` / `S_4`, all five monolithic, with `C_2×A_4` attached to `A_4`. | COMPUTED | group-theoretic content is `SMALL-NONAB-31-01` (`COMPUTED`, exact, unaffected by the audit); the correction is bookkeeping over this ledger's own statuses and is verifiable by inspecting the `A4-FULL-01` / `A4-ALLLANG-01` rows. CAUTION on scope, unchanged: the literature side is `UNREVIEWED` — the audit covers `PST-GRP-01/02/03` plus `PST-WREATH-COMM-01`, which proves that PST 1992's wreath-product result covers no non-abelian group at all and so cannot settle any of the six; what remains unchecked is other 1992–2026 literature, for which no search specific to these six groups was made; "outside the PST class" is never a star-height lower bound (research rule 1); `notes/small_group_pst_frontier.md` §§5–6  This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| ORD12-ALL-01 | Every language recognized by a group of order at most 12 has generalized star-height at most one. | EMPIRICAL | **Downgraded to `EMPIRICAL` 2026-07-25 by the completeness audit.** **Inherited via `A_4`**: with `A4-ALLLANG-01` at `EMPIRICAL`, order 12 is NOT settled and this claim is not established. Everything except `A_4` survives at its own level (`CITED` / `PROVED`). Do not write 'order <= 12 is settled' anywhere while this row is `EMPIRICAL`. Prior evidence: order < 12: PST 1992 Corollary 7.7 (`CITED`); `C_12`, `C_6×C_2`: PST-GRP-01; `Dih_6 = C_6⋊C_2`: PST-GRP-03; `Dic_3`: DIC3-ALL-01; `A_4`: A4-ALLLANG-01 — the label is the weakest link (`A_4` base case is `EMPIRICAL`); order-12 classification into 5 groups is standard (Sylow); independent human review pending for the `A_4` chain The upstream sample, stated here so the gap is auditable without following the chain: `A_4`'s reconstruction step is exhaustive only to length 4 plus 4,000 random words.  This row is `EMPIRICAL`. | hora-algebra | 2026-07-25 |
| SLIDE-WITHDRAW-01 | The workshop presentation deck `site/index.html` (removed from version control) asserted "order ≤ 12 is settled" and that the finite-group barrier had moved to order 20. Both statements are withdrawn, and the deck was removed from version control on 2026-07-25. | REFUTED | The deck's claims rest on `A4-ALLLANG-01`, which the completeness audit of 2026-07-25 set to `EMPIRICAL`: the `A_4` full-alphabet reconstruction is exhaustive only to word length 4 plus 4,000 random words. Recorded rather than silently deleted, per research rule 5. The file is git-ignored and kept locally; no live deployment existed (GitHub Pages disabled, no other host), so removal from the default branch is the whole of the exposure under this repository's control — the blob remains reachable in git history and in any fork. Re-publication is gated on `N-A4FULL-002`. This row is `REFUTED`. | hora-algebra | 2026-07-25 |

| CORE2-EQV-EXT-01 | For every finite alphabet, the global height-one collapse conjecture is equivalent to: for all star-free languages A,B,C,D, the language (A ∪ B D* C)* has generalized star-height at most one. | UNREVIEWED | External merged-main proof note `hora-algebra/exploring-math/ELEMENTARY_EXPANSION_TWO_STATE_EQUIVALENCE.md` (outside this repo); cross-repository audit in `RESULTS.md` §6.1; not yet independently reconstructed or formalized here | cross-repo audit | 2026-07-24 |
| CORE2-4096-EXT-01 | Every tuple in the designated 4096 singleton-word CORE2 family has generalized star-height at most one. | UNREVIEWED | External exact audit merged in `exploring-math` PR #2, especially `generalized-star-height/CORE2_FINAL_PAIR_EXPLICIT_GH1.md` (outside this repo) and `check_core2_final_pair_explicit_gh1.py`; merge commit 8ca43507f6ab2681535606abd3ac4215ffddab61; not replayed here | cross-repo audit | 2026-07-24 |
| GH1-KR-EXT-01 | For every n ≥ 3 there is a finite binary prefix code C_n with T_n dividing the syntactic monoid of C_n*, while C_n* has generalized star-height at most one; binary finite-code star syntactic monoids are division-universal. | UNREVIEWED | External proof/audit `exploring-math/generalized-star-height/FINITE_CODE_TN_LIFT_PROBE.md` (outside this repo), merged in PR #2; exact n=4 replay and primary references recorded there; not independently audited here | cross-repo audit | 2026-07-24 |
| CORE2-A5-RAW-EXT-01 | For the fixed order-5/order-5 CORE2 tuple A=S, B=C=D=∅ in exploring-math, the complete raw relation image has 2723 elements and all raw-stamp-recognized languages have an external fixed-stamp GH1 certificate; this does not settle the a5_235 word problem. | UNREVIEWED | External exact audit `exploring-math/generalized-star-height/CORE2_CONCRETE_A5_RAW_NUQ.md` (outside this repo), merged in PR #2; no local replay | cross-repo audit | 2026-07-24 |
| CORE2-SCH1-EXT-01 | For the fixed C4 benchmark, the full finite pool of 2178 one-marker languages leaves a period-4 relative-orbit witness, while the canonical full Sch1 profile search is inconclusive after 1,000,000 joint states without closure. | UNREVIEWED | External exact/cutoff records in `exploring-math` PR #2: `CORE2_C4_FULL_MARKED_POOL_CEGAR_20260722.md` and `CORE2_SCHUETZENBERGER_PROFILE_COMPLETENESS_20260722.md`; not replayed here | cross-repo audit | 2026-07-24 |
| F20-FIB-01 | The `F_20` word problem is exactly the trivial-holonomy condition of a flat affine `Z/5`-bundle. Writing elements as `(α, β)` with `α = 2^ε`, the map `ε : Σ* → Z/4` is a monoid morphism (the base projection) and `β : Σ* → Z/5` satisfies `β(uv) = ε(v)·β(u) + β(v)`, i.e. `β` is a **1-cocycle (crossed homomorphism)** for the `Z/4`-action pulled back along `ε`. The §5.12 coordinate formula `β(w) = Σ_i β_i 2^{E_i}` with `E_i` the suffix phase is not a derived gadget but the unique solution of that condition, and `E_i` is the base holonomy of the suffix. Over a free monoid there is no cocycle obstruction: `Z¹(Σ*, Z/5) = (Z/5)^Σ`, so the difficulty is the *definability* of `β`, never its existence. | PROVED | Induction on word length from the product law, in `notes/f20_fibration_geometry.md` §1. `scripts/research/f20_fibration_geometry.py` §1 checks the cocycle identity on all 177,241 pairs of words of length ≤ 2 over the full 20-letter alphabet, on 4,000 random pairs of length ≤ 11 each, and checks freeness by extending 200 random assignments of fibre values to the letters. Ground truth is the direct group evaluator, never the formula. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| F20-COH-SEP-01 | The cohomology of the extension **cannot** separate the settled cases from the open one. In the family `C_5 ⋊_r C_4`, `r ∈ {1, 4, 2}` — namely `C_20`, `Dic_5` and `F_20` — one has `H^n(C_4, Z/5) = 0` for every `n ≥ 1` and every one of the three module structures (orders 4 and 5 are coprime). So `H² = 0` (all three split) and `H¹ = 0` (splitting unique up to conjugacy): the classifying data of the extension is identical, and zero, for all three. Yet `C_20` is settled by `PST-GRP-01`, `Dic_5` by `PST-GRP-03` + `DICM-EMB-01`, and `F_20` is the open frontier `FRONTIER-ORD20-01`. Hence no functor of the extension's classifying data is a generalized-star-height invariant. | COMPUTED | `scripts/research/f20_fibration_geometry.py` §2–§3, exact: `H^n` for `n = 0..4` from the **bar resolution** by F_5 linear algebra (the periodic-resolution formula for cyclic groups is deliberately not used), plus the family table — pairwise non-isomorphic, `r=1 ≅ C_20` and `r=4 ≅ Dic_5` machine-verified, and `pst_necessary_criterion` (`PST-DIV-CRIT-01`) returning a witness for `r = 1, 4` and `None` for `r = 2`. This strengthens `COH-01` from "no such theorem was found" to "extension cohomology provably cannot decide it"; it does **not** rule out some other cohomological construction. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| F20-MONO-FRONT-01 | Inside the order-20 family `C_5 ⋊_r C_4` the settled/open boundary is a function of **one** parameter, the order of the monodromy `r` in `(Z/5)^×`: orders 1, 2, 4 give `C_20` (settled), `Dic_5` (settled), `F_20` (OPEN). The same integer is (a) the dimension of the hard irreducible representation — `Ind_{C_5}^{G}(χ)` is a sum of `4/ord(r)` distinct irreps of dimension `ord(r)`, and for `F_20` orthogonality gives `5·1_{g=e} = 1_{ε(g)=0} + χ_ρ(g)`, so the four linear characters supply only the base counting language and a single 4-dimensional induced character carries the whole difficulty; and (b) half the 5-primary cohomological period of `G`, which is `2·ord(r)` = 2, 4, 8. The PST condition "abelian by elementary abelian 2" reads, in this language, as "the monodromy is an involution". | COMPUTED | `scripts/research/f20_fibration_geometry.py` §3, §4, §7: `⟨Ind χ, Ind χ⟩` computed exactly in `Z[ζ_5]` from the coset formula (4, 2, 1), dimensions read off by Clifford theory; an explicit 4×4 monomial model of `ρ` verified a homomorphism on all 400 products, with trace equal to the coset character, exactly one nonzero entry per row and column, and permutation part a function of `ε(g)` alone; the character identity checked on all 20 elements; the period via the LHS collapse, whose short proof is in `notes/f20_fibration_geometry.md` §5 and whose input (`H^{2k}(C_5, Z) = Z/5` with `C_4` acting by `r^k`) is CITED standard. CAUTION: "settled" for `Dic_5` rests on `DICM-EMB-01` (PROVED) plus `PST-GRP-03` (CITED); the correspondence with PST's actual proof method is `UNREVIEWED` This row is `COMPUTED`. | hora-algebra | 2026-07-25 |
| F20-TRANSD-RED-01 | Let `Γ = Z/4 × Σ`, let `σ : Σ* → Γ*` be the length-preserving right-sequential transducer tagging each letter with its suffix phase (state set `Z/4`, the monodromy group itself), and let `K = { v ∈ Γ* : Σ_{(p,g)∈v} β_g 2^p ≡ 0 mod 5 }`, which is recognized by the commutative group `C_5`. Then `mu⁻¹(e) = {w : ε(w) ≡ 0 mod 4} ∩ σ⁻¹(K)`. Both factors have gsh ≤ 1 by `PST-GRP-01`, and Boolean operations do not raise generalized star height, so **`HeightOneForGroup F_20` follows if `σ⁻¹` preserves gsh ≤ 1** (via `FULL-ALPH-RED-01`). The step is not formal: `σ⁻¹` does **not** preserve star-freeness — `A·Γ*` uses no star yet its preimage has a 5-state minimal DFA with a period-4 element — so the needed closure cannot come from any star-free closure theorem, and the gap to `PST-CL-01` (inverse *alphabetic* morphisms) is exactly "one state" versus "monodromy many states". | PROVED | Immediate from `F20-FIB-01`; `notes/f20_fibration_geometry.md` §6, §8. `scripts/research/f20_fibration_geometry.py` §5, §8: the identity on all 168,421 words of length ≤ 4 over the full alphabet and all 8,191 of length ≤ 12 over two generators, against the direct group evaluator; three negative controls (constant weights, two transposed, one zeroed) wrong on 173/160/109 words, with the note that a cyclic weight shift is *not* a control since it scales by a unit; the wreath embedding `F_20 ↪ C_5 ≀ C_4` verified injective and multiplicative on all 400 products; non-preservation of star-freeness by transition-monoid enumeration of the minimized DFA. **CALIBRATION PASSED** (`F20-BASECODE-01` rule): specialised to `{a, b}` this is verbatim the published characterization of `F20-STD-01`, agreeing with the group on all 32,767 words of length ≤ 14 — the first `N-F20-001` mechanism to survive the 2-generator test. CAUTION: for this **single** `K` the reduction is close to a restatement and falls under the AGENTS.md stop condition, so it must not be attacked in that form; the usable content is `TRANSD-ABEL-01`. Prior art unsearched (`N-FIB-PRIOR-001`): this looks like a specialisation of Straubing's wreath product principle, and novelty must not be claimed — the derivation here is elementary and depends on no citation beyond `PST-GRP-01`. Run manifest `data/experiments/f20_fibration_geometry.md` | hora-algebra | 2026-07-25 |
| TRANSD-ABEL-01 | Let `σ` be a length-preserving sequential function whose transducer has a finite **abelian group** as its state monoid. Then `σ⁻¹` preserves generalized star height ≤ 1. | CONJECTURAL | Motivated by `F20-TRANSD-RED-01`, of which the case (state group `C_4`, input a `C_5`-counting language) is `HeightOneForGroup F_20`. Strictly stronger than that target, hence not blocked by the stop condition that applies to the single-language form. Reading of the known results, `UNREVIEWED`: applying the same construction to `A ⋊ E` with `A` commutative and `E` elementary abelian 2 gives a transducer with state group `E`, so `PST-GRP-03` would be exactly the elementary-abelian-2 case of this conjecture — whether PST 1992's proof actually proceeds this way is unverified for the same reason as `PST-WREATH-06-01` (primary source full text unobtained). Independent of that reading, the *reach* of the two statements coincides on the order-20 family (`F20-MONO-FRONT-01`). Expected to be **false** for non-abelian state groups; a counterexample there would be cheap and would correctly delimit the scope, since otherwise `A_5` would fall and contradict `GSH-STATUS-01`. **Prior-art search run 2026-07-25 (`N-FIB-PRIOR-001`), outcome: not found stated, and correctly placed.** The construction is standard (`EIL-WPP-01`), so no novelty is claimed for it; the aperiodic-state case is a theorem (`PST-WREATH-78-01`) and the abelian-state case was not located in the surveyed sources, neither proved nor refuted. The scope is now pinned by `TRANSD-LADDER-01`: unrestricted state monoid is equivalent to the entire generalized star-height conjecture, and this abelian form would settle every finite solvable group. Search coverage was limited — PST 1992's full text remains unobtained — so "not found" is weaker than "not known". Derivation `notes/f20_fibration_geometry.md` §8, §10 This row is `CONJECTURAL`. | hora-algebra | 2026-07-25 |
| PST-WREATH-78-01 | Pin–Straubing–Thérien 1992, Theorem 7.8: every language belonging to the pseudovariety generated by **wreath products of abelian groups by aperiodic monoids** has generalized star height at most one. Read through the transducer picture of `F20-TRANSD-RED-01`, this is the statement "`σ⁻¹` preserves gsh ≤ 1 when the transducer's state monoid is **aperiodic**", i.e. the aperiodic rung of `TRANSD-LADDER-01`. | CITED | Quoted verbatim from Bourne–Ruškuc, *On the star-height of subword counting languages and their relationship to Rees zero-matrix semigroups*, arXiv:1603.06236, which writes: "Since all languages that belong to the pseudovariety generated by wreath products of abelian groups by aperiodic monoids have star-height at most one [9, Theorem 7.8]", with [9] = Pin–Straubing–Thérien, *Inform. and Comput.* 101(2):219–250, 1992. Retrieved 2026-07-25 from the ar5iv HTML rendering. **This supersedes the wording of `PST-WREATH-06-01`**, which came from the paper's abstract and got the shape "`M ∘ (G ∘ N)`" without identifying which factor must be aperiodic; the two are consistent, and `PST-WREATH-COMM-01` (every group in that variety is commutative) is explained by exactly this: the base must be aperiodic, so no non-aperiodic monodromy is covered. CAUTION: the PST 1992 **primary full text is still unobtained** — HAL returns an Anubis anti-bot page, ScienceDirect 403, `irif.fr` 403, and `web.archive.org` is not reachable from this environment; the theorem number and wording rest on the secondary source | hora-algebra | 2026-07-25 |
| EIL-WPP-01 | Eilenberg, Proposition IX.1.1 (with the modification for **generalized** sequential functions): if `K` is obtained as the preimage of a language under a generalized sequential function, the transition monoid of `K` divides a wreath product of the recognizing monoid by the transducer's state monoid. This is the standard machinery that the construction of `F20-TRANSD-RED-01` instantiates. | CITED | Quoted as used in Bourne–Ruškuc arXiv:1603.06236: "by Eilenberg [3, Proposition IX.1.1] (suitably modified to deal with generalised sequential functions), the transition monoid of K divides a wreath product of an abelian group by an aperiodic monoid"; [3] = S. Eilenberg, *Automata, Languages and Machines*, vol. B. Retrieved 2026-07-25. Recorded so that **no novelty is claimed** for the transducer decomposition in `F20-TRANSD-RED-01`: that construction is the wreath-product principle applied to a specific `σ`, and the repo's derivation is elementary only so that nothing depends on an unobtained citation. CAUTION: Eilenberg vol. B not consulted directly | hora-algebra | 2026-07-25 |
| TRANSD-LADDER-01 | The hypothesis on the transducer's state monoid carries the entire content of `TRANSD-ABEL-01`, and the rungs are these. (a) **No hypothesis**: for every regular `L` over `Σ`, marking each letter with the DFA state in front of it makes `L = σ⁻¹(K)` with `K = Γ*·S` **star-free**; so "`σ⁻¹` preserves gsh ≤ 1 for every finite state monoid" is **equivalent to the whole generalized star-height conjecture**, and "`σ⁻¹` preserves gsh 0" is outright **false**. (b) **Aperiodic** state monoid: PROVED, `PST-WREATH-78-01`. (c) **Elementary abelian 2**: PROVED, `PST-GRP-03` (reading `UNREVIEWED`). (d) **Arbitrary abelian** (= `TRANSD-ABEL-01`): would settle gsh ≤ 1 for **every finite solvable group**, since iterating the Krasner–Kaloujnine embedding `G ↪ G' ≀ (G/G')` along the derived series presents `G` by transducers whose state monoids are the abelian quotients `G^(i)/G^(i+1)`. (e) The **first open rung** is a cyclic state monoid of order 4, which is exactly `N-F20-001`. | COMPUTED | `scripts/research/f20_fibration_geometry.py` §9–§10, exact. **2026-07-25 audit:** the bounded run below is a sanity check only, not load-bearing — it confirms that the marking construction really does satisfy `L = σ⁻¹(K)` on a finite window. What carries each rung is elsewhere and is not a sample: (a) the equivalence is the elementary argument that the mark before the last letter is the DFA state, so `K = Γ*·S` is star-free, and the gsh-0 refutation is Schützenberger 1965 (`CITED`); (b) `PST-WREATH-78-01` (`CITED` — Pin–Straubing–Thérien 1992 Theorem 7.8, not proved here); (c) `PST-GRP-03` (`CITED`, reading `UNREVIEWED`); (d) the Krasner–Kaloujnine step is verified an injective homomorphism at every stage of the derived series by complete enumeration, not by sampling; (e) is a definition. The sanity check: (a) run on all 8,191 words of length ≤ 12 over two generators and all 168,421 of length ≤ 4 over the full alphabet, against the direct group evaluator; the refutation of gsh-0 preservation uses that `mu⁻¹(e)` has the group `F_20` as syntactic monoid, hence is not star-free (Schützenberger 1965). (d) the Krasner–Kaloujnine step `g ↦ (q ↦ t_q g t_{q·π(g)}⁻¹, π(g))` is verified an injective homomorphism at **every** stage of the derived series for all six groups outside the PST class (`SMALL-NONAB-31-01`): `A_4` 12→4→1, `F_20` 20→5→1, `C_7⋊C_3` 21→7→1, `SL(2,3)` 24→8→2→1, `S_4` 24→12→4→1, `C_2×A_4` 24→4→1, with every quotient checked abelian. CAUTION: quotient orders are reported as orders, not names — an abelian group of order 4 may be `C_4` or `C_2×C_2` and the computation does not decide which. The consequence in (d) is a **warning as much as a motivation**: a conjecture that settles all solvable groups is not a small step, which is the argument for attacking rung (e) rather than (d) This row is `COMPUTED`. | hora-algebra | 2026-07-25 |

| SFA-EGGAN-01 | Let an SF-automaton be a finite automaton whose transitions are labelled by star-free languages, and let its loop complexity be the cycle rank of the underlying digraph (Lombardy–Sakarovitch Def. 7.4). Then for all vertices p,q the path language from p to q is denoted by a generalized regular expression of syntactic star height at most the loop complexity; hence gsh(L) ≤ r_SF(L) := min over SF-automata accepting L of the loop complexity. In particular loop complexity ≤ 1 implies gsh ≤ 1, and r_SF(L) = 0 iff L is star-free. | PROVED | full induction in `notes/sf_labeled_automata.md` §2 (three cases: no edges; several strongly connected components, where the acyclic condensation adds no star; strongly connected, where the rank-witness vertex contributes the single new star); implemented in `tools/sf_automaton.py` (`all_pairs`, `SFAutomaton.to_expression`), which re-checks the height-vs-rank inequality on every call and raises otherwise, so the theorem is a runtime-enforced invariant; exercised by `tests/test_sf_automaton.py` and by every step of `scripts/research/sf_automaton_calibration.py`; NOT a lower bound — the converse half of Eggan's theorem does not relativize because complementation has no loop-complexity-controlled automaton construction (`notes/sf_labeled_automata.md` §2.4); PRIORITY (prior-art search done 2026-07-25, `M-SFA-PRIOR-001`): the statement is NOT new. The label-agnostic index bookkeeping is Sakarovitch, "Automata and rational expressions" (arXiv:1502.03573, long version of AutoMathA Handbook Ch. 2) §3.6, which defines the index of a *generalised automaton* — transitions "labelled not by letters only but by rational expressions in general" — as eqs (3.24)–(3.26) from `i(e) = h[label]`, with Property 3.12 (`lc = min over orders of I_ω`) and Proposition 3.13 (`I_ω(A) = h[B_ω(A)]`, credited to Lombardy–Sakarovitch, "On the star height of rational languages", 2003), yielding Theorem 3.11 (Eggan 1963). That source's `h` is the RESTRICTED star height and complement is outside its framework, and it does give an EQUALITY over elimination orders (`min_ω h[B_ω(𝒜)] = lc(𝒜)`). The generalized reading is published too: Pin–Straubing–Thérien, "Some results on the generalized star-height problem", Inf. Comput. 101(2):219–250, 1992, Theorem 4.6 (p.229f) — for a star-free INJECTIVE substitution σ, `h(Lσ) ≤ h(L)` with `h` = generalized star height, by the same induction extending σ to expressions; their injectivity and `(Aσ)*` star-free hypotheses (their Prop. 4.4) are needed only because their expressions may contain complement and intersection, and drop out here where only `∪,·,*` are substituted into. Moreover `r_SF` is not a new quantity: it is Hashiguchi's RELATIVE star height (Inf. Comput. 78(2):124–169, 1988, Def. 2.6, pp.127–128) with the base ranging over star-free languages — `r_SF(L) = min over finite 𝒞 ⊆ SF of h_r(L,𝒞)`, since an SF-automaton has finitely many labels. NET: this row has NO new mathematical content; the repository claims the packaging (rank as a finite certificate object), the implementation, and the measurements only, and the row stays PROVED because the proof is checked here, not as a priority claim  This row is `PROVED`. | hora-algebra | 2026-07-25 |
| SFA-STAR-ONLY-01 | GH₁, the class of languages of generalized star height at most one, is the smallest class containing the star-free languages and the stars of star-free languages that is closed under union, intersection, complement and concatenation. Consequently GLOBAL-ONE holds if and only if GH₁ is closed under Kleene star. | PROVED | `notes/sf_labeled_automata.md` §3 (Propositions 3.1 and 3.2); both directions elementary — star height propagates as a maximum through the Boolean operations and concatenation and increases only at star, and the converse is Kleene's theorem; closure under quotients is `FULL-ALPH-RED-01` §3.5, not reproved here. The content is scope isolation, not difficulty: it shows the star is the unique missing closure property | hora-algebra | 2026-07-25 |
| SFA-CORE2-RANK-01 | Modulo CORE2-EQV-EXT-01, GLOBAL-ONE is equivalent to the statement that every SF-automaton of loop complexity 2 has generalized star height at most one — indeed to the single two-state shape with self-loops A and D and crossing edges B and C, whose path language from the initial vertex to itself is (A ∪ B D\* C)\* and whose cycle rank is exactly 2. Hence for every r ≥ 2 the statement "every rank-r SF-automaton has gsh ≤ 1" is equivalent to GLOBAL-ONE, and the rank hierarchy collapses to a single rank-2-to-rank-1 reduction question. | UNREVIEWED | `notes/sf_labeled_automata.md` §4; the automaton reading of the right-hand side and the cycle-rank computation are immediate and machine-checkable, but the equivalence itself is inherited from the external `CORE2-EQV-EXT-01`, which has not been independently reconstructed in this repository (`M-EXP-PR2-001`). Do not treat as a settled reformulation until that row is upgraded | hora-algebra | 2026-07-25 |
| SFA-L2-MEASURE-01 | The minimal DFA of the full Weis L2, read as a letter-labelled SF-automaton, has loop complexity exactly 2, so r_SF(L2) ≤ 2. After absorbing its two a-self-loops into the incoming edges via the star-free a\*, the four-diagonal walk graph of WEIS-L2-GSH-01 has loop complexity exactly 1, and all four atom families that that proof intersects — the eight anchor-walk languages W_d(x) for d in {D₂, D₃} and both letter parities — are languages of rank-1 SF-automata. Hence L2 lies in the Boolean closure of rank-1 SF-automaton languages. | COMPUTED | `scripts/research/sf_automaton_calibration.py` (exact, no sampling, no length cutoffs, ~0.04 s): ground truth recompiled from the PRINTED regex and asserted equal to the six-state walk automaton; cycle ranks by exhaustive vertex-deletion recursion; the eliminated first-return and escape languages asserted star-free and proved language-equal by DFA equivalence to the expressions printed in `notes/weis_l2_full_height_one.md` §3 for both anchors, by an independently written eliminator; each atom proved language-equal to its exact walk DFA; two certificates emitted and re-verified by the independent `tools/regex_cert.py` (`CERT-01`) on every `./scripts/check.sh`; run manifest `data/experiments/sf_automaton_calibration.md`. CAUTION: no rank-1 SF-automaton for L2 itself is known, but labels range over all star-free languages, so that is a search result and NEVER a lower bound; r_SF(L2) = 2 is not claimed | hora-algebra | 2026-07-25 |
| TOPOS-BASE-01 | Over a fixed finite alphabet, the class of languages of generalized star height at most one is the star-free closure of the base local variety obtained by closing the stars of star-free languages under Boolean operations and left and right quotients. | UNREVIEWED | asserted in `docs/Cenceptual_understanding.md` §0 and §1.1, deleted 2026-07-27 and restorable by `git show 230cde4:docs/Cenceptual_understanding.md`; that document calls it established by a syntax and closure argument but explicitly subject to the exact adopted definitions of the two classes, and it is not reconstructed here. Start any audit from `SFA-STAR-ONLY-01`, which is PROVED in this repository and characterises the height-one class as the smallest class containing the star-free languages and their stars closed under the Boolean operations and concatenation; the gap to check is quotients versus concatenation, so this row may be mostly discharged already. Condition (ii-a) of the blocker on `N-TOPOS-001` | hora-algebra | 2026-07-27 |
| TOPOS-ORB-01 | For every surjective recognizing morphism from the free monoid onto a finite monoid M and every idempotent e in M, the generalized star-height problem over a fixed finite alphabet is equivalent to the assertion that the Place–Zeitoun orbit monoid of e inside eMe is aperiodic. | UNREVIEWED | asserted in `docs/Cenceptual_understanding.md` §0 and §1.2, deleted 2026-07-27 and restorable by `git show 230cde4:docs/Cenceptual_understanding.md`; attributed there to Place–Zeitoun with NO page or theorem number, and NOT independently reconstructed in this repository. Condition (ii-b) of the blocker on `N-TOPOS-001`. Do not cite this row as a reformulation of the problem until it is upgraded | hora-algebra | 2026-07-27 |
| TOPOS-KAR-01 | The local monoid eMe is the endomorphism monoid of the object e in the Karoubi envelope of M, and a finite monoid is aperiodic if and only if the maximal subgroupoid of its Karoubi envelope is discrete. | UNREVIEWED | asserted in `docs/Cenceptual_understanding.md` §2.1 and §2.2, deleted 2026-07-27 and restorable by `git show 230cde4:docs/Cenceptual_understanding.md`; that document calls both standard and the first convention-sensitive. The literature is finite semigroup theory and category theory, NOT Place–Zeitoun, so auditing `TOPOS-ORB-01` does nothing for this row and vice versa. Condition (ii-c) of the blocker on `N-TOPOS-001` | hora-algebra | 2026-07-27 |
| TOPOS-ISO-01 | The hyperconnected hull of the Navir isotropy subtopos of a fixed finite alphabet is equivalent to the topos quotient corresponding to the base local variety. Even if true this does NOT settle generalized star height: it would supply the base quotient, after which a relative-anisotropy theorem is still required. | SPECULATIVE | recorded as the central conjecture in `docs/Cenceptual_understanding.md` §4.2 and §12, deleted 2026-07-27 and restorable by `git show 230cde4:docs/Cenceptual_understanding.md`. It is not yet a precise conjecture: the isotropy subtopos has no canonical definition until the Kobin construction is extracted, which is exactly what `N-TOPOS-001` asks for. No code, no Lean, no independent audit; the route is unstarted | hora-algebra | 2026-07-27 |
| COVER-LE59-01 | Among the 299 finite groups of order at most 59, exactly 36 are not covered by any of: abelian (`PST-GRP-01`), nilpotent of class at most two (`PST-GRP-02`), a split extension of an abelian group by an elementary abelian 2-group (`PST-GRP-03`), dicyclic (`DICM-EMB-01`), or the fixpoint of the subdirect reduction (`SUBDIRECT-RED-01` with `L-TRANS-001`). Exactly 24 of the 36 are monolithic and therefore require a direct attack. | UNREVIEWED | GENERATED BY A PROGRAM AND NOT RE-DERIVED IN THIS REPOSITORY, which is what holds the row here rather than at `COMPUTED`: every criterion is searched inside GAP and nothing here re-decides it, so by the rule that silence is never evidence the ceiling is `UNREVIEWED` until `M-COVER59-001` lands. `scripts/gap/coverage_le59.g` decides every criterion on every enumerated group by exhaustive search over normal subgroups and complements — no sampling and no length bound; certificate `data/experiments/coverage_le59.tsv`, manifest `data/experiments/small_group_coverage_le59.md`, derivation `notes/small_group_coverage_le59.md`. Re-checked without GAP by `tests/test_coverage_le59.py`, which recomputes the abelian counts from the integer partition function and rejects seven deliberately corrupted tables. POSITIVE CONTROL: restricted to order at most 31 the unresolved set is exactly the six groups of `FRONTIER-ORD20-01`, obtained independently and without GAP by `scripts/research/small_group_pst_coverage.py`. EXTERNAL INPUT, CITED not computed: completeness of the enumeration is GAP's SmallGroups library. NOT a lower bound — "unresolved" means untouched by these criteria, never height at least two (research rule 1), and since C3 and C4 are tested in a narrower form than the theorems they come from, 36 is an upper bound on the true count | hora-algebra | 2026-07-28 |
| FAMILY-PHASE-01 | The 24 monolithic groups of `COVER-LE59-01` partition by phase group — the cyclic quotient of a split extension of a maximal abelian normal subgroup — into 7 with a phase group of prime order, 7 with one of composite order, and 10 admitting no such decomposition, of which 4 are 2-groups of order 32 that are nilpotent of class exactly three. | UNREVIEWED | inherits the ceiling of `COVER-LE59-01`, which is not re-derived in this repository; same program and certificate as `COVER-LE59-01`; the partition is read off the `phase` column, and the class-3 verdict from GAP's nilpotency class. Tables in `notes/small_group_coverage_le59.md` §4. This row is the partition only, a finite computation; the prediction attached to it is the separate and much weaker `FAMILY-A-PRED-01` | hora-algebra | 2026-07-28 |
| FAMILY-A-PRED-01 | The multi-mover mechanism of `RESULTS.md` §5.5 closes every group of Family A of `FAMILY-PHASE-01`, that is every split extension of an abelian group by a cyclic group of prime order occurring in the range. | CONJECTURAL | motivated, not established. Supporting: `F20-FULL-OBS-01` (`COMPUTED`) proves the mechanism fails on `F_20` and localizes the failure to the phase group `Z/4` being composite, and `C7C3-FULL-01` then passed on the prime phase group `Z/3` as predicted. Against: `C7C3-FULL-01` is `EMPIRICAL` and `A4-FULL-01` is `EMPIRICAL`, so on this branch BOTH instances of the family that have been run are samples, and a conjecture resting on two sampled instances is a lead. Pending and not counted here: the unmerged branch `codex/order12-height-one-lean` raises `A4-FULL-01` to `PROVED`; if that is accepted this row gains one genuinely non-sampled instance, which is a different and much better position, but it is not the position today. Three of the five untested — `C_13 : C_3`, `C_11 : C_5`, `C_19 : C_3` — were run on 2026-07-28 and all three passed (`C13C3-FULL-01`, `C11C5-FULL-01`, `C19C3-FULL-01`), so this row now rests on five instances rather than two. All five are still `EMPIRICAL`, so the position is better and not different in kind. `(C_4 x C_4) : C_3` and `(C_2)^3 : C_7` remain unrun: their kernels are not cyclic, so the parameterization that produced the three does not reach them. A single failure still refutes this row | hora-algebra | 2026-07-28 |

| C13C3-FULL-01 | For the full 39-element alphabet of `C_13⋊C_3` (order 39, a Family A member of `FAMILY-PHASE-01`), the multi-mover mechanism of `RESULTS.md` §5.5 transposes without modification: all 1002 candidate cut patterns are aperiodic, each of the 2 mover classes gives a GF(13) system of full rank 6/6 so that `β` is determined, and the certified features reconstruct membership in the identity fibre correctly on every word tested. | EMPIRICAL | `scripts/research/metacyclic_full_alphabet.py --target C13C3` at `(p,q,r) = (13,3,3)`, manifest `data/experiments/metacyclic_prime_phase.md`, derivation `notes/metacyclic_prime_phase.md`. **Why EMPIRICAL and not COMPUTED — updated 2026-07-28, and the reason is now much narrower:** section 2 enumerates each candidate's transition monoid completely, and section 7 now DECIDES the reconstruction too, by BFS over the product of the certified-feature machine with the group element, agreeing at every reachable state and hence on every word, with every solved coefficient mutated and every mutation rejected. What is still sampled is only section 7(c), the comparison of that machine with section 5's own `prefix_beta_from_certified_features`, which runs on bounded and sampled words — so the chain from section 5's text to the fibre keeps one sampled link and the ceiling rule caps the row there (`N-METACYC-TRANS-001`). Section 5 alone would have reconstructed the identity fibre exhaustively only to length 3 (60880 words) and then on a fixed-seed sample of 20000 words, and a row is capped by its weakest step. This is the same cap that the 2026-07-25 completeness audit applied to `C7C3-FULL-01`, and the same one whose absence caused the retraction of `A4-FULL-01`. CONTROLS, all fired in the run this row records: the 26 excluded same-letter pure-power patterns are all NOT aperiodic (period 3), so the judge can answer no; 168 single-coefficient mutations of the solved combinations each broke the reconstruction with 0 survivors; the membership base rate is 1562/60880 (0.026), so section 5 is not passing on a near-constant predicate; a pair of words separated by no letter count is exhibited at length 4. POSITIVE CONTROL for the program itself: at `(7,3)` it reproduces every result line of `scripts/research/c7c3_full_alphabet.py`. NEGATIVE CONTROL: at `F_20 = (5,4)`, where `q` is composite, it certifies nothing and exits 1, reproducing `F20-FULL-OBS-01`. NOT a proof of `HeightOneForGroup C_13⋊C_3` — no height-one regular expression is built or compiled and no language equivalence is decided, so this group does NOT leave the unresolved list of `COVER-LE59-01`. Wall clock 63.4 s | hora-algebra | 2026-07-28 |
| C11C5-FULL-01 | For the full 55-element alphabet of `C_11⋊C_5` (order 55, a Family A member of `FAMILY-PHASE-01`), the multi-mover mechanism of `RESULTS.md` §5.5 transposes without modification: all 2388 candidate cut patterns are aperiodic, each of the 4 mover classes gives a GF(11) system of full rank 10/10 so that `β` is determined, and the certified features reconstruct membership in the identity fibre correctly on every word tested. | EMPIRICAL | `scripts/research/metacyclic_full_alphabet.py --target C11C5` at `(p,q,r) = (11,5,3)`, manifest `data/experiments/metacyclic_prime_phase.md`, derivation `notes/metacyclic_prime_phase.md`. **Why EMPIRICAL and not COMPUTED — updated 2026-07-28, and the reason is now much narrower:** section 2 enumerates each candidate's transition monoid completely, and section 7 now DECIDES the reconstruction too, by BFS over the product of the certified-feature machine with the group element, agreeing at every reachable state and hence on every word, with every solved coefficient mutated and every mutation rejected. What is still sampled is only section 7(c), the comparison of that machine with section 5's own `prefix_beta_from_certified_features`, which runs on bounded and sampled words — so the chain from section 5's text to the fibre keeps one sampled link and the ceiling rule caps the row there (`N-METACYC-TRANS-001`). Section 5 alone would have reconstructed the identity fibre exhaustively only to length 3 (169456 words) and then on a fixed-seed sample of 20000 words, and a row is capped by its weakest step. This is the same cap that the 2026-07-25 completeness audit applied to `C7C3-FULL-01`, and the same one whose absence caused the retraction of `A4-FULL-01`. CONTROLS, all fired in the run this row records: the 44 excluded same-letter pure-power patterns are all NOT aperiodic (period 5), so the judge can answer no; 440 single-coefficient mutations of the solved combinations each broke the reconstruction with 0 survivors; the membership base rate is 3082/169456 (0.018), so section 5 is not passing on a near-constant predicate; a pair of words separated by no letter count is exhibited at length 4. POSITIVE CONTROL for the program itself: at `(7,3)` it reproduces every result line of `scripts/research/c7c3_full_alphabet.py`. NEGATIVE CONTROL: at `F_20 = (5,4)`, where `q` is composite, it certifies nothing and exits 1, reproducing `F20-FULL-OBS-01`. NOT a proof of `HeightOneForGroup C_11⋊C_5` — no height-one regular expression is built or compiled and no language equivalence is decided, so this group does NOT leave the unresolved list of `COVER-LE59-01`. Wall clock 1485.1 s | hora-algebra | 2026-07-28 |
| C19C3-FULL-01 | For the full 57-element alphabet of `C_19⋊C_3` (order 57, a Family A member of `FAMILY-PHASE-01`), the multi-mover mechanism of `RESULTS.md` §5.5 transposes without modification: all 2148 candidate cut patterns are aperiodic, each of the 2 mover classes gives a GF(19) system of full rank 6/6 so that `β` is determined, and the certified features reconstruct membership in the identity fibre correctly on every word tested. | EMPIRICAL | `scripts/research/metacyclic_full_alphabet.py --target C19C3` at `(p,q,r) = (19,3,7)`, manifest `data/experiments/metacyclic_prime_phase.md`, derivation `notes/metacyclic_prime_phase.md`. **Why EMPIRICAL and not COMPUTED — updated 2026-07-28, and the reason is now much narrower:** section 2 enumerates each candidate's transition monoid completely, and section 7 now DECIDES the reconstruction too, by BFS over the product of the certified-feature machine with the group element, agreeing at every reachable state and hence on every word, with every solved coefficient mutated and every mutation rejected. What is still sampled is only section 7(c), the comparison of that machine with section 5's own `prefix_beta_from_certified_features`, which runs on bounded and sampled words — so the chain from section 5's text to the fibre keeps one sampled link and the ceiling rule caps the row there (`N-METACYC-TRANS-001`). Section 5 alone would have reconstructed the identity fibre exhaustively only to length 3 (188500 words) and then on a fixed-seed sample of 20000 words, and a row is capped by its weakest step. This is the same cap that the 2026-07-25 completeness audit applied to `C7C3-FULL-01`, and the same one whose absence caused the retraction of `A4-FULL-01`. CONTROLS, all fired in the run this row records: the 38 excluded same-letter pure-power patterns are all NOT aperiodic (period 3), so the judge can answer no; 252 single-coefficient mutations of the solved combinations each broke the reconstruction with 0 survivors; the membership base rate is 3308/188500 (0.018), so section 5 is not passing on a near-constant predicate; a pair of words separated by no letter count is exhibited at length 4. POSITIVE CONTROL for the program itself: at `(7,3)` it reproduces every result line of `scripts/research/c7c3_full_alphabet.py`. NEGATIVE CONTROL: at `F_20 = (5,4)`, where `q` is composite, it certifies nothing and exits 1, reproducing `F20-FULL-OBS-01`. NOT a proof of `HeightOneForGroup C_19⋊C_3` — no height-one regular expression is built or compiled and no language equivalence is decided, so this group does NOT leave the unresolved list of `COVER-LE59-01`. Wall clock 379.8 s | hora-algebra | 2026-07-28 |
| C7C3-SUB9-01 | Over the 9-letter sub-alphabet of `C_7⋊C_3` consisting of the seven kernel elements together with the two non-trivial elements of `C_3` — a generating set — the identity fibre is denoted by a generalized regular expression of syntactic star height at most one. | UNREVIEWED | `scripts/research/c7c3_expression_equivalence.py` §4: twenty-one cut features assembled over GF(7), star height MEASURED by machine as 1 rather than counted by eye, compiled by `tools/regex_cert.py` to 21 states, and proved equal to the identity fibre over those letters by product reachability — a complete finite decision, no sampling and no length bound. Two coefficient-shift controls are detected. NOT sampled, so `EMPIRICAL` would be the wrong label; `COMPUTED` is unavailable only because a new `COMPUTED` row needs a `tools/verdict.py` verdict and `data/verdicts/PENDING.md` is closed by its own terms, which is what `N-C7C3-EXPR-001` migrates. **This does NOT give `HeightOneForGroup (C_7⋊C_3)`**: `FULL-ALPH-RED-01` requires the FULL 21-letter alphabet, and `F20-SUB10-OBS-01` already established that a sub-alphabet success need not lift. Derivation `notes/c7c3_expression_obstruction.md` | hora-algebra | 2026-07-28 |
| C7C3-EXPR-OBS-01 | Over the full 21-letter alphabet of `C_7⋊C_3`, no Boolean combination of the letter counts, the 441 adjacent-pair counts, the three base cuts and the 21 non-mover phase counts denotes the identity fibre, at any star height. The phase-resolved pair cut — arrivals at a given phase whose preceding letter is a given letter and whose own letter is a mover, counted mod 7 — is exactly the missing family. | UNREVIEWED | `scripts/research/c7c3_expression_equivalence.py` §5, by rank over GF(7) on the cycle space of the phase automaton and not by search: 1323 cycle generators, rank 437 for the listed features and 438 once `beta'` is adjoined, so `beta'` misses the span by exactly one dimension; adjoining the phase-resolved pair counts gives rank 1021 with `beta'` inside. Minimal witness at length 5 with none at length ≤ 4 (exhaustive): `w1 = (2,5)(2,5)(2,0)(2,5)(1,3)` evaluates to `(1,1)` and `w2 = (2,5)(2,0)(2,5)(2,5)(1,3)` to `(1,0)`, the two differing by ONE ADJACENT TRANSPOSITION and therefore having the same letter multiset, which is why every listed feature is blind to the difference — verified independently from the group law while reviewing. The missing block language IS star-free (block automaton 12 raw states, transition monoid enumerated completely at 90 elements, aperiodic for both mover classes), so a height-one expression EXISTS by Schützenberger; it was not constructed. NOT a lower bound and NOT a claim that no height-one expression exists — research rule 1. The §2 construction did not reach it at any window tried: the locally testable approximation differs at windows 1..7 with the witness growing alongside. **That is seven refuted windows, NOT a proof that no bounded window works** — an earlier version of this row said "provably cannot reach it", which was an extrapolation from a finite sample of exactly the kind this ledger exists to refuse. The question is decidable: locally testable is equivalent to the syntactic semigroup lying in **LT** (`eSe` idempotent and commutative for every idempotent `e`), applied to the syntactic semigroup of the ALIVE LANGUAGE OF A PAIR-CUT BLOCK over its six flagged mover letters (mover class in {m1, m2, g}, times whether a non-mover run sat immediately before), computed from that language's MINIMAL automaton — NOT the 90-element object this script already prints: that is the transition monoid of the pair-cut block DFA over the 21-letter alphabet, it is built from 12 states the code itself labels *raw*, and a transition monoid of an unminimised automaton need not be the syntactic monoid. Different language, different alphabet, wrong kind of monoid — three ways to decide the wrong thing, and an earlier version of this row pointed at it. Unrun (`N-C7C3-EXPR-001`). A 7.2e5-language breadth-first search also failed, and a failed search proves nothing and is recorded as such. `N-C7C3-EXPR-001` | hora-algebra | 2026-07-28 |
## Update template

```text
| ID | Fully quantified claim. | STATUS | exact source, theorem, code, or counterexample | owner | YYYY-MM-DD |
```

For `COMPUTED` claims also record: commit hash, command, input hash, output hash, and resource bound in `data/experiments/`.
