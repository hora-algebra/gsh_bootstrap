# Proof Obligations and Formalization Queue

An entry is closed only when its acceptance test passes and the corresponding claim-ledger row is updated. A `sorry` may exist during scaffolding, but it must be named here.

## Status vocabulary

- `OPEN`: well specified, unassigned or incomplete.
- `ACTIVE`: assigned and under work.
- `BLOCKED`: exact obstruction recorded.
- `REVIEW`: implementation/proof exists; independent review pending.
- `CLOSED`: acceptance test and review completed.

## Lean foundation

| ID | Obligation | Depends on | Acceptance test | Status |
|---|---|---|---|---|
| L-WORD-001 | Compile basic word/language operations and prove elementary membership lemmas. (2026-07-23: definitions moved unchanged from `GSH/Language/Basic.lean` into `GSH/Challenges/GeneralizedStarHeight.lean` §1 during the single-file consolidation.) | none | `lake env lean GSH/Challenges/GeneralizedStarHeight.lean` | OPEN |
| L-REGEX-001 | Compile generalized-expression syntax, semantics, and syntactic height. (2026-07-23: definitions moved unchanged from `GSH/Regex/Generalized.lean` into `GSH/Challenges/GeneralizedStarHeight.lean` §2–§4 during the single-file consolidation.) | L-WORD-001 | `lake env lean GSH/Challenges/GeneralizedStarHeight.lean` | OPEN |
| L-REGEX-002 | Prove semantics of n-ary/sugar constructors used by certificates. | L-REGEX-001 | Lean file plus unit examples | OPEN |
| L-DFA-001 | Compile DFA definition and prove `run_append`. (2026-07-23: moved unchanged from `GSH/Automata/DFA.lean` into `GSH/Recognition.lean` §1.) | L-WORD-001 | `lake env lean GSH/Recognition.lean` | OPEN |
| L-DFA-002 | Define reachable/minimal or equivalence interface without committing to a full minimization library. | L-DFA-001 | theorem signatures reviewed by language lead | OPEN |
| L-REC-001 | Compile monoid recognition structure and inverse-image language. (2026-07-23: moved unchanged from `GSH/Monoid/Recognition.lean` into `GSH/Recognition.lean` §2; the syntactic-congruence and aperiodicity interfaces from `GSH/Monoid/Syntactic.lean` / `GSH/StarFree/Aperiodic.lean` are §3–§4 of the same file, and `HeightOneForMonoid` / `HeightOneForGroup` from `GSH/GroupLanguages/Basic.lean` are §5.) | L-WORD-001 | `lake env lean GSH/Recognition.lean` | OPEN |
| L-SYN-001 | Prove syntactic equivalence is a two-sided congruence. **Closed 2026-07-27.** `GSH.SyntacticEq.append_left`, `.append_right`, and `.append_congr` prove compatibility after the equivalence laws. | L-WORD-001 | `GSH/Recognition.lean`; reflexive/symmetric/transitive and append-compatibility compile; independent adversarial review | CLOSED |
| L-SYN-002 | Construct the syntactic quotient monoid or provide a clean interface to mathlib quotient monoids. **Closed 2026-07-27.** The unproved placeholder deleted on 2026-07-25 was not restored. Instead `GSH.syntacticCon` packages the proved contextual relation as mathlib's `Con`; `Con.Quotient` supplies the monoid structure and `GSH.syntacticMorphism` is the canonical surjective monoid homomorphism, with kernel characterized by `GSH.syntacticMorphism_eq_iff`. | L-SYN-001 | `GSHTest/SyntacticMonoid.lean`; `lake build GSH GSHTest`; namespace-wide axiom audit; independent adversarial review | CLOSED |
| L-SF-001 | Define aperiodicity and a theorem interface for Schützenberger. **2026-07-25: the placeholder `schutzenberger_interface` was deleted from `GSH/Recognition.lean`** for the same reason (it was a `sorry`, and it depended on L-SYN-002). `IsAperiodicMonoid` remains. An unproved `theorem` is not a formalization; the statement is recorded here instead. | L-SYN-002 | no false claim that interface is a proof | OPEN |
| L-CERT-001 | Formalize certificate AST and checker soundness boundary. (2026-07-23: `checker_sound` moved from `GSH/Certificates/RegexCertificate.lean` into `GSH/Certificates.lean`. Recorded here because the condensed first-build log below asserts that every renamed path's destination is in these rows, and until 2026-07-26 this one was only in a source comment.) | L-REGEX-001, L-DFA-001 | explicit theorem: checker acceptance implies language equality and height bound | OPEN |
| L-GRP-001 | Compile `HeightOneForGroup` with exact alphabet finiteness and morphism quantifiers. (2026-07-25: the statement is now *used*, not merely compiled — `GSH.heightOneUpTo_five` proves it for every group of order ≤ 5, so the quantifiers have been exercised against a real proof rather than only inspected.) | L-REGEX-001, L-REC-001 | group lead approves statement; Lean compiles | REVIEW |
| L-CNT-001 | Prove that `{w \| count a w % m = r}` has generalized star height at most one, with the letter-avoiding factor built by complement so that the expression carries exactly one star. | L-REGEX-001 | `GSH/Height/Counting.lean`: `GSH.Counting.hasHeightAtMost_count`, no `sorry`; ledger row LEAN-CNT-01; axiom audit passes | CLOSED |
| L-TRANS-001 | Prove that `HeightOneForGroup` descends along injective and surjective group morphisms (hence to divisors). | L-GRP-001 | `GSH/Transfer.lean`, no `sorry`; ledger row LEAN-TRANSFER-01 | CLOSED |
| L-ABEL-001 | Prove `HeightOneForGroup G` for every finite commutative `G`. | L-CNT-001, L-GRP-001 | `GSH/Groups/Abelian.lean`: `GSH.heightOne_of_commGroup`, no `sorry`; ledger row LEAN-ABELIAN-01; instantiated in `GSHTest/Smoke.lean` at `ZMod 5` and at the Klein four-group | CLOSED |
| L-ORD5-001 | Prove `HeightOneUpTo 5`: every group of order at most five has the height-one property. | L-ABEL-001 | `GSH/Groups/SmallOrder.lean`: `GSH.heightOneUpTo_five`, no `sorry`; ledger row LEAN-ORD5-01; negative control recorded: the same proof is rejected at order 6 | CLOSED |
| L-S3-KERNEL-001 | Isolate the concrete input to the index-two route for `S₃`: exhibit `A₃ ≤ Equiv.Perm (Fin 3)` with index two and prove `HeightOneForGroup A₃`. This is deliberately weaker than the missing lift to `S₃`. **Closed 2026-08-01.** | L-ABEL-001 | `GSH/Groups/SmallOrder.lean`: `GSH.heightOne_A3`, `GSH.s3_has_heightOne_index_two_subgroup`; compile-time acceptance in `GSHTest/Smoke.lean`; ledger row LEAN-S3-KERNEL-01 | CLOSED |
| L-S3-SPLIT-001 | Formalize the split-extension presentation of `S₃` and show that the missing height-one theorem for its concrete `A₃ ⋊ C₂` presentation implies the exact target `HeightOneForGroup (Equiv.Perm (Fin 3))`. **Closed 2026-08-01.** This is a group-theoretic reduction, not the missing language theorem. | L-S3-KERNEL-001, L-TRANS-001 | `GSH/Groups/S3.lean`: `GSH.s3SemidirectEquiv`, `GSH.heightOne_S3_of_semidirect`; compile-time acceptance in `GSHTest/S3.lean`; ledger row LEAN-S3-SPLIT-01 | CLOSED |
| L-ARROW-COUNT-001 | Formalize deterministic runs and labelled-arrow counts on words, including exact decomposition across concatenation. **Closed 2026-08-01.** This supplies only the word-level bookkeeping used by the PST two-state construction; it does not prove that arrow-count residue languages have height at most one. | L-WORD-001 | `GSH/Height/ArrowCounting.lean`: `GSH.ArrowCounting.runFrom_append`, `GSH.ArrowCounting.arrowCountFrom_append`; compile-time acceptance in `GSHTest/ArrowCounting.lean`; ledger row LEAN-ARROW-COUNT-01 | CLOSED |
| L-C2-RUN-001 | Specialize the deterministic run to `C₂ = ZMod 2`, prove each letter transition is an involution, and identify the reached state with the sum of letter phases. **Closed 2026-08-01.** This records one exact use of order two but does not construct expressions for arrow-count residue languages. | L-ARROW-COUNT-001 | `GSH/Height/ArrowCounting.lean`: `GSH.ArrowCounting.c2Step_twice`, `GSH.ArrowCounting.c2RunFrom_eq`; compile-time acceptance in `GSHTest/ArrowCounting.lean`; ledger row LEAN-C2-RUN-01 | CLOSED |
| L-C2-COUNT-PART-001 | Prove that the counts of a fixed input label leaving states `0` and `1` in the `C₂` run sum to the ordinary occurrence count. The earlier generic repair remains preserved in `notes/lean_c2_arrow_count_partition_blocked.md`; the exact `S₃` instance needed here was subsequently closed by explicit two-state case analysis. | L-ARROW-COUNT-001, L-C2-RUN-001 | `GSH/Height/S3ArrowAssembly.lean`: `GSH.s3ArrowCount_partition`; universal `S₃` acceptance in `GSHTest/S3ArrowAssembly.lean`. The generic-alphabet statement remains historical follow-up. | PARTIAL |
| L-PAIR-CODE-001 | Formalize the fixed pair-code arithmetic used immediately before the two-state height argument: for even Boolean words, source-zero all-toggle arrows are the first-letter pair counts, and ordinary `false` occurrences equal `2·#ff + #ft + #tf`. **Closed 2026-08-01.** This is the correct word-level part of PST Proposition 6.10 and makes no height claim. | L-ARROW-COUNT-001, L-C2-RUN-001 | `GSH/Height/PairCode.lean`; universal acceptance in `GSHTest/PairCode.lean`; ledger row LEAN-PAIR-CODE-01 | CLOSED |
| L-S3-FIBER-COORD-001 | Reconstruct the full-alphabet `S₃` identity fibre directly from its `ZMod 2` sign phase and the signed `ZMod 3` sum of phase-source labelled-arrow counts. **Closed 2026-08-01.** This removes all remaining group multiplication from the target but does not prove the arrow-residue languages have height one. | L-ARROW-COUNT-001, L-C2-RUN-001, L-RED-001 | `GSH/Height/S3FullFiber.lean`: `GSH.s3_prod_eq_one_iff_arrowCounts`, `GSH.fullIdentityFiber_s3_eq_arrowCoordinateFiber`; universal acceptance in `GSHTest/S3FullFiber.lean`; ledger row LEAN-S3-FIBER-COORD-01 | CLOSED |
| L-PST-TRANSFER-CORE-001 | Formalize the explicit height-one Boolean formula on the right-hand side of PST Lemma 6.1, assuming only that `L0`, `L1`, and `L0*` are star-free. **Closed 2026-08-01.** This closes the height calculation but deliberately leaves the prefix-code image equality to each consumer. | L-REGEX-001, L-FIN-BOOL-001 | `GSH/Height/TransferCore.lean`: `GSH.TransferCore.hasHeightAtMost_formula`; generic compile-time acceptance in `GSHTest/TransferCore.lean`; ledger row LEAN-PST-TRANSFER-CORE-01 | CLOSED |
| L-PST-TRANSFER-MOD-001 | Remove the fixed modulus three from the two combinatorial halves of the PST Transfer semantics. **Closed 2026-08-01 for the exact reusable helpers:** `mem_lengthSide_modulus_iff` gives the arbitrary-modulus length factorization, and `TokenMod.countSide_iff` gives the arbitrary-positive-modulus Boolean-token normalization. This does not yet lift the token grouping back to actual `L0/L1` codewords or prove `mem_formula_partition_iff`. | L-PST-TRANSFER-CORE-001 | `GSH/Height/TransferModulusSemantics.lean`, `GSH/Height/TransferSemantics.lean`; exact modulus-eight and modulus-four consumers in `GSHTest/TransferModulusSemantics.lean` and `GSHTest/TransferSemantics.lean`; ledger row LEAN-PST-TRANSFER-MOD-01 | CLOSED |
| L-C2-SELF-CODE-001 | Formalize the `C₂` self-loop return blocks `P = D ∪ C B* C`; prove that every zero-returning word admits a unique `{[label]} ∪ P` factorization, and that any supplied factorization returns to state zero and converts the labelled-arrow count into the ordinary count of `[label]` code blocks. **Closed 2026-08-01.** | L-C2-RUN-001 | `GSH/Height/S3ArrowResidue.lean`: existence, prefix-code uniqueness, `codeBlockFactorization_unique`, run and count theorems; universal acceptance in `GSHTest/S3ArrowResidue.lean`; ledger row LEAN-C2-SELF-CODE-01 | CLOSED |
| L-C2-PAIR-BAL-001 | Prove the corrected height-one core for the two-state route: on even Boolean words, every residue class of `#ab − #ba` in the fixed pair factorization has generalized star height at most one. **Partial 2026-08-01:** the exact stronger-in-context facts needed by `S₃` are closed: every distinct ordered phase-one pair residue has height one, and their finite signed balance has been consumed by the moving-arrow theorem. The separately named generic Boolean theorem remains unproved and is no longer on the `S₃` critical path. | L-PAIR-CODE-001, L-CNT-001, L-FIN-BOOL-001, M-PST-003 | `GSH/Height/S3FlipPairTransfer.lean`, `S3FlipPairQuotients.lean`, `S3FlipArrowAssembly.lean`; generic `GSH.hasHeightAtMost_pairBalance` remains absent | PARTIAL |
| L-C2-ARROW-ID-001 | Prove height at most one for residues of a source-labelled arrow when the chosen letter fixes both `C₂` states. **Closed 2026-08-01 for the exact `S₃` need:** every phase-zero label, both sources, every residue modulo three, and all final phases. The required modulus is three; no unnecessary arbitrary-modulus claim is made. | L-C2-RUN-001, L-CNT-001, L-PST-TRANSFER-CORE-001, L-C2-SELF-CODE-001 | `GSH/Height/S3SelfLoopTransfer.lean`: `GSH.hasHeightAtMost_s3SelfLoopArrowResidue`; exact consumers in `GSHTest/S3SelfLoopTransfer.lean`; supporting exact Transfer semantics in `TransferSemantics.lean`; ledger row LEAN-C2-ARROW-ID-01 | CLOSED |
| L-C2-ARROW-FLIP-001 | Prove height at most one for residues of a source-labelled arrow when the chosen letter flips the `C₂` state. **Closed 2026-08-01 for the exact `S₃` scope:** every phase-one label, both sources, every residue modulo three, and all final phases. | L-C2-RUN-001, L-CNT-001, L-FIN-BOOL-001, M-PST-003 | `GSH/Height/S3FlipPairCode.lean`, `S3FlipPairHeight.lean`, `S3FlipPairTransfer.lean`, `S3FlipPairQuotients.lean`, `S3FlipArrowAssembly.lean`, `S3FlipSourceOne.lean`, `S3FlipArrowResidue.lean`; final theorem `GSH.hasHeightAtMost_s3FlipArrowResidue`; ledger row LEAN-C2-ARROW-FLIP-01 | CLOSED |
| L-C2-ARROW-RES-001 | Combine the self-loop and flipping-letter cases into the exact height-one theorem for every labelled source arrow of the `s3Phase` two-state run, including source-one and arbitrary final-state words. **Closed 2026-08-01.** | L-C2-ARROW-ID-001, L-C2-ARROW-FLIP-001 | `GSH/Height/S3FlipArrowResidue.lean`: `GSH.hasHeightAtMost_s3ArrowResidue`; universal compile-time acceptance in `GSHTest/S3FlipArrowResidue.lean`; ledger row LEAN-C2-ARROW-RES-01 | CLOSED |
| L-S3-FULL-001 | Prove `HasHeightAtMost (fullIdentityFiber (Equiv.Perm (Fin 3))) 1`. **Closed 2026-08-01** by finite Boolean reconstruction of phase zero and all signed arrow residues. | L-S3-FIBER-COORD-001, L-C2-ARROW-RES-001, L-CNT-001, L-FIN-BOOL-001 | `GSH/Height/S3PhaseAssembly.lean`, `S3FullFiberAssembly.lean`, `S3FlipArrowResidue.lean`: `GSH.heightOne_fullIdentityFiber_S3`; exact consumer in `GSHTest/S3FlipArrowResidue.lean` | CLOSED |
| L-S3-001 | Prove the first noncommutative exact target `GSH.heightOne_S3 : HeightOneForGroup (Equiv.Perm (Fin 3))`, retaining every finite alphabet, recognizing morphism, and accepting subset quantified by `HeightOneForGroup`. **Closed 2026-08-01.** | L-S3-FULL-001, L-RED-001 | `GSH/Height/S3FlipArrowResidue.lean`: `GSH.heightOne_S3`; exact tracked consumer in `GSHTest/S3FlipArrowResidue.lean`; namespace axiom audit and headline `#print axioms` in `GSHTest/Axioms.lean`; ledger row LEAN-S3-HEIGHT-01 | CLOSED |
| L-D4-FIBER-COORD-001 | Reconstruct the full-alphabet `D₄ = DihedralGroup 4` identity fibre from its `ZMod 2` constructor phase and exact suffix-weighted `ZMod 4` coordinate. **Closed 2026-08-01.** | L-RED-001 | `GSH/Height/D4FullFiber.lean`: `GSH.d4_prod_eq_one_iff_coordinates`, `GSH.fullIdentityFiber_d4_eq_coordinateFiber`; acceptance in `GSHTest/D4FullFiber.lean`; ledger row LEAN-D4-FIBER-COORD-01 | CLOSED |
| L-D4-ARROW-COORD-001 | Reconstruct the full-alphabet `D₄` identity fibre from the phase sum and the signed `ZMod 4` sum of prefix labelled-arrow counts. **Closed 2026-08-01.** This is the exact bridge to the existing two-state Transfer machinery; it does not prove any arrow-residue language has height one. | L-D4-FIBER-COORD-001, L-ARROW-COUNT-001 | `GSH/Height/D4ArrowCoordinates.lean`: `GSH.d4Beta_prod_eq_arrowCounts`, `GSH.d4_prod_eq_one_iff_arrowCounts`, `GSH.fullIdentityFiber_d4_eq_arrowCoordinateFiber`; acceptance in `GSHTest/D4ArrowCoordinates.lean`; ledger row LEAN-D4-ARROW-COORD-01 | CLOSED |
| L-D4-FLIP-ARITH-001 | Prove the exact arithmetic replacement for the refuted mod-four pair-balance reconstruction: for each phase-one label on a phase-zero-total word, the ordinary count plus signed nonoverlapping pair balance in `ZMod 8` equals twice the source-zero arrow count and determines that count modulo four. **Closed 2026-08-01.** This makes no height claim. | L-D4-ARROW-COORD-001, L-ARROW-COUNT-001 | `GSH/Height/D4ArrowArithmetic.lean`: `GSH.D4ArrowArithmetic.two_mul_d4Arrow_eq_count_add_pairBalance`, `d4ArrowCount_mod_four_iff_features_mod_eight`; acceptance in `GSHTest/D4ArrowArithmetic.lean`; ledger row LEAN-D4-FLIP-ARITH-01 | CLOSED |
| L-D4-FLIP-FORMULA-001 | Prove that the modulus-eight Transfer formula for a fixed distinct phase-one `D₄` pair has height at most one, including the star-free distinguished block-star hypothesis. **Closed 2026-08-01.** The distinguished `D₄` block is obtained as the exact inverse image of a proved `S₃` pair block; semantic equality with the actual pair-count residue remains separate. | L-D4-FLIP-ARITH-001, L-PST-TRANSFER-CORE-001 | `GSH/Height/D4FlipPairHeight.lean`: `GSH.D4FlipPairHeight.d4PairBlockStar_starFree`, `GSH.D4FlipPairHeight.hasHeightAtMost_d4PairFormula`; acceptance in `GSHTest/D4FlipPairHeight.lean`; ledger row LEAN-D4-FLIP-FORMULA-01 | CLOSED |
| L-D4-ARROW-RES-001 | Construct only the height-one residue features needed to assemble the `D₄` beta condition on the phase-zero fibre. For phase-one letters, L-D4-FLIP-ARITH-001 reduces source-zero arrows to ordinary counts and signed pair-balance cells modulo eight, while L-D4-FLIP-FORMULA-001 closes the height of the required Transfer formula. L-PST-TRANSFER-MOD-001 now supplies the shared arbitrary-modulus length semantics and abstract Boolean-token normalization. The immediate gap is to connect each `TokenMod.TailPiece` grouping to the actual count-side expression and lift it to codewords, yielding `TransferSemantics.mem_formula_partition_iff`, with exact consumers at modulus eight for flip pairs and modulus four for self-loop cells. Source-one is then ordinary count minus source-zero. Do not generalize to all sources and all final phases before the direct `d4BetaByArrows = 0` consumer passes. | L-D4-ARROW-COORD-001, L-D4-FLIP-ARITH-001, L-D4-FLIP-FORMULA-001, L-PST-TRANSFER-MOD-001, L-CNT-001, L-FIN-BOOL-001, M-PST-003 | generic actual-codeword partition semantics with exact modulus-eight and modulus-four acceptance tests, then height-one pair residues and a finite Boolean assembly consumer in `d4ArrowCoordinateFiber` | OPEN |
| L-D4-FULL-001 | Prove `HasHeightAtMost (fullIdentityFiber (DihedralGroup 4)) 1` by consuming the exact coordinate theorem and verified height-one residue features. | L-D4-ARROW-COORD-001, L-D4-ARROW-RES-001, L-RED-001 | sorry-free `GSH.heightOne_fullIdentityFiber_D4`; exact consumer example | OPEN |
| L-D4-001 | Prove `GSH.heightOne_D4 : HeightOneForGroup (DihedralGroup 4)` with the exact quantifiers over every finite alphabet, recognizing morphism, and accepting subset. | L-D4-FULL-001, L-RED-001 | exact compile-time example; namespace axiom audit; `./scripts/check.sh` | OPEN |
| L-ABC2-001 | Prove `HeightOneForGroup G` for `G = A ⋊ C_2` with `A` finite commutative. This is the **entire** non-commutative content of the ladder up to order 19: every group of order ≤ 19 other than `A_4` has a commutative subgroup of index ≤ 2. The first concrete instance `S₃ = C₃ ⋊ C₂` is now closed by `L-S3-001`; the reusable arbitrary-`A` theorem, and the other small-group instances, remain open. | L-ABEL-001, L-TRANS-001, L-ARROW-COUNT-001, L-C2-RUN-001, M-PST-003 | Lean theorem without `sorry`; remaining instances include `D_4`, `Q_8`, `D_5` | OPEN |
| L-KK-001 | Formalize the Krasner–Kaloujnine embedding `G ↪ H ≀ (G/H)` for `H ◁ G`, specialized to index 2, so that the non-split case (e.g. `Q_8` over `C_4`) is covered by L-ABC2-001. Mathlib has only `RegularWreathProduct` (base indexed by the group itself), so the coset-indexed version must be built. | L-TRANS-001 | injective `MonoidHom` constructed; `Q_8 ↪ C_4 ≀ C_2` checked | OPEN |
| L-ORD19-GRP-001 | Prove the group-theoretic step: every finite group of order ≤ 19 not isomorphic to `A_4` has a commutative subgroup of index ≤ 2. Needs the Burnside basis theorem for the 2-group cases (mathlib has `frattini` but **not** `Φ(G) = G²[G,G]`) and Sylow arguments for orders `2p`, 12, 18. Mathlib has no classification of groups of order ≤ 19. | none | Lean theorem without `sorry`; the `A_4` exclusion is used, not assumed away | OPEN |
| L-A4-001 | Prove `HeightOneForGroup A4`. **Mathematical finite gap repaired 2026-07-27; Lean formalization remains open.** `A4-FULL-FINITE-CORE-01` now decides the former sampled token-factorization, feature, GF(2)-reconstruction, and group-formula obligations on the complete finite state spaces, with controls. `notes/a4_full_alphabet_exact.md` §§3–7 records the human proof of the full-alphabet identity fibre using Schützenberger's 1965 Main Property. `GSH.heightOneForGroup_of_fullIdentityFiber` (L-RED-001) supplies the final quantifier-preserving reduction, `GSH.HasHeightAtMost.reverse` (L-REV-001) formalizes the reversal step, and `GSH.HasHeightAtMost.booleanCombination` (L-FIN-BOOL-001) supplies the finite Boolean assembly. The missing kernel artifact is therefore one of: (a) explicit star-free expressions for the 279 aperiodic token DFAs plus their exact Lean semantics, followed by a Lean assembly of §§4–7; or (b) a verified finite-automaton/aperiodicity certificate checker and a formal Schützenberger interface. Python `COMPUTED` evidence must not be imported as a Lean theorem, and `native_decide` remains forbidden. | L-CNT-001, L-GRP-001, L-RED-001, L-REV-001, L-FIN-BOOL-001, L-SF-001, L-CERT-001 | `GSH.heightOne_A4 : HeightOneForGroup A4` without `sorry`; axiom audit shows only `propext`, `Classical.choice`, `Quot.sound` | OPEN |
| L-ORD12-GRP-001 | Formalize the exact finite-group split needed at order at most 12: every such group is either commutative, isomorphic to `A4`, or is one of the noncommutative cases covered by the index-two/PST mechanism (including the non-split dicyclic case through an explicit divisor embedding). Do not assume a catalogue without a Lean theorem or a precise cited classification. | L-ABC2-001, L-KK-001 | theorem with explicit alternatives and exact `Nat.card G ≤ 12` quantifier; instantiate all five order-12 isomorphism types | OPEN |
| L-ORD12-001 | Prove the requested headline theorem `heightOneUpTo_twelve : HeightOneUpTo 12`. This must compose the abelian case, the order-12 group classification, the dicyclic/index-two mechanism, and `heightOne_A4`; changing the bound or weakening “recognized by” is forbidden. | L-ABEL-001, L-ABC2-001, L-ORD12-GRP-001, L-A4-001 | sorry-free Lean theorem; compile-time use; namespace-wide axiom audit shows only the three standard axioms; `./scripts/check.sh` passes | OPEN |
| L-A5-001 | Compile the `A5` abbreviation and expose existing simplicity instance/theorem. (2026-07-23: moved unchanged from `GSH/Groups/A5.lean` into `GSH/Groups.lean`, together with `GSH/Groups/SmallGroups.lean` and the milestone names from `GSH/Blueprint.lean`.) | L-GRP-001 | `lake env lean GSH/Groups.lean` | OPEN |
| L-RED-001 | Formalize Proposition 3.1 (full-alphabet reduction) via the two §3.5 lemmas: height-preserving Brzozowski derivative and literal-inverse-morphism substitution for generalized expressions. **Closed 2026-07-27.** `GSH/Height/FullAlphabet.lean` proves, without `sorry`, preservation under a non-erasing letter-to-letter inverse map and under one-letter left quotients, then proves `heightOneForGroup_of_fullIdentityFiber`. Its conclusion unfolds to every finite alphabet, every recognition morphism, and every accepting subset. Compile-time uses are in `GSHTest/FullAlphabet.lean`; the namespace-wide axiom sweep and the explicit headline audit cover the new theorem. | L-REGEX-001 | `lake env lean GSH/Height/FullAlphabet.lean`; `lake env lean GSHTest/FullAlphabet.lean`; `./scripts/check.sh`; no `sorry`, `axiom`, or `native_decide` | CLOSED |
| L-REV-001 | Formalize that reversal preserves denotation and generalized star height, as used by the `A4-FULL-01` reconstruction. **Closed 2026-07-27.** | L-REGEX-001 | `GSH/Height/Reversal.lean`; `GSHTest/Reversal.lean`; sorry-free structural proof of concat, power, star, complement, semantics, and height | CLOSED |
| L-FIN-BOOL-001 | Formalize finite Boolean assembly: any truth-table predicate in finitely many feature languages of height at most `n` again has height at most `n`. **Closed 2026-07-27.** | L-REGEX-001 | `GSH/Height/FiniteBoolean.lean`; `GSHTest/FiniteBoolean.lean`; sorry-free cell decomposition valid also for the empty feature family; independent adversarial review and namespace-wide axiom audit pass | CLOSED |
| L-GSH-CHALLENGE-001 | Prove the generalized star-height-one conjecture, stated in `GSH/Challenges/GeneralizedStarHeight.lean` and left open in `GSH/Conjecture.lean` (ledger row GLOBAL-ONE); until then, preserve the statement as an explicitly open challenge. **2026-07-25:** the unproved declaration moved to `GSH/Conjecture.lean`, a leaf nothing imports, so that the ladder's import closure — and not only its dependency closure — is free of it; the statement is unchanged. **Consolidation recorded 2026-07-23:** the complete definition chain (`Word`/`Language` + operations, `GRegex`, `denote`, `starHeight`, `HasHeightAtMost`, `IsRegular`, `HeightOneCollapse`, `GeneralizedHeightOneConjecture`) was moved **unchanged** into the challenge file, which now imports only mathlib and is readable top-to-bottom; the former locations `GSH/Language/Basic.lean`, `GSH/Regex/Generalized.lean` were deleted and all importers repointed; the conjecture definition moved out of `GSH/Blueprint.lean` (that file was removed the same day; the milestone names now live in `GSH/Groups.lean`). The theorem statement `generalized_star_height_conjecture : GeneralizedHeightOneConjecture` is textually and definitionally identical to before. (Same day, earlier — withdrawn approach, kept for the record: a from-scratch self-contained restatement plus a `sorry`-free Lean proof of its equivalence, at every universe, with `∀ (α : Type u) [Fintype α], GSH.HeightOneCollapse α` was drafted and compiled, then withdrawn at maintainer request to avoid duplicate definitions; neither artifact was committed. Withdrawal was a design decision, not a mathematical obstruction.) Expert approval still pending. | L-WORD-001, L-REGEX-001 (same file) | statement approved by a formal-language expert; `lake env lean GSH/Challenges/GeneralizedStarHeight.lean` compiles (passes 2026-07-23, full `lake build` 1451 jobs green; only registered `sorry` warnings) | OPEN |

| L-CORE2-EQV-001 | Define the scalar CORE2 statement `∀ A B C D ∈ SF, (A ∪ B D* C)* ∈ GH1` over each finite alphabet and formalize its equivalence with `GeneralizedHeightOneConjecture`, following the external elementary-expansion cut compiler. Keep the result as an equivalence, not a proof of either side. | L-GSH-CHALLENGE-001, L-REGEX-001 | theorem statements compile without duplicate language definitions; both implications have no `sorry`; exact quantifiers and the erasing-letter step are approved by a formal-language expert | OPEN |

## Published mathematics reproduction

| ID | Obligation | Acceptance artifact | Status |
|---|---|---|---|
| M-PST-001 | Extract exact closure-theorem hypotheses from Pin–Straubing–Thérien. **Partially done 2026-07-25**: the paper was retrieved (local copy `hora-priority-papers/sources/1-s2.0-089054019290063L-main.pdf`, outside this repo; not redistributed here). Extracted so far, with page numbers, in `notes/sf_labeled_automata.md` §2.1b — Prop. 4.4 (an injective σ is star-free iff every `aσ` is star-free and `(Aσ)*` is star-free), Thm. 4.6 (p.229f, `h(Lσ) ≤ h(L)` for star-free injective σ, `h` = generalized), and the side condition of Lemma 6.1 (Transfer Lemma: `L₀, L₁` star-free with `L₁*` star-free). Confirmed absent from the paper: loop complexity, cycle rank, Eggan. Remaining: the quotient / inverse-alphabetic-morphism closure results and the §6–§7 wreath-product machinery. | theorem statements with page/number and notation map | OPEN |
| M-SFA-DECIDE-001 | Turn the `L2` rank measurement from a search into a decision. Hashiguchi 1988 makes relative star height `h_r(·, 𝒞)` computable for each FIXED finite base `𝒞`, and Kirsten 2011 (MFCS, LNCS 6907) gives the relative *inclusion* variant in triple exponential space, attributing the inclusion variants to Hashiguchi 1991 (TCS 91:85–100, doi:10.1016/0304-3975(91)90269-8, retrieved and read 2026-07-25; Defs. 2.7–2.8 p.87 transcribed in `notes/sf_labeled_automata.md` §7). Since `r_SF(L) = min over finite 𝒞 ⊆ SF of h_r(L,𝒞)`, the question "is there a rank-1 SF-automaton for `L2` over *this specified* finite label set" is decidable rather than a search. Fix the base actually used by `WEIS_L2_DIAGONAL_GRAPH` (the letters plus `a*`) and decide `h_r(L2, 𝒞)` for it. **Three cautions, all recorded before any work starts.** (a) Still not a gsh lower bound (README research rule 1): the minimum over all of `SF` remains an infinite search, so a verdict of 2 is a statement about that base only. (b) Hashiguchi's algorithm is non-elementary and his Algorithm 6.4 constructs a base with word-length bound `g = 16 m₁m₂(m₁m₂+2)(r(ℳ₂)·m₁m₂(m₁m₂+2)+1)`; with `\|C₂×S₄\| = 48` this is astronomically out of reach, so a direct implementation is not the plan. The only elementary route is Kirsten's distance desert automata, whose complexity analysis is **unread** (only the 2-page Springer preview of Kirsten 2011 was obtained). (c) Do NOT substitute the inclusion variant as a cheap proxy: Hashiguchi 1991 Prop. 3.4(1) shows the interpolant can be arbitrarily simpler than both endpoints (`h(R₁)=j`, `h(R₂)=k`, `h(R₁,R₂)=i` for any `i ≤ min{j,k}`). | either a decided value of `h_r(L2, 𝒞)` for the stated finite `𝒞`, with the algorithm implemented and its soundness argued, or an explicit `BLOCKED` recording which step is not implementable at this scale — given caution (b) the expected outcome is `BLOCKED`, and recording *where* it blocks is the deliverable | OPEN |
| M-PST-002 | Reconstruct the commutative-group height-one expression in repository notation. **2026-07-25: reconstructed and formalized** (L-ABEL-001). The reconstruction does not follow PST: it decomposes the *alphabet*, not the group. `φ(w) = ∏_a (φ a)^(count a w)` by commutativity, each factor depends only on `count a w mod \|G\|` by Lagrange, so every `φ`-fibre is a finite union of finite intersections of counting languages; counting languages are height one because the letter-avoiding factor `(Σ∖a)*` is available at height **zero** by complement. No structure theorem, no generators. Independent audit of the reconstruction against PST 1992 is still wanted. | complete hand proof plus at least two checked finite examples | REVIEW |
| M-PST-003 | Reconstruct the `A ⋊ C_2` mechanism and identify every use of order two. The general PST class `A ⋊ E` with `E` elementary abelian 2 is not needed for `n ≤ 19`. **Concrete `S₃ = C₃ ⋊ C₂` instance closed in Lean 2026-08-01:** L-C2-ARROW-ID-001, L-C2-ARROW-FLIP-001, L-C2-ARROW-RES-001, and L-S3-001 are all closed by a repaired six-letter pair-code proof that avoids the false printed image equality in PST Proposition 6.10 (`PST-P610-IMAGE-01`, counterexample `aa`). Proposition 6.10 and Theorem 7.6 remain `CITED`; the arbitrary finite commutative `A` reconstruction and its other small-group instances remain open. | general dependency graph and arbitrary-`A` proof extending the checked `S₃` construction without importing the refuted equality | OPEN |
| M-BR-001 | Encode one exact-count and one modular-count expression from Bourne–Ruškuc. | JSON certificates accepted by checker | OPEN |
| M-C3-FAIL-001 | Produce the smallest explicit ambiguity/counterexample to the attempted `C_3` unique factorization. | words, factors, and independent verification | OPEN |
| M-RED-001 | Independent human review of the self-contained closure lemmas (note §3.5: Brzozowski derivative, literal-inverse-morphism substitution) behind Proposition 3.1, and of the `Dic_3 ↪ (C_3×C_4)⋊C_2` order-12 audit in `RESULTS.md` §3. One AI adversarial pass (2026-07-23) found no gaps; machine companions `scripts/research/closure_lemmas_check.py`, `scripts/research/dic3_embedding.py` pass. | reviewer sign-off recorded in the ledger rows FULL-ALPH-RED-01, DIC3-RED-01, ORD12-ALL-01 | REVIEW |
| M-WEIS-001 | Audit the exact definitions of Weis's candidate languages (esp. L2) against Weis 2011 (UMass thesis, DOI 10.7275/2177022), incl. the "order-48 group" attribution and Weis's own height-one proofs. | definition transcript with page/theorem numbers (`notes/weis_2011_primary_audit.md`, DONE 2026-07-23); syntactic monoid recomputed from the audited definition (DONE: 48 ≅ C₂×S₄, `scripts/research/weis_l2_actual.py`); comparison with the stage-2 family theorem (DONE 2026-07-23: full L2 is NOT a function of the certified family or its tested extensions, exact witnesses in `scripts/research/weis_l2_actual.py`, ledger WEIS-L2-NOTFN-01); human cross-check of the audit note pending | REVIEW |
| M-EXP-PR2-001 | Independently audit and replay the imported `exploring-math` PR #2 claims used in `RESULTS.md` §6.1 (finite 4096 family, binary finite-code KR obstruction, fixed A5 raw stamp, and C4 one-marker boundaries). | checker commits and dependency hashes pinned; normal/`-O` outputs reproduced where applicable; proof notes checked against the exact merged commit; ledger rows CORE2-4096-EXT-01, GH1-KR-EXT-01, CORE2-A5-RAW-EXT-01, CORE2-SCH1-EXT-01 upgraded only claim-by-claim | REVIEW |
| M-SFA-PRIOR-001 | Prior-art search for star height measured over star-free (rather than letter) labels. **Done 2026-07-25; findings in `notes/sf_labeled_automata.md` §7 and §2.1a, ledger `SFA-EGGAN-01`.** `SFA-EGGAN-01` is confirmed NOT new: the mechanism is Sakarovitch, "Automata and rational expressions" (arXiv:1502.03573) §3.6, eqs (3.24)–(3.26) + Property 3.12 + Prop. 3.13 (Lombardy–Sakarovitch 2003) + Thm. 3.11 (Eggan 1963), which already indexes generalised automata by the star height of their labels; that source's `h` is the restricted star height, so only the instantiation to generalized star height with star-free labels is added here. Also established: the bound is an equality over elimination orders. No automaton-side treatment of *generalized* star height found in Sakarovitch, Gruber (arXiv:1111.5357), Bourne's NBSAN slides, or *Star Height via Games* (arXiv:1708.03603). **Residuals closed the same day**, both papers retrieved and read; findings in `notes/sf_labeled_automata.md` §2.1b and §7. (i) Hashiguchi 1988 Def. 2.6 (pp.127–128) defines relative star height over a FINITE base `𝒞` via the substitution `δ(b_i)=R_i`, with `h` restricted; since an SF-automaton has finitely many labels, `r_SF(L) = min over finite 𝒞 ⊆ SF of h_r(L,𝒞)` — so `r_SF` IS relative star height over star-free bases, not a new quantity. His main theorem makes `h_r(·,𝒞)` computable for each fixed `𝒞`; see the new `M-SFA-DECIDE-001`. (ii) PST 1992 retrieved: it contains no loop complexity / cycle rank / Eggan (automata only as recognizers, via transition monoids), so the automaton packaging is not there — but its **Theorem 4.6** (p.229f) IS the generalized-star-height substitution bound `h(Lσ) ≤ h(L)` for star-free injective `σ`, which removes the "added instantiation" that this obligation had previously credited to the repository. Its Lemma 6.1 (Transfer Lemma) also carries the `L₁*` star-free side condition of the self-loop absorption move. Optional and NOT blocking: Kirsten's Leipzig habilitation (no open copy) and the full text of Kirsten 2011 (only the 2-page preview was read). | closed: both definitions transcribed with page numbers in `notes/sf_labeled_automata.md` §2.1b/§7, and the verdict recorded — both subsume the content of `SFA-EGGAN-01`, whose ledger row now claims packaging only | DONE |
| M-COVER60-001 | **Closed 2026-08-01.** Re-derived every positive coverage verdict through order 60 inside this repository. GAP now emits identity-first multiplication tables and explicit C3/C4/C5/R1 witnesses for all 280 positive rows. The standard-library Python checker independently verifies every group axiom and the full sufficient criterion: C1 commutativity; C2 centrality of every commutator; C3 the subgroup, normality, abelian, exponent-two, trivial-intersection and product-cover conditions; C4 the defining dicyclic relations and normal forms; C5 a multiplication-preserving bijection onto the 12 even permutations of four points; R1 two surjective homomorphisms to already verified strictly smaller groups with nontrivial kernels intersecting only at the identity. It traverses 22,786,789 finite obligations and rejects all nine claim-level mutations. | `scripts/ci/verify_small_group_witnesses.py`; `data/experiments/coverage_le60_witnesses.jsonl`; `tests/test_small_group_witnesses.py`; verdict `data/verdicts/small_group_coverage_le60.json`; positive ledger row `COVER-LE60-POS-01` is COMPUTED. The required asymmetry is preserved: `COVER-LE60-RESIDUAL-01` and `FAMILY-PHASE-01` remain UNREVIEWED, so the 32-row residual is neither an exact non-membership theorem nor a height lower bound | CLOSED |

## New theorem routes

| ID | Target | Central missing lemma | Falsification test | Status |
|---|---|---|---|---|
| N-C3-001 | Height one for a specified `A ⋊ C_3` family. | canonical or unambiguous parsing replacing unique factorization | enumerate smallest overlaps and test claimed parser | OPEN |
| N-A4-001 | `HeightOneForGroup A4`. **Closed mathematically 2026-07-27** by `A4-FULL-01` plus the full-alphabet reduction (`A4-ALLLANG-01`). Lean formalization is tracked separately as `L-A4-001`. | `notes/a4_full_alphabet_exact.md`; `A4-FULL-FINITE-CORE-01`; `GSH.heightOneForGroup_of_fullIdentityFiber` | adversarial review found no logical gap; 16 acceptance tests and 17/17 controls pass | CLOSED |
| N-DIC3-001 | `HeightOneForGroup Dic3`. | transport through `C3 ⋊ C4` or alternative decomposition | same | OPEN |
| N-A5-001 | local certificates for all selected proper-subgroup restrictions. | precise restriction/gluing calculus | incompatible local accept sets | OPEN |
| N-A5-002 | `HeightOneForGroup A5`. | global gluing theorem not equivalent to target | adversarial subgroup intersections and coset actions | OPEN |
| N-LOWER-001 | sound invariant satisfied by all height-one languages. | closure under union, complement, concatenation, and one-level star | test against broad known height-one suite | OPEN |
| N-COH-001 | concrete cohomological transport lemma. | coefficient module and word/path-to-cocycle map | verify cocycle identity and behavior under language constructors | BLOCKED |
| N-L2-M3-001 | height ≤ 1 for stage-3 (phases mod 3) staged `ba*b` pair counts. | a certified height-1 feature reaching `T2 mod 2`: single-run-covering prefix codes are integrality-locked to the `(2·T1+T2) mod 3` combination, and the `(T1+T2)`-type combination needs the token `a`, which prefix-collides with the filler `aaa` | candidate feature suites judged exactly by `python3 scripts/research/weis_l2_family.py --m3` (currently: all stage-3 pair atoms NOT a function of LC/flags/flat code counts incl. cascade `Z3`/W atoms mod 6) | OPEN |

| N-L3-ANCHOR-001 | height ≤ 1 for Weis's L3 (syntactic monoid `S_5`, left open in Weis 2011). | a transitive action of the syntactic group in which every letter moves exactly the anchor pair, as the four-diagonal action does for L2 (`WEIS-L2-GSH-01`, RESULTS.md §5.10); for `S_5` the candidate actions are 5 points, the 6 cosets of `PGL_2(5)`, the 10 pairs, and the 15 involutions | for each candidate action: compute the induced permutations of the two letters, test the anchor criterion (`scripts/research/a5_frontier.py` style), then attempt the fiber separation with letter parities and compile-and-compare exactly as in `scripts/research/weis_l2_full_gsh1.py` | OPEN |
| N-L2-AUDIT-001 | independent human re-derivation and program audit of `WEIS-L2-GSH-01` / `WEIS-L2-RSH-01`. **Referee packet prepared 2026-07-28: `notes/weis_l2_review_packet.md`** — reading order, a ten-item checklist with a falsifier for each, the trust boundary, and the five places to push hardest. Done in preparation: the group data (order 48, uniqueness of `z`, `\|G'\| = 12`, `z ∉ G'`, the four diagonals, both cycle types, `parity(z) = (1,1)`, the 48/48 separation) re-derived from a fresh implementation and reproduced; the anchor-deletion argument and the explicit `R_{D₂}`, `R_{D₃}` traced by hand and correct; the ground truth anchored to PST 1992 §8 itself (`WEIS-L2-PST-SRC-01`); and the tightness identity `4 × 4 × 3 = 48`, which the derivation note does not state and which is the cheapest available consistency check, recorded. | **Still needed, in priority order.** (1) The prior-art question: the 2009 Weis talk abstract says all four candidates were shown to have height one while the 2011 thesis leaves full `L2` open; the slides were never retrieved and the author's UMass page is 404, so the repository's reading (`WEIS-TALK-01`) is an inference. This needs a human to write to Weis or Barrington, and it gates any external write-up. (2) An audit of the DFA compiler itself (`compile_dfa`, `d_star`, `d_min`) rather than of the three-path agreement — the one layer of §5's trust boundary that no check here covers. (3) A second reader on the human composition in `notes/weis_l2_full_height_one.md` §§2–4, especially the unique-factorization step at the last visit to the anchor. (4) For `WEIS-L2-RSH-01`, whether Lombardy–Sakarovitch Thm. 7.10 is applied inside its hypotheses | rerun `python3 scripts/research/weis_l2_pst_crosscheck.py`, `python3 scripts/research/weis_l2_full_gsh1.py` and `python3 scripts/research/weis_l2_restricted_height.py` after any compiler edit; all must stay green with the manifest hashes in `data/experiments/weis_l2_full_gsh1.md` | OPEN |
| N-F20-001 | `HeightOneForGroup F_20`, `F_20 = C_5 ⋊ C_4` with faithful action — the smallest non-abelian group outside the PST class (`FRONTIER-ORD20-01`, order 20). **Progress 2026-07-25**: the two-generator word problem is settled, gsh = 1 (`F20-STD-01`, `RESULTS.md` §5.11), via the W-atom scheme at phases mod 4 and counts mod 5 (`WATOM-45-01`). What remains is exactly the full 20-letter alphabet, which by `FULL-ALPH-RED-01` is equivalent to the obligation. **Correction 2026-07-25 (this branch)**: the clause above saying `SUBDIRECT-RED-01` "removes `C_2×A_4` from `FRONTIER-ORD20-01`" assumed `A_4` is height-one, which is `EMPIRICAL` (`A4-FULL-01`). `C_2×A_4` does not leave the list; it collapses onto `A_4`. The `C_2×S_4` → `S_4` collapse and the `DIC3-ALL-01` re-derivation are unaffected, since neither consumes `A_4`. | **Narrowed 2026-07-25 by `F20-FULL-OBS-01`**: the §5.5 mechanism fails on all 291 cut patterns, and the obstruction is exactly the five `ε = 2` letters — `Z/4` is composite, so their phase orbit `{0,2}` lets them bounce `1 ↔ 3` without meeting the cut phase. The `A_4` mechanism required a phase group of *prime* order. The GF(5) linear algebra is NOT the bottleneck: formally, `β` is determined. So what is missing is a cut construction that survives a composite phase group | **Route (ii) BLOCKED 2026-07-25 by `F20-SUB10-OBS-01`** (was: "first close the 15-letter sub-alphabet `ε ∈ {0,1,3}`, where the base cut already certifies"). Exact obstruction: on the 10-letter sub-alphabet `ε ∈ {0,1}`, which already generates `F_20`, **no pair pattern is certifiable at all** (period 2, or 4 for same-letter pairs), so the mechanism cannot order two distinct letters with equal `ε` and different `β`; the words `k u₀ u₁ k` and `k u₁ u₀ k` agree on every certified feature in both directions but differ in the group image. The witness contains no `ε = 3` letter, so the 50 pair patterns the 15-letter alphabet does certify never fire, and it is still not separated there. Adding 555 uncertified `Σε = 0` patterns for free does not help. **Route (i) is also insufficient on its own**: a filtration of the phase group does not address the ordering of two letters that carry the *same* `ε`. **Route (iii) BLOCKED 2026-07-25 by `F20-BLOCK-OBS-01`** (was: "abandon phase-arrival cuts for a finite-code block decomposition in the style of the §5.9 cascade code, so that the ordering information is confined inside a block whose `β` contribution is extractable"). Exact obstruction — the **delay theorem**, valid for every finite group: if `Σ = G` is the full alphabet then the identity element is a letter, so for any finite `X` with bounded delay and any `z ∈ G` the word `z e^D` has a prefix in `X`, and every nonempty prefix of it has image `z`; hence `mu(X) = G`. Parsing only the fibre gives the same conclusion up to the identity (`mu(X) ⊇ G ∖ {e}`, via `z e^n z^{-1} ∈ T`). Since the token word is again a word over an alphabet of group elements with the same coordinate formula, the block decomposition is a **self-reduction with no gain**, and the `ε = 2` letters of `F20-FULL-OBS-01` reappear at the token level: the base cut has a period-2 element there too and 0 of 291 patterns certify (token multiplicity does not repair it — verified on the genuine 400-token alphabet `Σ²`). Giving up finiteness does not help either: the one canonical phase-neutral code, the first-return code `R` with `T = R*`, is infinite *and* not star-free, because its DFA **is** the §5.5 base cut. **That residue is closed too, same day, by `F20-BASECODE-01`** (was: "star-free infinite codes, the most promising residue"). `T` is biunitary hence a free submonoid, so its base is *unique* — the first-return code `R_T` — and "is there a star-free code `C` with `C* = T`?" is a single decision problem, answered no: the minimal DFA of `R_T` has 22 states and one letter has period 5 in its syntactic monoid. **And the route was never viable**: the same computation on the 2-generator alphabet, where `gsh = 1` is proved by `F20-STD-01`, also returns "not star-free", so the condition is sufficient-only and already fails where height one holds. **Methodological consequence, to be applied to routes (iv) and (v): test any proposed mechanism on the 2-generator alphabet first — a mechanism that fails there cannot succeed on the full alphabet.** Still not excluded for route (iii), but neither is yet in the shape of a mechanism: non-greedy or context-dependent factorizations, finite unions using a different code per case, and stripping the four non-identity `ε = 0` letters. By-product of this route: `FULL-ALPH-RED-02`, the full-alphabet reduction needs only `|G| − 1` letters. **Route (iv) worked 2026-07-25 and is NOT blocked** (was: "exploit closure properties other than reversal — inverse *alphabetic* morphisms preserve height, so reconstruct the full alphabet as an intersection of reduced-alphabet instances"). Three results, all in `notes/f20_alphabetic_reduction.md`. (a) The inverse-alphabetic closure itself is now proved in-repo for the non-erasing case (`ALPH-RED-01`), so this route no longer rests on `PST-CL-01` (`CITED`). (b) The *identity-fibre* form of the route is rigid and blocked: it forces every relabeling to be a group homomorphism, so for the monolithic `F_20` it can only reach `mu^{-1}(N)` with `N ⊇ C_5` (`F20-QUOT-OBS-01`) — its one gain is the partial positive `gsh(mu^{-1}(C_5)) ≤ 1`, which localizes the whole remaining difficulty in the `C_5` coordinate at phase 0. (c) With **general accepting sets** the route is strictly stronger and delivers a real reduction: `HeightOneForGroup F_20` follows from `gsh(T_Δ) ≤ 1` for the single 8-letter alphabet `Δ = Z/4 × {0,1}`, and from 7 letters after erasing the identity (`F20-ALPH8-01`) — against 19 letters from `FULL-ALPH-RED-02`. The route also produced the general closure theorem `SUBDIRECT-RED-01`, which removes `C_2×A_4` from `FRONTIER-ORD20-01`, collapses `C_2×S_4` into `S_4`, and re-derives `DIC3-ALL-01` independently. **What route (iv) cannot do**: by Lemma G of `F20-ALPH8-01` it can never cross the frontier using known height-one *groups*, only alphabet-restricted results like `F20-STD-01`; and the 8-letter instance evades neither `F20-FULL-OBS-01` (the `ε = 2` letters are still in `Δ`) nor `F20-SUB10-OBS-01` (its minimal witness is a `Δ`-word), so this is a smaller obligation and not a mechanism. Successor obligations: `N-F20-ALPH2-001` (reach 2 letters and `F_20` closes) and `N-ALPH-CITE-001`. Remaining route, not started: (v) switch to the lower-bound side and target height ≥ 2, noting that no generalized-star-height lower bound above 1 is known for any language, so this is not to be started lightly. **Route (vi) opened 2026-07-25 by `F20-TRANSD-RED-01` — the first route to pass the 2-generator calibration.** Reading the split extension as a fibration (base `Z/4`, fibre `Z/5`) makes `β` a 1-cocycle (`F20-FIB-01`) and gives `mu⁻¹(e) = {ε ≡ 0} ∩ σ⁻¹(K)` with `σ` the suffix-phase transducer (state set `Z/4`) and `K` a `C_5`-counting language over `Γ = Z/4 × Σ`; both factors have gsh ≤ 1, so everything reduces to whether `σ⁻¹` preserves gsh ≤ 1. **Do not attack that in the single-`K` form** — it is close to a restatement and falls under the stop condition. Attack `TRANSD-ABEL-01` (abelian state group) instead, *after* `N-FIB-PRIOR-001`. Two negative facts bound the route: `σ⁻¹` does not preserve star-freeness, so no star-free closure theorem can supply the step; and `F20-COH-SEP-01` shows the extension cohomology is identically zero across `C_20`, `Dic_5`, `F_20`, so no cohomological obstruction is available — the only parameter that tracks the frontier is the monodromy order 1, 2, 4 (`F20-MONO-FRONT-01`), which is also the hard irrep's dimension and half the 5-primary cohomological period. **Relation between routes (iv) and (vi)**: they meet, but neither contains the other. Route (iv) delivered a reduction on its own — 20 letters down to the 8-letter `Z/4 × {0,1}`, 7 after erasure — and Lemma G of `F20-ALPH8-01` bounds it: it can never cross the frontier using known height-one *groups*, only alphabet-restricted results. Route (vi)'s marked alphabet `Γ = Z/4 × Σ` has the same `phase × letter` shape but runs the other way (80 letters, a marking rather than a reduction), and rewriting the `F20-STD-01` expression over `Γ` and pulling it back along `σ` is an inverse-alphabetic-morphism reconstruction in exactly the sense of `ALPH-RED-01`, which is now proved in-repo rather than cited. The sharpest common target is `N-F20-ALPH2-001`: if route (iv) reaches 2 letters, `F_20` closes without route (vi) | OPEN |
| N-A4FULL-002 | Decide step [3] of `A4-FULL-01`: reconstruct every mod-2 count `N[g,p]` from certified height-one features for every word, and close the full-alphabet composition. **Closed 2026-07-27.** `check_a4_full_feature_equations` traverses 5,792 reachable states; `check_a4_full_reconstruction` traverses 36,872 states for all eight movers and derives reverse observables from successor pairs; the combined step [3] verdict is `COMPUTED`. Token factorization and step [4] are also exact, and the finite parent is `A4-FULL-FINITE-CORE-01` with 17/17 controls. `notes/a4_full_alphabet_exact.md` gives the human composition with Schützenberger's theorem; adversarial review found no logical gap. | `tests/test_a4_full_exact.py`; `scripts/ci/completeness_upgrade.py`; `data/verdicts/completeness_upgrade.json`; `notes/a4_full_alphabet_exact.md` | CLOSED |
| N-C7C3-001 | `HeightOneForGroup (C_7 ⋊ C_3)` — smallest odd-order group outside the PST class (order 21), and the minimal instance of Bourne's failed `A ⋊ C_3` step (`SMALL-C3-FAIL`). **Progress 2026-07-25 (`C7C3-FULL-01`, `RESULTS.md` §5.16)**: the `A4-FULL-01` multi-mover mechanism transposes to the **full 21-letter alphabet** — which by `FULL-ALPH-RED-01` is the whole obligation — with all 288 cut patterns aperiodic, GF(7) rank 6/6, and exact reconstruction of the identity fibre on all 204205 words of length ≤ 4. The phase group is `Z/3`, of prime order, so the `Z/4` obstruction of `F20-FULL-OBS-01` does not arise. | **Narrowed twice 2026-07-25**: the arithmetic half is now `PROVED` (`C7C3-IDENT-01`, `RESULTS.md` §5.16.1) — the identity fibre is a Boolean combination of the residue languages of 57 cut counts whose token languages are certified star-free, for every word and with no length bound. What is missing is exactly one thing: a **star-free expression** for each token language. Nothing else is needed, because with `V` the language of words that never cut, `T = ¬V ∩ ¬(¬V · Σ⁺)` and the residue language is `cat(T_first, T^h, star(T^7), V)`, so all remaining operations are Boolean, concatenation and a single star | **Route survey 2026-07-25 (`notes/c7c3_token_starfree.md`)**: the 21 letters collapse to 3–4 symbols per pattern (machine-checked congruence), and over that collapsed alphabet with a left anchor the `base` and `set` token languages are *strictly locally testable* (widths 3 and 4), so those 12 atoms have explicit forbidden-factor star-free expressions. The 45 `anti` atoms are not: whether a given `g` is a phase-q jump depends on the jump status of the mover two before it, and the chain only breaks at a nonmover, so in mover-only words it runs back to the start. (1) the *local-only* route is now BLOCKED: all 528 locally-writable cut observables in both directions plus the 21 letter counts mod 7 fail to determine β — witness, at length 4, `g(e=1,b=0) g(e=2,b=0) g(e=2,b=0) g(e=1,b=1)` (β=4) against its reversal `g(e=1,b=1) g(e=2,b=0) g(e=2,b=0) g(e=1,b=0)` (β=1). Note the same survey found a partial crack that does *not* suffice: `("pair", h, g)` is strictly local exactly when `h` is a mover of the same class as `g` (6 of the 20), because that pins the phase two steps back; (2) the remaining route is a general aperiodic-DFA-to-star-free construction (Schützenberger) applied to the 5-state, 4-symbol token automata — small enough to be practical, and every output is checkable by exact DFA equivalence, so the risk is constructibility rather than correctness. Then assemble with the running-sum tree and check the height symbolically; the assembled expression need NOT be compiled, since its correctness follows from `C7C3-IDENT-01` algebraically; (2) if (1) fails, fall back to the original route: enumerate the transitive actions (7 points, 3 cosets of `C_7`, 21 points) and test the anchor criterion on each, noting that `C_7 ⋊ C_3` has no subgroup of index 2 so parity-based fiber separation is unavailable | OPEN |
| N-SL23-001 | `HeightOneForGroup SL(2,3)` (order 24, outside the PST class, monolithic). Added 2026-07-26: it appears in the `FRONTIER-ORD20-01` list and in `PROGRESS.md`, and it was the only one of the five independent groups with no obligation row — so it was named as open everywhere and tracked nowhere. `SL(2,3) = Q_8 ⋊ C_3`, and its **Fitting subgroup `Q_8` is non-abelian** — the first frontier group for which that is true, so the `A ⋊ cyclic` counting shape of `A4-STD-01` / `F20-STD-01` does not present itself at all. (Decided by `scripts/research/small_group_pst_coverage.py` §[7], which re-runs in CI: order 24, normal subgroup orders 1/2/8/24, Fitting subgroup of order 8 with the `Q_8` order profile and non-abelian, four subgroups of order 3 and none normal, trivial intersection, product of order 24. The first version of this row asserted the same enumeration with no check anywhere in the repository — run by hand, written down — which is the provenance failure this file exists to prevent.) | a height-one mechanism for a non-abelian normal subgroup, or a reduction to one | **closing test:** a generalized expression of star height ≤ 1 for the identity fibre over the full 24-element alphabet, checked for language equality against the word-problem DFA by exact product reachability (as `A4-STD-02` does), which with `FULL-ALPH-RED-01` closes `HeightOneForGroup SL(2,3)`. **Falsification:** exhibit a language recognized by `SL(2,3)` outside the certified feature family, which refutes the current mechanism without bounding the height from below (research rule 1). | OPEN |
| N-S4-001 | `HeightOneForGroup S_4` (order 24, outside the PST class). **Narrowed 2026-07-25**: the second half, `HeightOneForGroup (C_2 × S_4)`, is no longer an independent obligation — `C_2 × S_4` is not monolithic, so `SUBDIRECT-RED-01` derives it from `HeightOneForGroup S_4` alone. | a mechanism for a non-cyclic quotient: `S_4 / V_4 ≅ S_3`; `WEIS-L2-GSH-01` settled one `C_2×S_4`-recognized language, not the whole family | the full-alphabet reduction (`FULL-ALPH-RED-01`) makes the 24-letter identity fibre the single target; attack it with the diagonal-anchor pipeline of `scripts/research/weis_l2_full_gsh1.py` | OPEN |
| N-ERASE-AUDIT-001 | Independent audit of `FULL-ALPH-RED-02`: the identity letter may be erased, so the full-alphabet reduction of `FULL-ALPH-RED-01` needs only `|G| - 1` letters. The lemma is that `pi^{-1}` for the erasing morphism `pi : Σ* → (Σ∖{e})*` preserves generalized star height ≤ 1. | `FULL-ALPH-RED-01` (`PROVED`) | Re-derive the four steps by hand, in particular the star step `pi^{-1}(A*) = (B ∪ {e})*` with `B = pi^{-1}(A)` minus the empty word and minus everything ending in `e`; confirm that the naive `({e} ∪ A)*` is genuinely false (counterexample `A = {a, ka}`, `w = k e a`); and reconcile with PST 1992 item 7, which says non-alphabetic inverse morphisms do NOT preserve height in general — the claim is that the concatenation step is what fails for length-increasing morphisms and holds for erasure. Machine support: `scripts/research/f20_block_decomposition.py` §6 | OPEN |
| N-FIB-PRIOR-001 | Literature check on `F20-TRANSD-RED-01` / `TRANSD-ABEL-01` before any further effort is spent on route (vi). The decomposition `mu⁻¹(e) = {ε ≡ 0} ∩ σ⁻¹(K)` looks like a specialisation of Straubing's wreath product principle, and the conjecture that `σ⁻¹` preserves gsh ≤ 1 for abelian-state transducers may be known, or known to be false. | none — this is a search, not a proof | Search Straubing 1979/1985 (wreath product principle), Pin, *Varieties of Formal Languages* (the wreath-product and sequential-function chapters), and the combination "sequential function" + "star height". Record the exact statement and hypotheses in `CLAIMS_LEDGER.md` as `CITED`; if the conjecture is refuted in the literature, mark `TRANSD-ABEL-01` `REFUTED` with the counterexample and close route (vi). Note the precedent: `PST-WREATH-06-01`'s primary source was unobtainable — **no longer, as of 2026-07-28**: `https://www.irif.fr/~jep/PDF/StarHeight.pdf` yields to `curl -sL` with a browser user-agent, and PST §7 has been read. It contains no abelian-state rung (7.6 is elementary abelian 2, 7.8 is aperiodic, nothing between), so this search is narrowed to the non-PST sources listed above, so budget for that outcome and do not let an unreachable PDF become an implicit citation. **RUN 2026-07-25 — PARTIAL, and the route survives.** Found: (1) `PST-WREATH-78-01` — PST 1992 **Theorem 7.8** is "every language in the pseudovariety generated by wreath products of abelian groups by **aperiodic** monoids has star-height at most one", quoted verbatim from Bourne–Ruškuc arXiv:1603.06236. This is exactly the **aperiodic-state** case of `TRANSD-ABEL-01`, supersedes the vaguer `PST-WREATH-06-01` wording, and explains `PST-WREATH-COMM-01` structurally. (2) `EIL-WPP-01` — the transducer decomposition is Eilenberg's wreath-product machinery (Prop. IX.1.1, modified for generalized sequential functions), so **no novelty may be claimed** for the construction in `F20-TRANSD-RED-01`. (3) The **abelian-state** case was not located: neither proved nor refuted in the surveyed sources. (4) Scope now pinned by `TRANSD-LADDER-01`. NOT obtained: the PST 1992 full text (HAL → Anubis, ScienceDirect → 403, irif.fr → 403, web.archive.org unreachable from this environment), so the theorem number rests on a secondary source and "not found" must not be read as "not known". **Remaining sub-task**: get PST 1992 §7 and the sequential-function section through an institutional library, and check Pin, *Varieties of Formal Languages*, ch. on wreath products | PARTIAL |
| N-TRANSD-ABEL-001 | `TRANSD-ABEL-01`: `σ⁻¹` preserves generalized star height ≤ 1 when the transducer's state monoid is a finite abelian group. Settles `N-F20-001` as the case (state group `C_4`, input a `C_5`-counting language), and more. | `N-FIB-PRIOR-001` first. Then a construction that turns a height-1 expression over `Γ = Q × Σ` into one over `Σ`; the known input is that `PST-GRP-03` covers the elementary-abelian-2 state group, so the first genuinely new case is a cyclic state group of order 4 | Start from the `F20-STD-01` height-1 expression, which is an existence proof for exactly that case on two letters, and try to make it alphabet-independent. Then run the 2-generator calibration on whatever emerges before scaling to 20 letters. Expect the statement to be **false** for non-abelian state groups (otherwise `A_5` would fall, contradicting `GSH-STATUS-01`); hunting that counterexample is cheap and delimits the scope. **Scope pinned 2026-07-25 by `TRANSD-LADDER-01`**: with no hypothesis on the state monoid the statement is *equivalent to the whole generalized star-height conjecture* (mark each letter with the DFA state and the target becomes star-free), so the non-abelian version is not merely false-looking, it is the entire open problem. With the abelian hypothesis it settles **every finite solvable group** — verified concretely: the Krasner–Kaloujnine step `G ↪ G' ≀ (G/G')` is an injective homomorphism at every stage of the derived series for all six groups outside the PST class. That is a warning as much as a motivation. **Therefore attack the minimal open rung — state monoid `C_4`, input a `C_5`-counting language — and not the general conjecture.** The rung below it (aperiodic) is `PST-WREATH-78-01`; the rung beside it (elementary abelian 2) is `PST-GRP-03` | OPEN |
| N-SFA-RANK2-001 | Rank reduction for star-free-labelled automata: every SF-automaton of loop complexity 2 accepts a language that is a Boolean combination of rank-1 SF-automaton languages. By `SFA-CORE2-RANK-01` this is equivalent to `GLOBAL-ONE` (modulo the external `CORE2-EQV-EXT-01`), so it is a full-strength target, not a lemma — see the stop condition in `AGENTS.md`. | a transformation turning the two-state shape `(A ∪ B D* C)*`, `A,B,C,D` star-free, into a Boolean combination of rank-1 SF-automata. The only transformations currently known to preserve star-freeness of labels are self-loop absorption and quotient of the action (`notes/sf_labeled_automata.md` §6); neither applies to a self-loop labelled by an arbitrary star-free `D`, because `D*` need not be star-free | build the two-state SF-automaton for concrete star-free `A,B,C,D` with `tools/sf_automaton.py`, eliminate to a height-2 expression, and run `python3 -m tools.height_search` on its minimal DFA for small instances; a proposed transformation is falsified as soon as one instance's output fails `scripts/ci/check_certificate.py` | OPEN |
| N-SFA-AUDIT-001 | Independent program audit and human re-derivation of `SFA-EGGAN-01` and `SFA-L2-MEASURE-01`: in particular the cycle-rank recursion, the choice of rank-witness vertex, and the condensation stage of `tools/sf_automaton.py`, which are trusted by every certificate the module emits. | a reader who checks the §2 induction by hand and re-derives `R_{D₂}` from `notes/weis_l2_full_height_one.md` §3 independently of the eliminator | rerun `python3 scripts/research/sf_automaton_calibration.py` and `python3 -m unittest tests.test_sf_automaton` after any edit; both must stay green with the manifest hashes in `data/experiments/sf_automaton_calibration.md`, and the two emitted certificates must keep passing `tools/regex_cert.py` | OPEN |
| N-F20-ALPH2-001 | Can the route-(iv) scheme of `F20-ALPH8-01` reach a **2-letter** alphabet? Precisely: do there exist `k` relabelings `f_j : F_20 → Δ_j` with `\|Δ_j\| = 2` such that `mu` factors through `Φ = (mu_1,…,mu_k)`? A positive answer closes `HeightOneForGroup F_20` outright, since `F20-STD-01` settles the 2-generator instance and `ALPH-RED-01` lifts it. | `F20-ALPH8-01` (`PROVED`), `ALPH-RED-01` (`PROVED`), `F20-STD-01` (`COMPUTED`), plus the right-quotient step of `FULL-ALPH-RED-01` §3.5 to go from the identity fibre to all accepting sets over a 2-letter alphabet | The criterion is decidable: `mu` factors iff `K = ⟨(λ(g), g) : g ∈ Σ⟩ ≤ (∏_j F_20) × F_20` satisfies `K ∩ (1 × F_20) = 1`. Established necessary conditions (`scripts/research/f20_alphabetic_reduction.py` §7): the counting bound `∏_j \|Δ_j\| ≥ 20`, so 2-letter alphabets need `k ≥ 5`; `ord(g) \| lcm_j ord(f_j(g))` for every letter; and Lemma G, at least one coordinate generates `F_20`. The blind search space is astronomically large, so the recommended attack is the structural one: inside the equal-phase family the minimum is exactly `4 × 2 = 8` letters (all-singleton images are impossible because the sum would be constant), so **the ansatz to break is "equal phase"** — take `rho` on a subgroup of `F_20^k` that does not require the coordinate phases to agree. Note the achieved family sits at `8^4 = 4096 ≫ 20`, far above the counting bound, which is why this is open rather than blocked | OPEN |
| N-ALPH-CITE-001 | Literature check for `SUBDIRECT-RED-01`: is "the class of height-one groups is closed under direct products" already in print? The proof in `notes/f20_alphabetic_reduction.md` §4 is three lines given `ALPH-RED-01` and `FULL-ALPH-RED-01`, so it is a natural candidate for folklore, and the claim must not be reported as new until this closes. | `SUBDIRECT-RED-01` (`PROVED`, so the mathematics does not depend on the answer) | Check Pin–Straubing–Thérien 1992 for a closure list, then the star-height survey literature 1992–2026; the wording to search for is closure of a *group* class rather than of a language class. If found, downgrade the row's novelty note and cite; if not found after a real search, record that explicitly rather than leaving the caution in place forever | OPEN |
| N-TOPOS-001 | Decide the comparison equality of `TOPOS-ISO-01` in the local state classifier. Prerequisite: extract the Kobin construction so that the Navir isotropy generators have a canonical definition — that missing definition is exactly why `TOPOS-ISO-01` is `SPECULATIVE` and not `CONJECTURAL`, and the falsification test cannot be coded until it exists. Source, target theorem package, and the two tests below are in `docs/Cenceptual_understanding.md` §11 and §13, deleted 2026-07-27 and restorable by `git show 230cde4:docs/Cenceptual_understanding.md`. **Nothing has been started**: no code, no Lean, no audited source. Read `TOPOS-BASE-01`, `TOPOS-ORB-01` and `TOPOS-KAR-01` first — the reformulation this route rests on is those three claims together, none reconstructed here, so the cheap failure mode is to build on unverified citations. | the equality, in the complete lattice of internal filters, of the Navir isotropy-generated filter with the filter generated by the star images of star-free languages. It is an equality of two generated filters, so each inclusion can be attacked separately. Note what it does NOT give even if true: the route still needs a relative-anisotropy theorem, because the local monoid contains more than isotropy and all isotropy may be too large (that document §6.1, §6.3) | two finite tests, both refutation-only by construction. (a) Filter test: on finite quotients, generate both filters and compare. (b) Finite-monoid test: for a small generated finite monoid, enumerate idempotents, build the Karoubi envelope, enumerate the maximal subgroups of each local monoid, approximate the base local variety by bounded star-free generators, compute the approximate orbit monoids, and compare their maximal subgroupoids against the predicted isotropy generators. Agreement on finite objects proves nothing — the approximation is bounded — but a single disagreement refutes the identification, which is the point of running them first | BLOCKED |
| N-FAMILY-A-001 | Run the multi-mover mechanism of `RESULTS.md` §5.5 on the five untested Family A groups of `FAMILY-PHASE-01`: `C_13 : C_3` (39), `(C_4 x C_4) : C_3` (48), `C_11 : C_5` (55), `(C_2)^3 : C_7` (56), `C_19 : C_3` (57). `scripts/research/c7c3_full_alphabet.py` is the template but is hard-coded to GF(7) and phase `Z/3`, so the work is to parameterize it by the abelian kernel and the prime phase, then run it. Note what a pass would and would not give: `C7C3-FULL-01` passed and is still `EMPIRICAL`, because certifying the patterns and solving the linear system is not the same as proving language equality, so five more passes produce five more `EMPIRICAL` rows and NOT a covered family. | a mechanism that either certifies the patterns and reconstructs the identity fibre for each of the five, or reports the exact obstruction per group | the same-letter pure-power patterns must FAIL for every group before any pass is trusted, as in §2 of the template, and `F_20` must still fail — a parameterized mechanism that suddenly certifies `F_20` has broken `F20-FULL-OBS-01` and is wrong, not a breakthrough | OPEN |
| N-NILP3-001 | The four 2-groups of order 32 that `FAMILY-PHASE-01` isolates — `(C_8 : C_2) : C_2`, `(C_2 x C_2) . (C_4 x C_2)`, `C_4 . D_8`, `(C_2 x Q_8) : C_2` — are nilpotent of class exactly 3, so they miss `PST-GRP-02` by one step and have no `abelian : cyclic` split for the phase mechanism to use. They are the smallest groups in the surveyed range whose obstruction is nilpotency class. Decide whether the class-2 argument of `PST-GRP-02` extends to class 3, or exhibit where it breaks. | the step of the class-2 proof that uses commutativity of the commutator subgroup, stated exactly, together with what replaces it at class 3 | reconstruct the class-2 mechanism first on a class-2 group of order 32 and confirm it certifies there; a proposed class-3 extension that cannot reproduce the class-2 case is falsified before it is applied | OPEN |

**Blocker for N-TOPOS-001:** both stop conditions of `AGENTS.md` apply at once. (i) Undefined notion: the Navir isotropy generators have no canonical definition, so neither §11 test has an object to compute — there is nothing to code, not merely nothing coded. (ii) Unverified citations: the reformulation the whole route stands on is not one claim but three, all `UNREVIEWED` and all resting on different literature — `TOPOS-BASE-01` (the height-one class is the star-free closure of the base local variety), `TOPOS-ORB-01` (the problem is equivalent to aperiodicity of the Place–Zeitoun orbit monoids, cited with no page or theorem number), and `TOPOS-KAR-01` (the Karoubi-envelope and aperiodicity facts, called standard and convention-sensitive). Unblocking takes **both**, and they are different work on different sources: (i) is cleared by extracting the construction from the Kobin notes with a source location, which is what supplies the definition the tests need; (ii) is cleared only by auditing **all three** of (ii-a) `TOPOS-BASE-01`, (ii-b) `TOPOS-ORB-01` and (ii-c) `TOPOS-KAR-01` — each to an exact statement with page or theorem number, or a reconstruction here — and since their sources are different, discharging one says nothing about the others. The Kobin extraction touches none of them. Clearing (i) alone leaves a computable test standing on unverified citations, and clearing (ii) alone leaves a verified reformulation with nothing to compute; the row stays `BLOCKED` until (i) and all of (ii-a)–(ii-c) are done. Cheapest first: (ii-a) may already be most of the way there via the PROVED `SFA-STAR-ONLY-01`, and if it fails, the route dies before either of the other two audits is worth starting. Until then the route may not be worked, and it must not be counted as an open lead in any summary.

**Blocker for N-COH-001:** no coefficient object or exact map has yet been selected. Invoking an LHS spectral sequence before closing this gap is out of scope.

**M-WEIS-001 unblocked (2026-07-23):** the full thesis PDF (143 pages) was retrieved via the Wayback Machine and the 2009 seminar abstract directly; URLs, bibliographic data, verbatim quotes, and the corrections they force (L2 was proposed by PST 1992; full L2 and L3 are left OPEN in the thesis; the order-48 monoid is real, ≅ C₂×S₄) are in `notes/weis_2011_primary_audit.md`. The earlier claim "Weis showed all four candidates have height one" survives only as a statement about the 2009 abstract, not about the thesis. Remaining: the §5.9-family vs actual-L2 comparison and a human cross-check of the audit.

## First-build repair log — condensed 2026-07-25, full text at `230cde4`

The source was generated without a local Lean installation. The first real build
needed six repairs: `MulOneClass` on raw `List α` (the free monoid is
`FreeMonoid α`), a placeholder syntactic-quotient instance, the certificate
checker's soundness boundary, the `HeightOneForMonoid` / `HeightOneForGroup`
quantifiers, four milestone declarations, and `decide` failing on `Nat.card`.
After them `./scripts/check.sh` passed end to end on 2026-07-22 (macOS, lean4
v4.32.0 + mathlib v4.32.0, 1446 `lake build` jobs).

The per-entry detail is deleted rather than kept, because all seven files it
named were renamed away by the 2026-07-23 and 2026-07-25 reorganizations and not
one of them exists; where each went is recorded in the obligation rows above,
which is the copy that stays true. A repair log whose every path is dead costs a
reader more than it tells them. `git show 230cde4:PROOF_OBLIGATIONS.md` has the
original.

It is worth saying how those dead paths survived the gate that just caught this
paragraph. `lint_claims.py` checks that a cited path exists, and the log's paths
sat in `###` headings without backticks, so the check never saw them. The rot
was invisible to a checker that was already running, and it stayed invisible
because nobody re-read a section that looked like settled history.
